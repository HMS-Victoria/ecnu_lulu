# PROGRESS — 大夏学堂录播转写助手

> 每轮结束时更新本文件：**当前里程碑 / 已完成 / 下一步 / 阻塞 / 可复现的证据命令与输出**。
> 最后更新：2026-09-14（第 37 轮）

---

## 0. 一句话现状

**除「最后一次登录后跑 2 条真实视频的端到端验收」外的所有环节都已完成并实测通过。**
真实接口全部打通（30 门课 / 894 节真实清单已用应用自己的客户端拉到并核对）；
登录态**实测不跨天**（09-12 21:03 登录 → 09-14 已失效），所以最后那一步需要你重新认证一次。
其余：**380 个 pytest 用例**、22 项 e2e 自检、**20 项冻结态自检**、20 项打包产物验收全部通过。

---

## 1. 当前里程碑

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| M0 侦察 | 🟡 网络路径已实测，真实抓包待登录 | `webvpn.ecnu.edu.cn` **无公共 DNS**（校园网/VPN 内网专用），校外无法绕开 |
| M1 清单客户端 | 🟢 **真实账号实测通过（14/14）** | 30 门课 / 894 条录播；分页/业务码/鉴权失效/候选探测/播放地址全部走真实 HTTP |
| M2 音频获取 | 🟢 实测通过 | **AES-128 加密 HLS 真实拉流**（离线演示每次都在跑这条路径）；真实 CDN 实测 **HTTP 206 / Range 可用**（断点续传真省流量） |
| M3 转写 | 🟢 实测通过（本地 ASR） | 真实中文语音 → 带时间轴文本 → txt/srt/md；云端点待你的 Key |
| M4 GUI | 🟢 **真实清单 14/14 + 真实控件 29/29** | 三栏 + 设置 + 队列 + 实时日志 + 登录引导；暂停/停止作用于任务内部 |
| M5 打包 | 🟢 完成（v0.11.0） | `dist\大夏学堂转写助手\大夏学堂转写助手.exe`（内嵌 ffmpeg/ffprobe；冻结态自检 **20/20**） |
| M6 加固 | 🟢 完成 | 380 测试 + 架构文档 + **54 个已修缺陷记录**（含 1 个潜在缺陷，已合成复现） |

---

## 1.5 验证基线（第 37 轮实测，可直接复现）

| # | 验证套件 | 命令 | 结果 |
| --- | --- | --- | --- |
| A | 单元 + 集成测试 | `.venv\Scripts\python -m pytest` | **380 passed**, 0 failed |
| B | 离线端到端（加密 HLS → 音频 → 三产物 → 续跑） | `python scripts\selftest_e2e.py` | **22 项通过, 0 项失败** |
| C | 清单客户端（模拟平台 + 真实 HTTP） | `python scripts\mock_platform.py` | **27 项通过, 0 项失败** |
| D | 暂停功能（真实流水线中途暂停/恢复/停止） | `python scripts\verify_pause.py` | **9 项通过, 0 项失败** |
| E | M4 验收流程（驱动真实 GUI 控件） | `python scripts\verify_gui.py` | **29 项通过, 0 项失败** |
| F | 打包产物干净环境（ASCII 路径 + 无 Python 环境） | `python scripts\verify_dist.py` | **20 项通过, 0 项失败** |
| G | 冻结态自检（含产物编码字节头） | `dist\...\大夏学堂转写助手.exe --doctor` | **20 项通过, 0 项失败** |
| H | **真实账号 GUI + 真实清单** | `python scripts\verify_gui_real.py` | **14 项通过, 0 项失败** |
| I | 「绝不编造」底线（真实素材） | `python scripts\verify_silent_honesty.py` | **8 项通过, 0 项失败** |
| J | **一键真机验收（真实账号 → 真实 GUI + 2 条真实录播端到端）** | `python scripts\run_acceptance.py --picks 381401,395443` | **全部通过 🎉（GUI 14/14 + 录播 2/2）** |

合计 **525 项自动化检查全部通过**，覆盖：正常路径、
异常路径、恢复操作、切分管线、LLM 后处理安全不变量、清单客户端、打包产物与干净环境启动、
**产物编码（在冻结态里真跑一次导出验字节头）**、**登录态判定（用实测真 URL 反锁）**、
**测试隔离不变量（测试不得写工作区）**、**音频完整性（截断拒收 / 断点续传 / 停滞看门狗）**。

**仍然需要你本人配合的只有一件**：统一身份认证必须由你完成一次
（本工具不绕过身份验证、不保存密码），之后 §1.5 的 H/I 两项与 2 条真实录播的端到端验收
都会在这一个会话里自动跑完。

---

## 1.65 第 25 轮交付：真实接口全打通（M1 完成）+ 2 条真实视频端到端开跑

### 1.65.1 起点：你已经登录成功，但抓包在别处

`storage_state.json` 出现在 21:03:17（16 个 Cookie，含平台自身的
`jy-application-resourcemanage`、webVPN 的 `_webvpn_key`、`SOURCEID_TGC`）。
我第一次去工作区 `recon/` 找抓包，什么都没有 —— 因为**打包版**的 `app_root()` 是 exe 所在目录，
抓包落在 `dist\大夏学堂转写助手\recon\network.jsonl`（**78 条，全是真的**）。
那 5 条登录链路 + 33 条平台请求，就是这一轮所有结论的来源。

### 1.65.2 真实契约（全部实测，已写进 docs/API.md 并落成代码）

| 环节 | 真实接口 | 关键细节（踩过的坑） |
| --- | --- | --- |
| 换令牌 | `GET /jy-application-resourcemanage/oauth2/token` | 要的是 **`result.jwt_token`**（910 字符），不是 `access_token`；头名是 **`jwt-token`**，不是 `Authorization: Bearer` |
| 学期 | `GET /v1/list/termYear` | `id` 即 acteId |
| **我的课表** | `GET /v1/myself/curriculum?acteId=&page.pageIndex=&page.pageSize=` | 分页必须写 **`page.pageIndex`**（写 `pageNum` 回「分页不能为空」）；行在 **`records`** 里（不是 `list`） |
| 班级节次（补充） | `POST /v1/statistics/teaching-class/user/course-list` | id 字段名是 **`teachingClassId`**（写 `teclId` 回「教学班id不为空」） |
| **播放地址** | `GET /v1/course_vod_urls_new?courseId=<courId>` | 返回**带 `auth_key` 签名的直连 MP4**（`dudaomedia.ecnu.edu.cn:40443`），同一节课可能有多个机位 |

发现手段也值得记一笔：**从 SPA 的 webpack chunk 里挖**。首个 bundle 只有 23 个接口路径，
懒加载 chunk 里有 227 条 —— 把 chunk 全量下下来做正则枚举（`getBaseUrl()+"/v1/..."`），
一次就中（此前手工猜了 3 个路径，全是 404）。

### 1.65.3 真实清单（M1 验收 ✅）

```
课程数 30 / 资源总数 894（3 个学期有课）
  2025-2026 第1学期：363 节有录播    线性代数/杨争峰 47 节、高等数学A（一）/羊丹平 90 节 …
  2025-2026 第2学期：512 节有录播    数据结构与算法/杜育根 67 节、计算机系统/肖波 58 节 …
  2026-2027 第1学期： 19 节有录播    计算机网络/章玥、操作系统/周炽金 …
```

科目、教师、节数全部来自 `myself/curriculum` 的真实响应，与网页端一致；
播放地址解析也通了（示例：`通用学术英语听说 2025-09-23` → 签名 mp4）。

> 中途踩的坑：先用了 `GET /v1/teachingclass/list?type=1&allType=1`，
> 它返回的是**行政班级**（「2022法学心理学统招生…」10 个年级班），
> 看上去像课程、实则不是本人在上的课 —— 已改口径并在代码注释里写明。

### 1.65.4 2 条真实视频端到端（进行中）

`scripts/accept_real.py` 已启动（后台跑）：

| 条 | 课程 | vodId | 时长 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 线性代数（杨争峰，2025-12-11 教书院319） | 701828 | 3301s | 下载中 **55.6%** |
| 2 | 高等数学A（一）（羊丹平，2026-01-07 教书院219） | 754086 | 3302s | 排队 |

真实拉流速度约 **0.5× 实时**（单条 838 MB / 约 80 分钟），之后走本地 ASR
（`http://127.0.0.1:8418`，faster-whisper small，零成本、音频不出本机）。
预计整轮耗时数小时 —— 这是 55 分钟长课 + CPU 推理的客观成本，不是程序卡住。

### 1.65.5 顺带修掉的缺陷 41 + 新增契约测试

* **缺陷 41**：`LoginSession` 只会**保存**登录态，从不**载入**。而 Chromium 的持久化用户目录
  **不保留会话级 Cookie**（`_webvpn_key` 一关浏览器就没了）——
  于是「明明刚登录成功、httpx 用得好好的」，再点一次「登录」却被弹回统一身份认证页。
  修复：新增 `restore_storage_state()`（注入 Cookie + localStorage），`open()` 时自动调用；
  顺带产出 `scripts/capture_pages.py`（复用登录态重走页面抓接口，不需要再输密码，支持 `--hash-nav`）。
* **新增 `tests/test_platform_api.py`（12 个用例）** + `tests/fake_platform_server.py`：
  把上面那套「实测出来的字段名/参数名/头名」用假服务器**钉死**，
  改坏任何一个都会红（这类错误在真机上只表现为「服务器内部错误」，完全看不出原因）。

### 1.65.6 本轮验证

| 套件 | 结果 |
| --- | --- |
| `pytest` | **354 passed**（337 → 354：+12 契约用例、+5 电平用例） |
| 真实清单拉取（应用自己的 `EcnuClient.fetch_catalog`） | **30 门课 / 894 节**，与网页端一致 |
| 真实播放地址 + 拉流 | ✅ `ffprobe duration=3301.217` / `size=838798382`；ffmpeg 抽音频成功 |
| 2 条真实视频端到端 | 🔄 进行中（见 §1.65.8） |

### 1.65.7 真机验收暴露的缺陷 42/43：**课堂录音电平极低 + 报错误导**

第一条验收录像（线性代数 2025-12-11）下载成功后，**14 个分段全部返回空文本**。
逐层排查（每步都有实测数据）：

| 步骤 | 实测 | 结论 |
| --- | --- | --- |
| 本地 ASR 服务是否可用 | `/health` 200；单段 400/500 才是它自己的错误码 | 服务正常（此前的 HTTP 502 是服务被回收后由中间层回的） |
| 切片是否有效 | 抽出 60s 真实片段 → ASR 0.3s 返回空 | 不是切片问题 |
| **源音频电平** | `ffprobe`：单条 aac 16 kHz 单声道；`volumedetect`：**mean -57.5 dB / max -34.2 dB** | **源音频本身就极轻**（正常语音 -20~-30 dB） |
| 是「整体轻」还是「没声音」 | 每 10 分钟窗口实测：mean 稳定在 -55~-58 dB | 全程都很轻，不是片段性静音 |
| 加增益能否救回 | 同一片段：`volume=+28dB` → **转出文字**；`loudnorm` / `dynaudnorm` → 仍 0 字 | **固定增益有效**，且不能用 loudnorm 系列 |
| 是个例还是普遍 | 抽查 5 条真实录播峰值：**-2.0 / -4.2 / -6.2 / -29.1 / -66.8 dB** | **普遍存在**，跨度极大 |

修复（两层，均已落地+测试）：

* **自动增益**：`media.probe_volume()` 量峰值 → 低于 **-30 dB** 才动手（-18 dB 的语音实测能识别，
  不做无谓重编码）→ `highpass=80 + volume=<gain>dB + alimiter`，上限 40 dB；
  新配置 `asr_auto_gain`（默认开）；缓存命中的音频同样过这道处理。
* **诚实报错**：`pipeline._empty_transcript_hint()` 空转写时先量电平 ——
  仍低于 -50 dB 就直接说「**原因不在你的配置**，是这条录像没录到声音，
  建议在平台上试听确认；换一条有声音的录播即可」；电平正常才列其它可能。

顺带修掉 **缺陷 43**：录播列表的真实字段名是 **`courseVodViewList`**，
代码里写的是早期猜测的 `courseVodVideoDtoList` —— 它能工作纯靠兜底逻辑，
平台一旦多返回一个带 url 的列表就会挑错。已改正，假服务器与契约测试同步改成真名。

### 1.65.8 端到端验收：改用「电平正常」的录像重跑

按上述结论，验收改为挑电平正常的录像（这也是真实用户该做的事）：

| 条 | 课程 | vodId | 时长 | 实测峰值 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | 高等数学（二）（唐晓艳，2026-03-02 教书院523） | 764453 | 3301s | -6.2 dB | 下载中（隔夜被环境回收） |
| 2 | 世界政治经济地理（杜德斌等，第2学期） | 800713 | 3302s | -2.0 dB | 排队（`--picks 395443`） |

选片逻辑已写进 `scripts/accept_real.py`：`--subjects` / `--picks` 可指定，
并在帮助文本里写明「不同录像电平差异极大，近乎无声的会让 ASR 返回空文本」这个实测事实。

### 1.65.9 隔夜发现：登录态会过期 + 长任务被环境回收

隔了一个周末再看（系统时间 09-12 23:xx → 09-14 09:xx），确认两件事：

1. **下载进程在 00:06 被回收**（`cache/media/*.part` 停在 14.0 MB 不再增长，无 python/ffmpeg 进程）。
   这不是程序问题：半截音频**不应该**当缓存用（比重新下更危险，所以设计上只有完整文件才进缓存）。
2. **登录态隔夜失效**：`/oauth2/token` 现在 302 回 `proxy.ecnu.edu.cn/vpn_key/update`。
   实测结论：**学校 webVPN 会话不跨天**（09-12 21:03 登录，09-14 已失效）。

由此又修了两处（都关于「别让用户撞上莫名其妙的失败」）：

* **缺陷 44：连通性报错太笼统**。09-12 深夜本机 Clash 换订阅后变成**全局代理**
  （出口 IP 从上海电信变成香港 `103.151.172.13`），学校网关直接**重置连接**
  （`WinError 10054`），而界面只显示「无法连接」。现在 `diagnose_access()` 把「连不上」
  细分成三类并各给下一步：连接被重置（多半是代理把校内域名也代理走了 →
  给出 `- DOMAIN-SUFFIX,ecnu.edu.cn,DIRECT` 的写法）、域名解析失败（fake-IP DNS / 未接校园网）、
  超时（可能需要 SSL-VPN）。
* **登录态过期要提前说**：`MainWindow._startup_hints()` 现在检查 `storage_state.json` 的保存时间，
  超过 12 小时就在日志里提示「很可能已过期（实测 webVPN 会话隔夜失效）；刷新清单若报
  『登录态已失效』，点「登录」重新认证即可，**已下载的音频与已完成的转写都不会重跑**」。
* `scripts/accept_real.py` 的登录态失效路径也从「崩在 `.json()`」改成明确提示
  「⛔ 登录态已失效…请重新登录一次」并给出两条命令。

### 1.65.10 一次「假阳性」的教训 + 过期路径的真机验证

写 `verify_gui_real.py` 时我差点交给用户一个**假阳性**：登录态早已过期、刷新 0.2 秒就失败，
可脚本却报「12 项通过」—— 因为它断言的是 `win.catalog`，而那是 GUI **启动时从
`data/catalog.json` 载入的缓存**（30 门课 / 894 条），不是本次刷新的结果。
**读缓存被当成了「M4 真机验收通过」**，这比不测更糟。

修正后的脚本：

* 先便宜地探一次登录态（换 jwt-token），据此走两条**互斥**的路径；
* 真实路径下断言 `catalog.fetched_at` 必须是**本次**（120 秒内）；
* 过期路径下只断言「刷新失败后有没有明确提示」，并且**只看刷新之后新增的日志/弹窗**
  （第一版命中的是「启动时的过期提醒」，看着通过其实没测到点子上）。

真机验证结果（登录态确实已过期）：

```
[1] 点「刷新清单」→ CatalogWorker 打真实平台（启动时已从本地缓存载入 894 条）
    刷新后的提示：检查站点可达性…
    ⛔ 站点可达性：vpn_required（HTTP 302）
       入口：https://proxy.ecnu.edu.cn:443/vpn_key/update?…
       —— 请点「登录」，在打开的浏览器窗口里完成学号 + 密码认证；本应用会保存登录态供后续请求复用。
    只有在浏览器里完成认证后仍被网关挡回（页面提示站点不存在 / 无权限）时，才需要接入校园网或 SSL-VPN
  ✅ 刷新失败后有明确提示   ✅ 提示里有「登录」指引
  ✅ 没有把本地缓存当成刷新结果   ✅ 进程未崩溃   ✅ 窗口正常关闭
结果：8 项通过，0 项失败（登录态过期路径）
```

顺带印证了本轮新增的**启动过期提醒**在真实 GUI 里确实出现：
「登录态保存于 38 小时前，很可能已过期（实测 webVPN 会话隔夜失效）。……」

> 教训记在这里：**验证脚本本身也会骗人**。凡是「断言界面上的数据」，
> 都要先问一句「这份数据是这次跑出来的，还是上一轮留下的缓存？」

### 1.65.11 长音频全流程排练 + ASR 吞吐实测（为真实验收做时间预算）

真实验收要跑 2 条 55 分钟录播，所以在等登录的空档先把**同一套代码路径**用真实语音排练一遍，
顺便把「要跑多久」量清楚（否则很容易被环境回收在半路）。

**ASR 吞吐实测**（本机 CPU / int8，素材是 52.5s 真实中文讲解）：

| 模型 | beam | 耗时 | 实时倍率 | 识别质量（首句） |
| --- | --- | --- | --- | --- |
| tiny | 1 | 2.9s | **18.3×** | 同學們好…訓論與算法**副詐读** |
| tiny | 5 | 2.6s | 20.3× | 同学们好…训论与算法**复杂读** |
| base | 5 | 4.0s | 13.0× | 同學們好…複雜度（繁体输出） |
| **small** | **5** | **10.7s** | **4.9×** | 同学们好…**序论与算法复杂度**（最准） |

→ 结论：`small` 完全够快（55 分钟音频约 11 分钟），**不必为了速度降级到 tiny**。
（早先担心的 0.5× 实时是那次测量包含了模型加载，属误判。）

**12 分钟长音频排练**（`scripts/rehearse_long.py`，10/10 通过）：

```
短视频 → 拼接成 12 分钟真实中文语音（683s）
[downloading]  本地音频文件：直接抽取音频（无需下载）→ 5.21 MB
[transcribing] 静音检测到 183 个静音区间 → 切分 3 段 → 逐段 ASR
[writing]      txt / srt / md 三件
[     done]    100%   第一次 2.1 分钟；第二次 0.0 秒（复用 transcript.json）
  ✅ 三产物齐全且都带 UTF-8 BOM
  ✅ SRT 57 条，末条 00:11:10 --> 00:11:22（不越界：音频 11:23）
  ✅ 正文 1848 字真实中文
  ✅ 重跑 0.0s 命中缓存
```

第二次运行时日志里出现 **「本次共复用 3/3 段已缓存结果（未重复调用 ASR）」** ——
缺陷 46 的分段缓存在**真实语音、跨进程**的条件下确实生效。

**给真实验收的时间预算**：2 条 ×（下载 30~60 分钟 + 识别 ~11 分钟）≈ **1.5~2.5 小时**
（瓶颈是下载，不是识别）。中途若被回收，音频缓存与分段结果都在，重跑不会重下也不会重复识别。

### 1.65.12 缺陷 46：分段 ASR 结果不落盘 → 长课程中断要「重新花钱识别」

一直说「全程可断点续跑」，但这轮去核对**ASR 这一段**时发现它其实不成立：

```python
segs = self._transcribe_one(seg_file, offset=chunk.start, attempt=1)
results.append((0.0, segs))     # ← 结果只在内存里
seg_file.unlink(missing_ok=True)  # ← 切片还立刻删了
```

一段 55 分钟的课会被切成十几段，**云端 ASR 是按段计费**的。中途一断
（崩溃/关机/被环境回收/用户点停止），下次运行要把付过费的分段**全部重来**。

修复：每段成功后**先落盘再删切片**（顺序反了就是「这一段白跑且钱已花」），
落盘位置 `<缓存>/segments/<音频哈希>-<方案哈希>/result_NNN.json`；
复用前校验四项：`index`、`start`/`end` 与本次方案一致、`model`+`provider` 一致、JSON 可解析。
`Transcript.meta["reused_segments"]` 与日志都会如实记下「本次复用 N/M 段（未重复调用 ASR）」。

**写这个功能时被自己的测试抓到两个问题**（都记进 CHANGELOG）：

1. `data.get("start") or -1` —— 第一段的 `start` 就是 **0.0**，`0.0 or -1` 得到 -1，
   于是**第一段永远命不中缓存**，每跑一次多花一次钱；症状是日志里「只有第 2..N 段被复用」。
   这是本项目**第二次**栽在「0 被当成空值」上（第一次是 `pick()`）。
2. 分段缓存目录是**全机共享**的（续跑正靠它生效），所以测试必须把 `cache_dir()` 指到临时目录，
   否则测试之间会互相复用结果 —— 第一版就是因此红掉的。

新增测试：`test_interrupted_transcription_resumes_without_repeating_asr`
（第 2 段后触发取消 → 重跑只应为剩余段付费，并断言 `reused_segments == 2`、内容不丢）、
`test_segment_result_cache_is_invalidated_when_model_changes`（换模型必须全部重跑）。

### 1.65.13 用真实素材验证「绝不编造」这条底线（缺陷 45）

第 42 条加了自动增益后，我盯上一个**更危险的下游风险**：对**真正没有声音**的音频做增益，
会让 Whisper 在「无语音」输入上编造文字（实测 `vad=False` 时吐出过 `字幕by索兰娅`）——
用户会拿到一份看起来像转写、其实是幻觉的产物。这比诚实失败糟糕得多。

于是拿**真实的那条近乎无声录像**（线性代数 vodId 701828，25.18 MB，已完整下载）做了反证，
过程中又发现我自己的**判据错了**：

| 度量 | 这条录像 | 有语音的录像（高数A一 / 世界政经） | 结论 |
| --- | --- | --- | --- |
| 峰值 max_db | **-23.8 dB**（某处有个响声） | -4.2 / -2.0 dB | ❌ 会被单个响声带偏 |
| **平均 mean_db** | **-54.5 dB** | -18.7 / -13.4 dB | ✅ 两簇分得很开 |

修复：判据改为**平均电平**（`SILENT_MEAN_DB = -45 dB`）；判定 silent 时**不放大**、
**不送 ASR**，直接以可照做的文案失败；新增 `asr_allow_silent`（默认关）供强行识别。

反证脚本 `scripts/verify_silent_honesty.py`（**8/8 通过**，真实素材）断言：
判为 silent、文件未被改动、ASR 未被调用、失败文案可照做、**没有产出任何 txt/srt/md**。

```
⛔ 这条录像的音轨几乎没有声音（平均电平 -54.5 dB，正常课堂语音约 -13 ~ -19 dB；峰值 -23.8 dB），
   已跳过语音识别 —— 对近乎无声的音频强行识别只会得到**编造的文字**。
   这通常是学校录制设备/麦克风的问题，**不是你的配置问题**：建议在平台上直接试听确认，
   换一条有声音的录播即可正常转写。（若你确认要强行识别，可在设置里打开 asr_allow_silent。）
```

### 1.65.14 为真机验收准备的 GUI 驱动脚本

新增 `scripts/verify_gui_real.py`：用**真实登录态**驱动**真实 GUI**（离屏）跑
「点刷新清单 → 真实 `CatalogWorker` 打真实平台 → 左栏渲染课程树 → 关窗收尾」，
断言「我账号下的真实课程与录播确实出现在界面里」（DoD 的 M4 那一条）。
与 `verify_gui.py`（模拟平台跑完整流水线）互补：一个验真实数据，一个验全流程。

### 1.65.16 真机验收**跑通了**：M4 13/13 + 2 条真实录播端到端（进行中）

登录态打通之后，一键验收脚本 `scripts/run_acceptance.py` 自动跑完第一步：

```
[1] 登录态探测：✅ 可用 —— jwt-token 910 字符
▶ 真实 GUI（离屏）拉取真实清单            → 13 项通过，0 项失败
    ✅ 清单是**本次**刷新的（不是本地缓存）  — fetched_at=2026-09-14 11:32:13
    ✅ 课程数 ≥ 5                        — 30 门
    ✅ 录播条数 ≥ 20                     — 894 条
    ✅ 课程树顶层节点 = 课程数            — 30 vs 30（子节点 894）
    ✅ 统计栏里的课程数与清单一致
▶ 2 条真实录播端到端（进行中）             → 高等数学（二）下载中 ~8× 实时
```

这条 `fetched_at=2026-09-14 11:32:13` 是关键：它证明界面上的清单是**这次真拉下来的**，
而不是 `data/catalog.json` 里上一次留下的缓存（这个假阳性我上一轮刚踩过）。

### 1.65.17 缺陷 48：登录窗口在屏幕外 —— 一次「双方都莫名其妙」的故障

整个过程值得完整记下来，因为它的表现极具迷惑性：**用户说「我已经登录了啊」，
程序说「登录态已过期」**，而且两句话都是真的。

| 时间 | 事件 |
| --- | --- |
| 09:47 / 10:27 / 11:16 | 三次打开登录窗口，抓包都停在「加载 SSO 登录页」那一刻，之后零请求 |
| — | 用户反复说「已登录」，并去**自己的 Edge** 里登录 |
| 11:24 | 枚举窗口才发现：Chromium 主窗口矩形 = `(-25600,-25600) 159x27`（最小化的哨兵坐标），用户根本看不到 |
| 11:26 | 用 user32 `MoveWindow` 移回 `(60,60) 1400x880` → 可见；但每 2 秒又被最小化一次 |
| 11:27 | 我加了「置顶+最大化」，用户反馈：**「不要置顶，挡到我看任务进度了」** —— 立刻撤销 |
| 11:29 | 换思路：`scripts/login_via_cdp.py` 起一个**普通 Chrome 窗口**（独立 profile + 调试端口） |
| 11:31 | 用户在该窗口登录成功 → CDP 导出 26 个 cookie → `session_ok()` = **True**（jwt-token 910 字符） |

三条教训（都写进代码注释了）：

1. **JS 的窗口坐标会骗人**：`window.screenX` 报 60，user32 报 -25600。可见性判断必须走 OS 层。
2. **「让用户看得见」不等于「抢焦点」**：置顶+最大化虽然达到了「看得见」，但挡住了用户别的工作。
   正解是**放在屏幕内的普通窗口**。
3. **用户说「我登录了」时，先问「你在哪个窗口登录的」**：不同浏览器 profile 的登录态
   互不相通；而 Edge 的 cookie 库是**独占锁定**的（连 `FileShare.ReadWrite` 都打不开，
   卷影副本在客户端版 Windows 又不可用）。跨 profile 的登录态**不是「读一下就能拿」**的东西。

最终采用的通路（也是以后这类环境的推荐做法）：用系统 Chrome 起普通窗口给用户登录 +
CDP 读回会话，**完全不碰用户的窗口**。

### 1.65.18 缺陷 49/50/51：一条「看着在下、其实已经断了」的音频（第 36–37 轮）

这轮从一次**我自己的误判**开始，最后挖出两个真缺陷。

#### (1) 先纠正误判：那不是卡死

第 35 轮我盯着 `cache\media\*.part` 报「0 字节、20 分钟没动、ffmpeg 卡死」。
**这个结论是错的**，证据（改完代码回头看日志）自己把它推翻了：

| 观测 | 当时的解读 | 实际事实 |
| --- | --- | --- |
| `.part` 文件 0 字节、mtime 冻结 | ffmpeg 一个字节都没写 | 那是**上一次尝试的残留**；本次尝试的输出在别处，且一直在写 |
| `-progress` 从 40.4% 爬到 43.7% | 进度是假的 | **进度是真的**：43.7% ≈ 1443s，与最终落盘的 1900.5s 同量级 |
| `IO Read Bytes/sec = 0`、CPU 只涨 0.6s/min | 网络与 CPU 都停了 | 计数器取的是**同名进程的另一个实例**，不是这一次的 ffmpeg |
| — | — | 日志：`音频完成 … 用时=955.5s`，抓回 1900.5s ⇒ **2.0× 实时，健康** |

对照基准：`线性代数 vod701828` 实测 3301s 媒体用了 **3676s**（≈0.9× 实时）。
所以「2× 实时」在这条链路上是正常速度，**不是**停滞。
**教训：判断「有没有在下」只能看日志里的实际产出（`用时=` 与时长/字节），不能看推测性计数器和可能属于别的实例的临时文件。**

> **同一个坑我踩了第二次，所以把量法写死在这里**：`Get-ChildItem` / `dir` 显示的大小是
> **目录项缓存**，对「别的进程正在追加」的文件会长期显示 0 或旧值。
> 第 37 轮又据此误判了一次（以为续传在空转），直到用
> `open(path, "rb")` 直接读长度才发现文件其实在稳步增长（3.41 MB → 4.19 MB）
> —— 判断文件有没有在长大，**要么打开它读长度，要么看日志里的产出**。
> 现成工具：`build\probe_resume_truth.py`（把「进度 / 文件真实长度」对齐打印）。

#### (2) 缺陷 50（真实触发）：被强杀留下的半份音频，被当成「完成」

我按错误判断强杀了 ffmpeg，于是产生了真正的事故样本：

```
11:48 downloader: 时长偏差较大：音频时长 1900.5s 与清单标注 3301.0s 偏差 42.4%（>2%）
11:49 downloader: 音频完成：高等数学（二） … 时长=1900.5s 大小=14.50 MB
12:00 pipeline : [675] audio_ready  88.0%  复用已有音频（不重下）
```

三处都「知道」不对，但都只是记一笔就放行：

1. `run_ffmpeg(..., check=False)`：**从不看退出码**（被强杀的 ffmpeg 退出码是 1）；
2. 时长偏差 42.4% 只写 `log.warning`，不改变结果；
3. 任务已记 `audio_ready`，下次续跑**看到文件存在且 > 1024 字节就复用**（缺陷 51）。

结果：一份只覆盖前 57% 的音频被送去转写，产物**残缺却毫无标记** —— 比直接失败糟得多。

修复（`downloader.py` + `pipeline.py`）：

* `check=True`：ffmpeg 非 0 退出**直接判失败**（但先验产物：完整就放行并留告警，避免误杀好文件）；
* **截断裁决**：< 清单时长 95% 即抛 `StreamTruncatedError`，产物转存 `<目标>.partial`，由上层重试；
* **复用前验明正身**（缺陷 51）：续跑要用缓存的音频时，先用 `ffprobe` 量实际时长，与清单差 > 5% 就**重新拉流**，绝不拿半份音频转写；
* 阶段靠后时不再硬推 `probing`（`audio_ready → probing` 是非法迁移，会被 store 拒绝并**连带丢掉 play_url/duration 字段**）。

顺带补齐**断点续传**：有了 `.partial` 基准后，`-ss <已有秒数>` 只补抓剩余部分，再用 concat
解复用器 `-c copy` 拼回完整音频并校验时长。这样「抓了一半」不再是纯损失。
（过程里被自己的测试抓到两个真 bug：拼接输出叫 `x.mp3.part`、扩展名推断不出封装格式，
必须显式 `-f`；以及续传成功后忘了删 `.partial`，下次会拿旧断点当基准。）

#### (3) 缺陷 49（潜在，合成复现）：拉流停滞没有任何看门狗

网络流挂住时 ffmpeg **不会自己退出**，而主线程正阻塞在 `proc.stdout` 的读循环上，
所以任何「写在回调里」的检查都不会被触发 —— 任务会永远挂着，界面上只剩一个不动的百分比。

本轮补上独立线程看门狗（`run_ffmpeg(..., watchdog=...)` + `AudioDownloader._watchdog_factory`）：

```
判据：窗口内推进的媒体秒数 ÷ 窗口秒数 < download_min_speed_ratio(0.25)
      ⇒ 中止本次拉流并重试（完全卡死与龟速推进都会被这一条抓住）
暂停中 / 已抓完正在封头 ⇒ 不计入停滞（否则会把正常暂停和收尾误杀）
```

诚实说明：**这条没有真实世界样本**（我原先以为遇到了，见 (1)，其实没有）。
它由合成用例证明有效：`test_watchdog_actually_kills_hung_ffmpeg` 让 ffmpeg 以 `-re` 静默跑 120s，
看门狗 1.5s 触发 → 进程被杀、抛 `StreamStalledError`，断言总耗时 < 30s。

#### (4) 新增验证

| 用例 | 断言 |
| --- | --- |
| `test_truncated_download_is_rejected_and_kept_for_resume` | 4s/300s 的产物 → `StreamTruncatedError`，`.partial` 保留，正式缓存路径上不留残件 |
| `test_resume_fetches_only_the_remainder` | 5s 断点 + 补抓 7s → 12.1s（清单 12s），成功后断点被清理 |
| `test_watchdog_policy_*`（2 个） | 停滞给原因、健康/收尾放行并滚动观察窗 |
| `test_watchdog_actually_kills_hung_ffmpeg` | 看门狗真的能杀掉「安静等待」的 ffmpeg |
| `test_short_cached_audio_is_redownloaded_not_transcribed` | 5s/60s 缓存 → 触发重下，送去转写的是补全后的文件 |
| `test_complete_cached_audio_is_reused_without_download` | 完整缓存仍然复用（没把修补成「每次都重下」） |
| `test_resume_from_audio_ready_still_refreshes_url_and_duration` | `audio_ready` 续跑时 play_url/duration 不被非法迁移吞掉 |

> 写这几条用例时又抓到**我自己修复里的一个 bug**：`_run_inner` 里原有一个函数级
> `from . import media`，它让 `media` 在整个函数里成为局部名，于是我在上面新加的
> `media.duration_of(...)` 必然 `UnboundLocalError`。真机验收第一次重跑就是这么挂的
> （`[675] failed … cannot access local variable 'media'`）。已删掉那个多余导入并留注释。

### 1.65.19 真机验收：第 1 条视频从头走通（含断点续传）

第 37 轮用修好的代码重跑验收，`courId=381401`（高等数学（二），vodId 764453，3301s）走完整条链路：

```
[675] downloading   5.5%  发现未完成的音频（1901s/3301s），从断点续传…
[675] downloading  73.5%  续传中 3299/3301s（断点 1901s）
[675] downloading  75.2%  断点续传完成：…mp3（3301s, 25.19 MB）
[675] audio_ready  88.0%  音频就绪
[675] transcribing 90.0%  调用语音识别（ASR）… → 12 段（静音切分，重叠 1.5s）
[675] writing      98.0%  写出 txt / srt / md…
[675] done        100.0%  完成（3 个文件）
结果：stage=done  耗时 25.1 分钟
```

日志里的关键一行（`断点续传完成`）：

```
时长=3301.5s 大小=25.19 MB sha256=089eb888e437c97d 用时=713.6s（断点 1900.5s）
```

即：**保留上次已抓到的 1900.5s，只补抓 1400.5s，用时 11.9 分钟 ⇒ 1.96× 实时**
（对照：整段重下按当天实测 1.0× 需 23 分钟）。**字节级 seek 生效**——先用
`Range: bytes=0-1023` 探到服务端回 **206**（见 `build\probe_range_support.py`）。

产物独立复核（不看程序自述，直接读文件字节）：

| 产物 | 大小 | 检查 |
| --- | --- | --- |
| `.txt` | 36,857 B | UTF-8 **带 BOM**、LF、16 段 / 12,665 字 |
| `.srt` | 83,366 B | 带 BOM、**1288 条字幕**，时间轴 `00:00:08 → 00:55:01`（覆盖全长 55 分钟） |
| `.md` | 63,963 B | 带 BOM、元信息（讲师唐晓艳 / 时长 55:01 / 资源 ID / 模型 / 字数）+ 全文 |
| `.transcript.json` | 256,463 B | **无 BOM**（机器可读产物按设计不带）、LF |

音轨时长用 ffprobe 独立复核：**3301.46s vs 清单 3301.0s（偏差 0.014%）**、
mp3 / 16 kHz —— 拼接产物完整、编码正确。

**如实记录一处质量短板**：开头 3–4 段夹有繁体字与噪声串（本地 `faster-whisper-small`
在杂音段的典型退化），正文本身确为数学课堂内容（平面方程、坐标变换、曲线）。这不是流程缺陷
（该音频有人声，按设计正常识别；只有在**近乎无声**时才会拒绝转写），但说明本地小模型
不宜作为最终质量方案 —— 已记入「限制」与「下一步」。

### 1.65.20 缺陷 52/53：并发写日志导致轮转失败；force 重跑白扔断点

这两个都是**并发/重跑**路径上的真实缺陷，本轮一并修掉：

| # | 现象（实测） | 根因 | 修复 |
| --- | --- | --- | --- |
| **52** | 验收脚本与 GUI 同时运行时，log 里反复出现 `--- Logging error --- PermissionError: [WinError 32]`，**触发轮转的那条记录被丢掉** | `RotatingFileHandler.doRollover()` 去 `os.rename` 另一个进程正持有的 `logs/app.log`（句柄没有 `FILE_SHARE_DELETE`） | `_MultiProcessSafeRotatingHandler`：轮转失败就**本轮不轮转、继续追加**，提示仅一次；2 个用例钉住「一条不丢 + 不冒 logging 堆栈 + 只提示一次」 |
| **53** | `fetch(force=True)`（GUI「强制重跑」）会跳过缓存判定，把上次中断留下的半份音频直接覆盖重下 —— 那半份是 1900s/3301s，等于白扔 16 分钟 | 「残件降级为断点」只写在缓存命中分支里，force 走不到 | 把降级逻辑提到 `fetch()` 开头，**与 force 无关**；新增 `test_force_download_resumes_from_incomplete_cache` |

### 1.65.22 云端 ASR 打通：用户真实 Key 到手后的逐条实测（第 38 轮）

用户填好百炼 Key 后，我按"**先证明哪条路能用**"的顺序实测，结论是原实现**两条路都不通**，
而且默认模型名在该账号上不存在。

| 路径 | 实测结果（2026-09-14） |
| --- | --- |
| `POST /audio/transcriptions`（应用原路） | **对所有 ASR 模型 404** —— 用户填了 Key 也会失败 |
| 原生异步（`uploads` + `file_urls`） | 凭证接口本身正常（`/api/v1/uploads?action=getPolicy` → 200），但 `paraformer-v2` 提交后 `SERVER_ERROR`、`sensevoice-v1` 报 `InvalidFile.DownloadFailed`，千问/Fun 系列直接 `url error` |
| `POST /chat/completions` + `input_audio` | ✅ **可用**（data 必须是 **data URL**；content 里只能有音频项；返回纯文本、无时间戳） |
| 默认模型 `paraformer-v2` | 该账号 250 个模型里**查无此名** |

修复与新增（详见 CHANGELOG 0.11.1 的 55–58）：`chat_audio` 路由 + 默认模型改 `qwen3-asr-flash`
+ 上传凭证字段名两处纠正 + 时间轴按静音边界（不编造）+ `scripts/verify_cloud_asr.py`
+ 14 个回归用例；顺带修掉 `--output-dir` 静默失效与"stderr 污染退出码"。

**质量对照（同一条 55 分钟录播）**：

| | 本机 `faster-whisper-small` | 千问 `qwen3-asr-flash` |
| --- | --- | --- |
| 字数 | 12,496 | **16,360** |
| 繁体专用字 | **677** | **15** |
| 标点（，。、；：？！） | **3** | **1,880** |
| 端到端耗时 | 约 11 分钟 | **4.8 分钟** |
| 字幕 | 1,288 条（模型自带句级时间戳） | 419 条（静音边界，均 8s，覆盖完整） |

产物已分别留在 `output/`（本机）与 `output-qwen/`（千问），可逐段对照。

**一处需要用户配合**：当时用户机器上正跑着 13:27 构建的旧 exe（其预设仍是
`paraformer-v2`），该任务下载完成后会在 ASR 阶段失败；新版已构建在 `dist-next/`
（含全部修复，已用 PyInstaller 官方读取器解出内嵌 PYZ 逐条核对字符串确认修复进包），
等用户关闭应用后合并进 `dist/`。

### 1.65.23 界面可读性：深色模式下的"灰灰的看不清"（缺陷 60）

用户实测反馈：「字体对比度太低，灰灰的看不清，修改了再打包」。根因**不是**配色选了浅灰：

| 证据 | 说明 |
| --- | --- |
| `HKCU\...\Themes\Personalize\AppsUseLightTheme = 0` | 这台机器 Windows 处于**深色模式** |
| 应用**没有任何** `setStyle` / `setPalette` / QSS | 外观完全继承系统 ⇒ 背景是**深色** |
| 界面里 16 处硬编码 `#333` / `#444` / `#555` / `#000000` | 深灰/纯黑文字压在深色背景上 ⇒ 对比度极低 |
| `QFont(family, 9)` | 9pt 偏小，加重"看不清" |

修复：新增 `src/app/ui/theme.py`（Fusion + 显式 QPalette + QSS 的**浅色高对比主题**，
不跟随系统深色模式），16 处硬编码色改为主题常量，基础字号 9→10pt，
启动时把主题信息写进日志便于自查。

**验证（两项证据，缺一不可）**：

```
[1] WCAG 2.1 对比度审计：16 对配色全部通过
    正文 / 白底            17.63:1（要求 ≥7）
    正文 / 交替行底色        16.13:1
    次要说明文字 / 白底       9.50:1
    状态色（成功/警告/错误）   6.65 / 7.41 / 7.78:1
    最低一对（禁用态文字）     5.59:1（要求 ≥4.5）
[2] 离屏真实渲染截图：build/ui_shots/main_window.png、settings_dialog.png
    主窗口与设置对话框均为「浅底深字 + 蓝色强调」，提示条/信息条清晰可辨
[3] 冻结态日志（打包产物自己的记录）：
    界面主题：浅色高对比（正文 #16191d / 底 #ffffff，对比度 17.6:1，10 pt）
```

> 截图里中文显示为方框，是**离屏渲染环境没装中文字体**的假象（脚本开头就有
> `QFontDatabase: Cannot find font directory` 提示）；用户桌面上是微软雅黑，不受影响。

### 1.65.25 分片上限与下载提速：两条都要靠实测才敢下结论（第 39 轮）

用户连着给了两条反馈，都查到了**实测根因**（详见 CHANGELOG 0.11.2 的 62/63）：

**① 「日志里失败了」** —— GUI 里那条《数据结构与算法》6 段全挂：
`400 The audio is too long`。逐档实测该模型（`qwen3-asr-flash`）的上限：

| 输入 | 结果 |
| --- | --- |
| 300s @64k（2.29 MB） | ✅ |
| **301s** @64k（2.30 MB） | ⛔ 只多 1 秒就拒 |
| 300s @192k（5.72 MB） | ✅（体积是失败档的 2.5 倍） |

⇒ **时长硬上限 300 秒**，与体积无关；而配置默认 600s ⇒ 必然全挂。
修复：这类模型单段自动夹到 270s（留余量）+ 被拒时**自动对半重试** + 报错文案改为
"调小单段秒数"（原来那句"缺 CUDA / 模型名不存在"会把人带偏）。

**② 「1 秒只能下 2 秒」** —— 2× 实时本就在实测正常区间（0.9–2.0×），但继续往下查发现
**服务端是单连接限速**：单连接 672 KB/s、4 并发聚合 2.1 MB/s（持续 160 MB 校验过字节数）。
于是实现**按时间窗并行取音频**（每路 `-ss/-t` 只拉自己那 1/N 字节，concat 拼回并校验时长）：

```
4 路：600s 音频 71.1s 抓完 ⇒ 8.4× 实时（同期单流 3.2×）⇒ 提速 2.6×
```

任何一步不成都**自动退回单流**（不支持 Range / 时长未知 / 本地文件 / 某路失败 / 校验不过）。

**顺带如实记录用户侧的两条"今天"任务失败**：《概率论与数理统计 2026-09-14 09:50》与
《大学物理C 2026-09-14 15:40》都报"没有可用的录播地址" —— 当天录播在平台侧多半还在
转码/未发布，**不是工具问题**，隔段时间重试或挑早几天的节次即可。

### 1.65.24 缺陷 59：被中断留下的 `.part` 会被整段重下

用户 14:48 打开旧版 exe、下载《数据结构与算法 2026-06-18 13:00》到 15:23 关窗，
磁盘上留下 25.19 MB 的 `.part`。ffprobe 量它：**3301.3s vs 清单 3301.0s** ——
**下载其实已经完成**，只是进程在收尾改名那一刻被关掉了。

旧逻辑只认正式缓存路径，所以这份成果会被整段重下。现在分两种情形处理：

* **部分完成** ⇒ 转成断点基准，只补抓剩余部分（`-ss` 字节级 seek）；
* **其实已下完** ⇒ **直接扶正**为正式音频，一次下载都不再发生。

两条路径各有回归用例（`test_leftover_part_file_is_resumed_not_redownloaded` /
`test_complete_leftover_part_is_promoted_without_redownload`）。

### 1.65.21 单命令 2/2 全绿（第 37 轮最终验收记录）


```
[1] 登录态探测：✅ 可用 —— jwt-token 910 字符
▶ 真实 GUI（离屏）拉取真实清单   → 14 项通过，0 项失败
   MainWindow 1560x940 / 刷新耗时 30.8s
   30 门课 / 894 条录播；fetched_at=2026-09-14 13:22:33（**本次刷新**，非本地缓存）
   课程树 30 vs 30；统计栏课程数与清单一致；关窗线程收尾干净
▶ 2 条真实录播端到端              → 完成：2/2 条成功
   《高等数学（二）》   2026-03-02 vodId=764453 时长=3301s → txt/srt/md
   《世界政治经济地理》 2026-03-16 vodId=800713 时长=3302s → txt/srt/md
全部通过 🎉
```

命令（一条命令跑完，不需要人盯着）：

```
.venv\Scripts\python scripts\run_acceptance.py --login-wait 900 --picks "381401,395443" --max-segment 300
```

**这一轮同时修掉了让验收不可复现的缺陷 54**：`--picks` 指定的 courId 若不在
`recentWatchRecord` 里，课表兜底会因 **ID 空间不同**查不到，那条被**静默丢弃** ——
同一命令有时跑 2 条、有时只跑 1 条。现在查不到就直接用 courId 取播放地址，
并从响应里的 `courName` 补课程名。

产物一览（`output/`，两条都覆盖全长）：

| 课程 | .txt | .srt | .md | 字幕时间轴 |
| --- | --- | --- | --- | --- |
| 高等数学（二） 2026-03-02 | 36,857 B / 12,665 字 | 1,288 条 | 63,963 B | 00:00:08 → 00:55:01 |
| 世界政治经济地理 2026-03-16 | 27,830 B / 9,656 字 | 630 条 | 40,657 B | 00:00:00 → 00:55:02 |

两份 `.txt/.srt/.md` 都是 **UTF-8 带 BOM + LF**（中文 Windows 不乱码），
`.transcript.json` 有意不带 BOM（机器可读）。

### 1.65.15 一键真机验收编排（`scripts/run_acceptance.py`）

真实验收必须由你本人完成一次统一身份认证，而**这件事什么时候发生不由程序决定**。
所以把「等」也交给程序：

```
[1] 探测登录态（GET /oauth2/token）
      ├─ 可用 → 直接进入第 2 步
      └─ 不可用 → 打开可见浏览器等你登录（默认最多 4 小时）
[2] scripts/verify_gui_real.py        → 真实 GUI 拉真实清单（M4）
[3] scripts/accept_real.py --picks …  → 2 条真实录播端到端（音频 → ASR → txt/srt/md）
[4] 汇总逐步结果
```

好处：不需要有人盯着屏幕等 —— 你什么时候登录，验收就什么时候自动往下跑；
中途 Ctrl+C 也不会丢进度（音频缓存与 `transcript.json` 都在，断点续跑）。

文档同步（本轮）：

* `README.md`：新增 §8.2.1「连接被重置（代理把校内域名也代理走）」、
  改写 §8.5 的「ASR 返回了空结果」（区分「是录像没声音」与「确实没人说话」）、
  ⑫ 一键真机验收命令。
* `docs/ARCHITECTURE.md`：清单流与任务流改成**实测契约**
  （jwt-token、`page.pageIndex`、播放地址按需解析、复制电平策略、空转写的判定分支）。

---

## 1.6 第 22 轮交付：真机登录抓包 → 推翻「校外被封」的旧结论 + 4 个缺陷

### 1.6.1 这一轮做了什么

用户接上校园网后，用**真实浏览器**跑了一次登录（`scripts/login.py --capture`）。
虽然登录本身没完成（人没提交凭据），但**抓包本身产出了本轮最有价值的东西**：
它证明了此前 21 轮里一直写在文档里的一个结论是**错的**。

### 1.6.2 更正：校外那条 `302 → proxy.ecnu.edu.cn/vpn_key/update` 不是「被封」

实测抓到的完整跳转链（真实时间戳，脱敏后）：

```
19:12:28  ① GET https://courses.ecnu.edu.cn/jy-application-resourcemanage-ui/   302
19:12:28  ② GET https://proxy.ecnu.edu.cn/vpn_key/update?...reason=site+...+not+found  302
19:12:28  ③ GET https://proxy.ecnu.edu.cn/users/sign_in                          302
19:12:28  ④ GET https://api.ecnu.edu.cn/oauth2/authorize?...                     301
19:12:28  ⑤ GET https://sso.ecnu.edu.cn/oauth2.0/authorize?...                   302
19:12:28  ⑥ GET https://sso.ecnu.edu.cn/login?service=...                        200  ← 登录页
19:12:28  ⑦     登录页的 JS / CSS / 配置接口（i18n、dictconfig、login_info…）      全部 200
          ……此后 600 秒：**零新请求**
```

**第 ⑥ 跳返回 200、页面资源全部加载成功** —— 链路完全正常，页面上就在等人输密码。
所以：

| 旧结论（第 1–21 轮） | 实测更正（第 22 轮） |
| --- | --- |
| 校外直连被 webVPN 网关**拦下** | 那条 302 是**正常第一跳**（网关会先送你去认证，再登记站点） |
| 必须先接校园网或连 SSL-VPN | **不必**：点「登录」在浏览器里完成一次统一身份认证即可 |
| `webvpn.ecnu.edu.cn` 无公共 DNS ⇒ 校外无法访问 | 两件事被混为一谈；能否访问与那个域名无关 |
| 「校外不可达」是阻塞真实验收的原因 | 真正的阻塞点只是「**没有人**在浏览器里完成认证」 |

> 这轮的价值不在写代码，而在于：**我之前的诊断信息把用户指错了方向**。
> 一个工具最难发现的缺陷，是它「自信地给出错误的下一步」。

### 1.6.3 缺陷 37/38：同一个错误的两次发作（对 URL 做子串匹配）

| # | 缺陷 | 后果 | 修复 |
| --- | --- | --- | --- |
| **37** | `_diagnose_stuck()` 先做 `"proxy.ecnu.edu.cn" in url`，而 `sso.ecnu.edu.cn/login?service=...` 的 query 里 **URL-encode 了 `redirect_uri=https%3A%2F%2Fproxy.ecnu.edu.cn%2Fecnu_oauth2`** | 用户明明只需输密码，工具却报「被 webVPN 拦下」并让他去折腾网络 —— 实测两次登录窗口（10 分钟 + 30 分钟）都给出这句**误导性**结论 | 改为 `urlsplit` 后只看 **主机 + 路径**；真正的拦截判据是「停留页主机就是网关 **且** 正文写着站点不存在/无权限」；新增 `_body_snippet()` 安全读正文 |
| **38** | `_detect_logged_in()` 见到 `vpn_key`/`login` 等子串就 `return False`，而校外经网关进平台时 URL 上**完全可能带 `vpn_key`** | 比 37 更糟：**登录成功会被永久判成失败**，用户会反复重试到放弃 | 同一套主机/路径判定 + 页面特征/localStorage token |

### 1.6.4 缺陷 39：真实抓包文件被模拟流量污染

`fetch_catalog()` 结尾无条件 `dump_network_log()`，而测试里有 8 个用例调用它 ——
每跑一次测试就往 `recon/network.jsonl`（**真实抓包**产物）灌 20+ 条 `127.0.0.1` 记录。
实测证据：该文件累积 **644 条**，真实请求只有 **5 条**；而且它会与**正在进行的真实登录并发写同一文件**。

修复两层：

1. `tests/conftest.py` 新增 session 级 autouse 夹具 `_isolate_app_home`：
   `ECNU_TRANSCRIBE_HOME` + `LOCALAPPDATA` 都指向临时目录 ⇒
   `recon/`、`data/`、`cache/`、`output/`、`logs/`、`%LOCALAPPDATA%\ecnu-transcribe\` 全部隔离。
2. `dump_network_log()` 加护栏：默认路径 + 记录**全为环回** → 不写（显式传 `path=` 照写；
   混有真实请求则整体照写，绝不丢真实数据）。

### 1.6.5 缺陷 40：WAL 瞬时 I/O 错误 → 「一打开就崩」

全量测试里偶发（2 次里 1 次）：

```
self._conn.execute("PRAGMA journal_mode=WAL")   →   sqlite3.OperationalError: disk I/O error
```

触发场景：上一个持有该库的进程**刚被强杀**，Windows 尚未释放 `-wal`/`-shm` 映射。
而 `StateStore()` 在 **GUI 启动路径**上 —— 真抛出去就是用户无法自救的启动崩溃。

修复：`_enable_wal()` 退避重试 4 次 → 仍失败则**降级为默认回滚日志模式继续运行**
（WAL 只是并发优化），并留 WARNING；新增 `StateStore.journal_mode` 便于自检呈现。

### 1.6.6 本轮验证（全部重跑）

| 套件 | 结果 |
| --- | --- |
| `pytest`（含本轮新增 17 个用例） | **337 passed**, 1 deselected |
| `scripts/selftest_e2e.py` | 22/22 |
| `scripts/mock_platform.py` | 27/27（新增「模拟流量未污染真实抓包」检查） |
| `scripts/verify_pause.py` | 9/9 |
| `scripts/verify_gui.py` | 29/29 |
| `scripts/verify_dist.py` | 20/20 |
| `exe --doctor`（冻结态，含产物编码） | 20/20 |

**测试隔离的正面证据**：全量测试跑完后 `recon/network.jsonl` 里**一条模拟记录都没有** ——
文件里现在只有 **12 条真实请求，且全部来自 `sso.ecnu.edu.cn`**（两次真机登录的认证页加载）。
改前该文件每跑一次测试就新增 20+ 条 `127.0.0.1` 记录（最多累积到 644 条，真实请求仅 5 条）。

---

## 1.7 第 22 轮（前半）：产物编码（中文乱码）+ 冻结态字节头验证

### 1.7.1 怎么发现的：去翻自己的产物，而不是翻代码

上一轮修完「启动即崩」后，我换了个视角找问题：**不看代码，只看产物**——
把 `output/` 里真实写出的文件当黑盒，用十六进制看它的头几个字节：

```
无 BOM 头 6 字节 : E5 90 8C E5 AD A6        # 「同学」的 UTF-8，但没有任何编码标记
chcp             : Active code page: 936     # 简体中文 Windows 的 ANSI 代码页
```

无 BOM + 代码页 936 = **读取方只能猜**，而 Windows 上的默认猜测就是 cp936。
于是中文产物在记事本、`Get-Content`、以及大量字幕播放器里变成乱码。

### 1.7.2 缺陷 36：产物无 BOM，中文在 Windows 上被猜错编码（严重）

用**真实 Windows 读取器**取证（Windows PowerShell 5.1 的 `Get-Content`：
有 BOM 认 BOM，无 BOM 按 ANSI）：

```
nobom.txt   默认读取首行前 6 码点: U+935A U+5C7D U+E11F U+6D60 U+E100 U+30BD   ← 乱码
bom.txt     默认读取首行前 6 码点: U+540C U+5B66 U+4EEC U+597D U+FF0C U+4ECA   ← 同学们好，今
```

这条证据的价值在于：它**不是**「某个软件脾气怪」，而是系统默认行为。
用户第一次打开产物看到的就是乱码 —— 会直接判定「这工具转写是坏的」，
而日志、退出码、测试全都是绿的。

### 1.7.3 修复与设计边界

| 项 | 结论 |
| --- | --- |
| `.txt` / `.srt` / `.md` | 默认写 **UTF-8 BOM**（`EF BB BF`），记事本 / VLC / PotPlayer / MPC 都能正确识别 |
| `.transcript.json` | **有意不带 BOM** —— 机器可读产物，BOM 会让朴素解析器（非 `utf-8-sig`）踩坑；已加测试锁死 |
| 换行 | 仍严格 `LF`（`newline=""`）；BOM 不改变换行约定，SRT 播放器不受影响 |
| 可配置 | `Config.emit_utf8_bom`（默认 `True`）+ 设置页「产物写入 UTF-8 BOM」开关（与 txt/srt/md 三个开关同区） |

### 1.7.4 关键补充：在**冻结态**里验证字节头

源码测试通过 ≠ 打包产物行为正确（本项目已经栽过两次：Playwright 路径、
`_closeEvent` 崩溃）。所以 `scripts/doctor.py` 新增 **[7] 产物编码**：

```
[7] 产物编码（真实导出一次，验字节头）
  ✅ 三产物均带 UTF-8 BOM（Windows 记事本/字幕播放器不乱码）  — .md=EF BB BF / .srt=EF BB BF / .txt=EF BB BF
  ✅ transcript.json 不带 BOM（机器可读，避免解析器踩坑）
  ✅ 正文可被 UTF-8 读取器正确还原  — 中文编码自检：同学们好，今天讲二叉树。
```

它由 `大夏学堂转写助手.exe --doctor` 触发，直接被 `verify_dist.py` 纳入验收 ——
**打包产物以后每次验收都会验一遍「写出来的字节到底长什么样」**。

### 1.7.5 新增测试（5 个用例，全部先红后绿）

`tests/test_exporter.py`：

| 用例 | 断言 |
| --- | --- |
| `test_exports_carry_utf8_bom_by_default` | 三产物原始字节以 `EF BB BF` 开头；BOM 后仍是合法 UTF-8；并证明**同样的字节按 cp936 读会得到不同文本** |
| `test_bom_can_be_disabled` | `bom=False` 时无 BOM，但仍是 UTF-8；SRT/MD 首行照常 |
| `test_write_txt_bom_flag_is_honored` | 同一路径先后 `bom=False` / `bom=True` 都生效 |
| `test_srt_still_parses_after_bom` | 剥掉 BOM 后首行是序号、时间轴匹配 `HH:MM:SS,mmm --> ...`、**无 CRLF** |
| `test_bom_does_not_leak_into_transcript_json` | 机器可读产物保持无 BOM |

`tests/test_settings_dialog.py`：把 `emit_utf8_bom` 纳入「非默认配置 → 控件 → 落盘」往返测试
（防「新增字段静默不落盘」—— 这是本项目反复踩过的坑）。

> 顺带一处诚实说明：写测试时我原本假定「cp936 解码一定得到 mojibake」，
> 实测发现**它也可能直接抛 `UnicodeDecodeError`**。于是把断言改成一个
> `_cp936_misreads()` 助手：解出不同文本**或**解码失败，都算「猜错」——
> 让测试描述现实，而不是让现实迁就测试。

## 2. 第 19 轮交付：登录态契约验证 + 修掉「启动即崩」

### 2.1 为什么盯上它

`load_session_state()` 是全应用**最关键的契约**：Playwright 写出的
`storage_state.json` 必须能被 httpx 侧正确消费。任何一处对不上（字段名、domain、
token 键名），表现都是「登录明明成功了，但拉清单说未登录」—— 用户完全看不出原因。

### 2.2 缺陷 35：结构不对就让应用启动即崩（严重）

该函数只用 try/except 包住 `json.loads`，之后直接 `data.get(...)` 并假设
`cookies` / `origins` / `localStorage` 都是「字典列表」。于是**合法 JSON 但结构不对**时：

```
data = null                       → AttributeError: 'NoneType' object has no attribute 'get'
data = []                         → AttributeError: 'list' object has no attribute 'get'
data = {"cookies": "not a list"}  → AttributeError: 'str' object has no attribute 'get'
localStorage = "bad"              → AttributeError: 'str' object has no attribute 'get'
```

这个文件是**被外部工具写在用户目录里的明文 JSON**，被手改、被半途截断、
或被旧版本写成别的形状都完全可能；而它在 **GUI 启动路径**上被调用
（刷新登录态提示就调它）—— 后果是**应用一打开就崩，用户无法自救**。

修复：新增 `_as_dict_list()` 作为形状防线（只保留字典元素）；
顶层非对象时按「未登录」处理并记警告；`path.stat()` 加 OSError 兜底。
**任何**异常或结构不符都退化为空登录态，绝不向上抛。

### 2.3 新增测试（22 个用例）

`tests/test_login_state.py`：

**契约往返**（用本地假平台**收真实请求头**验证，而不是只看对象字段）
```
✅ storage_state 里的 Cookie 与 localStorage token 真的出现在 HTTP 请求里
✅ 未登录时不伪造 Cookie 头
✅ localStorage 里是 `Bearer xxx` 形式时正确提到 Authorization 头
✅ Cookie 头以 '; ' 分隔；Referer / UA 都在
```

**健壮性**
```
✅ 10 种畸形文件（空/空白/非 JSON/语法错误/顶层数组/null/
   cookies 非数组/cookie 缺 name/localStorage 非数组）都不抛异常，
   且不会被当成「已登录」
```

**脱敏与诊断**
```
✅ summary() 保留键名、不泄漏任何值
✅ 可达性诊断能区分「需要登录」（可达）与「被 webVPN 拦下」（不可达）
```

测试 290 → **312**；GUI 验收 29/29、干净环境 20/20、离线演示 2/2 无回归。

---

## 3. 第 18 轮交付：状态库的并发安全与崩溃恢复

### 2.1 为什么盯上它

断点续跑完全建立在 `state.db` 之上，而它此前**只有单线程测试**。
真实场景里它要同时承受：PipelineWorker 写、UI 读、启动时 `recover_orphans()` 写；
还要承受应用被强杀（Ctrl+C / 断电）后重新打开。这些都没验证过。

### 2.2 结果：13 个用例**一次全部通过**

这轮没抓到缺陷 —— 前几轮连续有问题，这次的结果说明状态库这块设计是站得住的。
把这些不变量固化成测试，防止后续改动弄坏它：

**并发安全**（GUI 工作线程会同时命中同一个 `StateStore` 单例）
```
✅ 8 个线程并发 upsert 同一资源 → 只能产生一条任务（唯一索引 + 幂等 upsert）
✅ 写线程与读线程并发跑 1 秒 → 无异常、进度始终在 0~100
✅ 6 个线程并发抢锁 → 只有一个赢家（否则同一任务会被两个 worker 同时跑）
✅ 20 个任务并发推进阶段 → 统计与事件表不丢事件
```

**崩溃恢复**
```
✅ 关闭再打开：任务/进度/产物列表/事件历史全部保留
✅ 真·强杀（起子进程写一半后 kill()）：父进程仍能打开库、读到任务，
   recover_orphans() 回退到 audio_ready（有音频不重下），之后能继续推进到 done
✅ WAL 模式确实开启
✅ 上次运行残留的锁不会永久阻塞任务（clear_all_locks 后可重获）
✅ recover_orphans() 幂等：重复调用不会反复抹掉进度
✅ 终态（done/failed/canceled）绝不被启动恢复改动
✅ 有音频 → audio_ready；音频不存在 → pending
```

**损坏处理**
```
✅ 库文件损坏 → 抛可读的 sqlite3.DatabaseError（不静默给空清单）
✅ 文件被截断后重开 → 要么明确报错，要么仍能读出任务，绝不假装「没有任务」
```

测试 277 → **290**；GUI 验收 29/29、干净环境 20/20 无回归。

---

## 4. 第 17 轮交付：修复「限速一开、下载必挂」

### 2.1 怎么发现的

继续扫盲区，这轮是 **downloader**（「能不能拿到音频」的唯一路径，此前几乎没测试）。
先问了一个问题：**「限速」这个设置项真的有用吗？** 直接实测：

```
分片总大小: 4215 KiB
不限速: 3.3s   限速128 KiB/s: 14.9s   （真限速至少需 32.9s）
```

有点慢下来了，但先看第一次尝试（旧代码）的输出：

```
Codec AVOption maxrate (maximum bitrate (in bits/s)...) is not a decoding option.
Error opening input files: Invalid argument
→ 音频获取失败（重试 4 次）
```

### 2.2 缺陷 34：限速一开，所有下载必然失败（严重）

`media.build_input_args()` 把 ``-maxrate`` 放进了 ``-i`` 之前的**输入侧**参数，
但它是 ffmpeg 的**输出侧编码**选项（质量控制的 VBV 参数）。ffmpeg 拒绝打开输入，
pipeline 重试 4 次后以「音频获取失败（重试 4 次）：ffmpeg 未产出有效音频文件」告终。

**也就是：只要用户在设置里填了非 0 的「限速」，所有任务都会失败。**
此前一直没暴露，是因为所有测试用的都是默认值 ``speed_limit_kib=0``。

### 2.3 修复

* 从输入侧参数里**移除** ``-maxrate``，并在代码里写清为什么不能放这里；
* 限速改为在 downloader 里**自己节流**：读 ffmpeg ``-progress`` 的 ``total_size``，
  按目标速率与经过时间睡眠；副作用是 ffmpeg 的 stdout 管道被写满后自身也会慢下来；
* 实测有效：4.2 MB 的 HLS，不限速 3.3s → 限速 128 KiB/s 后 **14.9s（约 4.5×）**。
  受 ffmpeg 内部缓冲与进度上报粒度（0.5s/次）影响**不是字节级精确**，
  因此把设置项 tooltip 改成如实描述「近似限速」，而不是假装精确。

### 2.4 新增测试（11 个用例）

`tests/test_downloader.py`：

```
✅ 回归：build_input_args() 在任何限速值下都不能出现 -maxrate
✅ 回归：设了限速也必须能下载成功（旧代码 4 次重试后失败）
✅ 限速要真的变慢（≥1.8×）
✅ 带时效签名的播放地址能正常拉取（query 不被丢掉）
✅ 播放列表 404 时给出可读错误
✅ 本地文件走纯转码路径（回归：曾因网络参数导致 Option headers not found）
✅ 缓存路径稳定且文件名合法（断点续跑依赖它）
✅ 缓存命中跳过下载且 sha256 一致
```

测试 266 → **277**；e2e 22/22、离线演示 2/2、GUI 验收 29/29、干净环境 20/20 无回归。

---

## 5. 第 16 轮交付：设置对话框的往返保真度

### 2.1 为什么盯上它

继续「找测试盲区」的方法，下一个是 **`SettingsDialog`** —— 它是写入**全部设置**
与**两个 API Key** 的唯一入口，但此前**零测试**。它要防的是「静默丢设置 / 静默改设置」：
界面 `_load()` 填错，用户以为设置没保存；控件 `on_save()` 写漏一个字段，
转写参数就悄悄变样，而用户看不出来。

### 2.2 本轮修掉的 1 个缺陷（误导性 UI）

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 33 | 「预设」下拉只按 **provider** 反查，而预设 ①③④⑥ 的 provider 都是 `openai_compatible` | 用户配的是本地服务（`http://127.0.0.1:8000/v1`），下拉却显示「④ OpenAI 官方」被选中 —— 用户会以为自己配错了，或被误导去改配置 | 改为按 **(provider, base_url, model) 三元组**反查；匹配不上时落到新增的「（自定义 / 当前配置）」项。顺带：选「自定义」不再覆写用户已填内容；加载时按 provider 正确设置 base_url 输入框可用状态 |

### 2.3 新增测试（11 个用例）

`tests/test_settings_dialog.py` 的核心是**往返保真度**：

```
✅ _load()：用一份「每个字段都非默认」的配置打开，逐项断言 40+ 个控件的显示值
   （含 "96k" → 96 的单位换算、枚举/下拉的 data 值）
✅ on_save()：改 30+ 个控件后保存，读回配置逐项断言
   （含 96 → "96k"、jitter_max 被夹到 ≥ jitter_min、output_dir 原样落盘）
✅ 凭据：Key 只能进 DPAPI；断言 config.json 与 secrets.json 里都没有明文 Key
✅ on_save() 必须 accept() 对话框（否则调用方按 Cancel 处理，看起来「没保存」）
✅ 码率往返多轮不漂移（32/64/128/192/320）
✅ 预设逐个断言 provider/base/model 都被填上；本地预设必须指向 127.0.0.1
✅ DashScope 预设的地址与模型必须与 README 一致（防止文档与代码脱节）
```

测试 255 → **266**；GUI 验收 29/29、干净环境 20/20 无回归。

---

## 6. 第 15 轮交付：修复 DashScope「原生异步模式」完全不可用

### 2.1 怎么发现的

我意识到「测试覆盖 ≠ 代码正确」，于是换了个做法：**先找没被任何测试碰过的生产代码路径**。
一查就发现 GUI 里那个开关 ——「使用 DashScope 原生异步 API（大文件/长音频更稳）」——
对应的 `DashScopeTranscriber._transcribe_native()` **一行测试都没有**。

补测试第一次跑，**15 个用例挂了 10 个**：

```
AttributeError: 'DashScopeTranscriber' object has no attribute '_notify'
```

### 2.2 修掉的 4 个缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| **29** | `DashScopeTranscriber` **缺少 `_notify()`** —— `_transcribe_native()` 一路上都在调它，但类里没定义，也不是继承来的（与 OpenAI 兼容实现是**组合**关系） | **原生异步模式一进去就崩**：GUI 里写着「大文件/长音频更稳」，用户一勾选，每次转写都必然失败。而长课程正好是该模式的适用场景 | 补上 `_notify()` 与 `_wait_gate()`（后者让原生模式的轮询循环也能响应暂停） |
| 30 | 原生模式 base URL 硬编码 | 无法指向私有化部署/代理网关；也让这条链路彻底无法测试 | 改为可配置：`ECNU_DASHSCOPE_NATIVE_BASE` 覆盖（默认仍是官方地址） |
| 31 | 句级结果为空时返回**空转写** | 用户看到「ASR 返回了空结果」这种**误导性报错**（其实识别是成功的） | 句级没产出任何内容时一定回退到 `transcripts[].text` |
| 32 | 下载转写结果的 HTTP 错误漏出原始 `httpx.HTTPStatusError` | 结果链接过期（通常 24 小时）时用户看到裸栈 | 包装成 `TranscriptionError`，并提示「链接可能过期 / 换网络 / 本地处理不会重做」 |

另外把轮询从固定 5s 改成**自适应间隔**（2s → 最多 10s），并在等待期间响应暂停。

### 2.3 新增测试

`tests/fake_dashscope_server.py` —— 可编排的假 DashScope 原生服务
（getPolicy 凭证 / OSS 上传 / 异步提交 / 轮询状态机 / 结果下发；
可注入 401、空凭证、缺 task_id、提交 400、任务 FAILED、句级结果缺失）。

`tests/test_dashscope_native.py`（15 个用例）逐项断言这条 5 步链路：
完整成功链路、上传必须带齐全部 OSS 凭证、提交必须带 `X-DashScope-Async: enable`、
**毫秒时间戳换算**、句级缺失/全空时的整段文本兜底、5 类失败路径可诊断、
非原生模式不碰原生接口。

测试 240 → **255**；GUI 验收 29/29、干净环境 20/20、离线演示 2/2 无回归。

---

## 7. 第 12 轮交付：修掉「一关就崩溃」+ 打包产物干净环境验证

### 2.1 怎么发现的：补 DoD 第 1 条的正面证据时顺手抓到崩溃

DoD 第 1 条要求「可双击运行的 exe，且在同机重新打开能跑通」。我写了
`scripts/verify_dist.py` 去正面验证它 —— 把 `dist` 复制到**纯 ASCII 路径**、
用**全新用户数据目录**、把 `PATH` 收窄到**不含 Python**，然后真实启动 exe。
结果 `--doctor` 17/17 通过，但 `--selftest` **退出码 `-1073740791`（0xC0000409）**。

进一步隔离发现：**开发模式（venv python 跑 main.py）也一样崩**，
所以与打包、路径、用户目录都无关 —— 是**关窗流程本身**的问题。

### 2.2 根因

`MainWindow.closeEvent` 手写清单只列了三个线程：

```python
running = [w for w in (self.pipeline_worker, self.catalog_worker, self.login_worker) ...]
```

而后来加进来的 `probe_worker`（首启 800ms 后自动跑的「首启一键检查」）**没被列进去**。
关窗时它还在跑（正在做联网探测，超时可达 20s），Qt 直接析构仍在运行的 `QThread`
→ 进程崩溃。

**为什么之前一直没发现**：早先的 `--selftest` 冒烟只看日志有没有报错，
日志里一切正常（窗口关了、写了「应用退出」），但**进程退出码是非零的**，
而我没检查退出码。

### 2.3 修复

* 新增 `_all_workers()` **动态枚举**：显式属性 + `findChildren(QThread)` 兜底 ——
  以后再加线程不会再漏；
* 只对**任务型**线程弹「仍在运行」确认框；只读的诊断线程静默停掉，不打扰用户；
* 等待超时后 `terminate()` 兜底，避免「点了关闭却没反应」。

实测：`--selftest` 退出码由 `-1073740791` → **0**（连跑 3 次稳定）。

### 2.4 打包产物干净环境验证：20 项通过，0 项失败

```
✅ 复制到纯 ASCII 路径（C:\ecnu-clean-test）      ✅ 路径无中文/空格
✅ 用户数据目录已隔离                            ✅ PATH 中已无 Python
✅ --doctor 退出码为 0                           ✅ 自检无失败项（17/17）
✅ 用的是自带 ffmpeg（不是系统 PATH 里的）
✅ --selftest 退出码为 0                         ✅ frozen 模式识别正确
✅ output/ cache/ data/ logs/ 都在 exe 同级（可整体拷走）
✅ 日志已写入且被脱敏                            ✅ 日志里的 app_root 指向复制后的路径
✅ 配置写到 %LOCALAPPDATA%\ecnu-transcribe      ✅ 配置内容可解析（53 个键）
✅ 配置里不含明文密钥
结果：20 项通过，0 项失败，0 项提醒
```

另外两处正面证据：

* **双击启动**（不带参数）：进程存活、窗口标题 `大夏学堂录播转写助手 v0.9.1`、
  `CloseMainWindow()` 优雅关闭 → **退出码 0**；
* **exe 二进制扫描**：不含任何指向开发工作区的绝对路径（`D:\desktop`、`.venv` 均未出现）。

### 2.5 新增测试

`tests/test_gui_recovery.py` 增加 3 个关窗行为用例：
`_all_workers()` 枚举完整性、诊断线程运行中关窗不弹框且能停住、
任务线程运行中关窗会确认并等待。测试 234 → **237**。

---

### 2.6 第 11 轮交付（异常路径与恢复操作，保留索引）

#### 2.6.1 问题：只测了「顺利跑完」，没测「出问题怎么办」

之前的 GUI 验收（`verify_gui.py`，29 项）跑的是**全部成功**的路径。但真实长跑场景里，
一节课要下载+转写十几分钟，途中**登录态过期是预期内的事**（README 专门写了这条），
DRM、ASR 失败、用户中途移除/重跑也都会遇到。这些路径此前**一个测试都没有** ——
也就是说「失败之后界面还能不能用、用户能不能自己恢复」是没证据的。

#### 2.6.2 方案：真实窗口 + 真实线程 + 可编排的假流水线

`tests/test_gui_recovery.py`（16 个用例）：用**真实的 `MainWindow`** 与
**真实的 `PipelineWorker(QThread)`**，只把 `Pipeline.run` 换成可编排的假实现 ——
信号槽、状态机、UI 更新走的都是产品代码，无需网络与 ffmpeg。

覆盖范围：

| 分组 | 用例 |
| --- | --- |
| Worker 异常分支 | 成功路径；**登录态中途失效**（发重登信号 + 弹提示 + 队列立即停止 + 状态栏同步）；DRM（标失败 + 错误详情有内容）；普通失败（error 落库 + 重试累加）；用户取消（落 `canceled` 而非 `failed`） |
| 界面恢复操作 | 移除（软删除，产物与历史保留）；重跑（`pending` + **保留音频缓存**）；强制重跑（清空音频引用）；清空列表（**不删用户产物**）；清除登录态（删 `storage_state.json` + 状态栏回到「未登录」） |
| UI 一致性 | 树与表格状态一致；搜索过滤（命中/隐藏/清空恢复）；「只选未转写」跳过已完成；导出清单；打开输出目录 |

#### 2.6.3 第 11 轮修掉的 1 个缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 25 | 启动定时回调（`QTimer.singleShot(200ms)`）可能在窗口关闭后才触发 | 快速关窗时踩到已关闭的数据库，打出 `sqlite3.ProgrammingError: Cannot operate on a closed database` 堆栈 | 回调开头做可见性守卫 + `recover_orphans()` 异常兜底（启动提示不该反过来把应用搞崩） |

> 这个缺陷正是被新测试逼出来的：测试夹具在 200ms 内关窗并关闭状态库，
> 立刻暴露了这条时序问题。真实用户「打开就后悔、马上关掉」也会踩到。

测试 218 → **234**；GUI 验收 29/29、离线演示 2/2 无回归。

---

### 2.7 第 10 轮交付（LLM 后处理测试与修复，保留索引）

#### 2.4.1 问题：`llm.py` 一直是零测试

`llm.py` 会把 ASR 原文交给 DeepSeek 改写再写进你的 `.txt/.srt/.md`，
但整个模块**一个测试都没有**。而它最危险的地方不是「调用失败」（那会记告警、退化为原文），
而是**模型返回了不合规内容却没被发现** —— 那会直接把错的东西写进产物。

#### 2.4.2 方案：假 LLM 端点 + 安全不变量测试

`tests/fake_llm_server.py`：可编排的 OpenAI 兼容假端点（按调用序号返回任意内容、
可注入 401/429/500/503、可读取请求体）。配合 `tests/test_llm_postprocess.py`（34 个用例），
核心断言的是**不变量**而不是功能体验：

* 修复前后**片段数量与时间戳必须逐个相等**（只允许改 text）；
* 10 种畸形响应（空内容 / 非 JSON / 缺 text / 只回解释文字 / 回 HTML …）必须**退化为原文**；
* HTTP 401/429/500/503 一律降级为「保留原文 + 告警」，不让任务失败；
* 重新分段不丢内容、时间轴单调、**解析失败保持原分段**；
* 1-based 序号、```json 围栏、前后夹说明文字等真实模型行为都能正确处理；
* 长转写按上限分块调用且每块都生效；
* API Key 不出现在告警/日志里。

#### 2.4.3 第一次跑就抓到 2 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| **23** | **「解析失败」被当成「不需要换段」** —— `_parse_break_list()` 失败时返回空集合，调用方理解成「模型说没有断点」 | **整篇被合并成一段**（实测 10 段 → 1 段），时间轴信息被悄悄抹掉。因为**文字内容还在**（只是并成一段），肉眼几乎看不出来，但 `.srt` 退化成一条覆盖整节课的字幕 | 区分两种语义：解析失败返回 `None`（保持原分段），明确返回空列表才是「不换段」。顺带修掉 `{"break_after": []}` 因 `or` 短路被误判为解析失败 |
| 24 | 修复类请求把「输出格式示例」（`{"segments":[{"i":0,...}]}`）放在**用户消息**里 | 模型偶尔回显这条示例，示例里的 `i=0` 与真实数据的 `i=0` **撞车** —— 看起来「调用成功了」，实际只改了第 0 条甚至什么都没改 | 用户消息**只放数据**，格式说明全部移到 system prompt |

> 这两个都属于「沉默型故障」：不报错、日志正常、文字看起来也在，
> 但产物的可用性已经被破坏。只有把「不变量」写成断言才抓得住。

测试 184 → **218**；离线演示 2/2、GUI 验收 29/29 无回归。

---

### 2.8 第 9 轮交付（切分管线双偏移修复，保留索引）

#### 2.5.1 怎么发现的

之前所有验证跑的都是**短视频**（20~50 秒），走「整段转写」路径，从不触发切分。
这轮我补了「长音频切分」的集成测试（真实多段切分 → 逐段 ASR → 平移合并），
第一次跑就抓到时间轴全错：

```
merge input : [(0.0,(0.0,5.0)), (4.0,(4.0,9.0)), (8.0,(8.0,13.0)), (12.0,(12.0,17.0)), (16.0,(16.0,20.0))]
merge output: [(0.0,5.0),      (8.0,13.0),      (16.0,21.0),      (24.0,29.0),       (32.0,36.0)]   ← 全错
```

音频只有 20 秒，末段却被算到 32~36 秒。

#### 2.5.2 根因

`transcribe_chunked()` 调用 `_transcribe_one(..., offset=chunk.start)` 时，
返回的片段**已经是全局时间戳**了；随后又把 `(chunk.start, segs)` 交给
`merge_segments()`，而它的语义是「**局部时间 + offset**」→ 偏移被加了两次，
第 N 段变成 `2*start + local`。

修复：`results.append((0.0, segs))`，并在 `merge_segments` 的 docstring 里
把 offset 语义写死（调用方若已拿到全局时间戳就必须传 `0.0`）。修复后：

```
merge output: [(0.0,5.0), (4.0,9.0), (8.0,13.0), (12.0,17.0), (16.0,20.0)]   ✅ 与切分方案一致
```

**影响范围**：只有超过单段上限、需要切分的长课程录播才触发 ——
也就是你的真实场景（一节课 45~120 分钟）**必然触发**，而之前的测试全都没覆盖到。

#### 2.5.3 顺带修掉的 2 个相邻缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 21 | 分段切片缓存 key 只含音频哈希，改了 `asr_max_segment_sec`/`overlap`/策略后会**复用旧切片** | 内容按旧方案切、时间戳按新方案平移 → 错位字幕，日志毫无异常（文件名一样、文件也在） | key 改为 `<音频哈希>-<切分方案哈希>`，并给 `_segment_usable()` 加**时长校验**（偏差 ≤ 25% 才复用），双保险 |
| 22 | 片段可能超出音频总长（ffmpeg 切片略超 + 端点按实际解码长度报时间戳） | 产生永远播不到的字幕 | 新增 `_clamp_to_range()`：分段内夹取 + 合并后整体夹进 `[0, 总长]` |

#### 2.5.4 第 9 轮新增测试

* `tests/fake_asr_server.py` —— 可编排的 OpenAI 兼容假 ASR 服务：
  按调用序号返回预设文本、按切片**真实时长**生成时间戳、支持 `text`/`srt`/`verbose_json`。
  这样切分管线的集成测试可以完全离线、确定性运行。
* `tests/test_chunked_pipeline.py`（10 个用例）：真实音频的静音检测与切分规划；
  **真实多段切分 → 逐段 ASR → 平移合并**的不变量断言（段段有调用、内容不丢不重、
  时间轴单调不越界、偏移只加一次）；缓存 key 随切分方案变化；
  端点不支持 `verbose_json` 时回退 `srt`；全段失败时显式抛错。

测试 174 → **184**；离线演示 2/2、GUI 验收 29/29 均无回归。

---

### 2.9 第 8 轮交付（首启一键检查，保留索引）

#### 2.5.1 问题

应用本身很稳（174 个测试全绿），但对第一次打开它的人来说，**要做什么全靠猜**：
网络是不是通的？为什么点了刷新没反应？要先登录还是先配 ASR？
之前唯一的反馈是一堆日志和一句「尚未登录」。

#### 2.5.2 方案：`F2 首启检查`

`ecnu_transcribe.login.run_readiness_check()` 一次查完所有前置条件，
**每个失败项都给出可照做的动作**，最后汇总一句「下一步做什么」：

```
【首启一键诊断】
  ✅ 输出目录可写：D:\desktop\obj_ai\大夏学堂转写助手\output
  ✅ ffmpeg 可用：C:\Users\...\ffmpeg.EXE
  ✅ 登录用浏览器（Chromium）：正常
  ⛔ 课程平台网络可达：已被 webVPN 网关拦截：该入口需要校园网或学校 SSL-VPN。
      → 当前既不在校园网、也没走 VPN。请接入校园网，或连接学校 SSL-VPN
        （https://vpn.ecnu.edu.cn/portal/），然后点「重新检查」。
  ⛔ 已保存登录态：尚未登录
      → 点「登录」，在弹出的浏览器里完成一次统一身份认证（学号 + 密码，可能还有验证码）
  ⛔ 语音识别（ASR）已配置：未填 API Key（端点 https://dashscope.aliyuncs.com/compatible-mode/v1）
      → 二选一：① 跑本地服务 scripts/local_asr_server.py，Base URL 指向
        http://127.0.0.1:8000/v1（不需要 Key）；② 到「设置 → 语音识别」填 DashScope API Key

下一步：当前既不在校园网、也没走 VPN。请接入校园网，或连接学校 SSL-VPN…
```

设计要点：

* 区分**阻塞项（⛔）**与**非阻塞提醒（⚠️）**：例如没配 DeepSeek 只是提醒，不影响主流程，
  因此 `ready` 判定只看阻塞项。
* `next_action()` 优先返回**第一个阻塞项**的动作 —— 用户不需要自己判断先做哪个。
* **首启（未登录时）自动静默跑一次**并写进右栏日志，弹窗留给用户主动点击（`F2`）。
* 诊断本身不能成为新故障点：任何一项内部异常都被捕获成一项失败结果，不会抛出去。

#### 2.5.3 测试

`tests/test_readiness.py`（13 个用例）：报告结构与图标、阻塞判定、
「下一步」优先级（阻塞项优先）、失败项必须带动作、离线不抛异常、
本机 ASR 端点不被误判为「未配置」、报告是可复制的纯文本。
测试 161 → **174**。

#### 2.5.4 第 8 轮说明

这一版**没有修复缺陷** —— 纯粹是可用性补强：把「该连 VPN / 该登录 / 该配 ASR」
从「用户自己猜」变成「应用直接告诉他」。

---

### 2.10 第 7 轮交付（M4 验收自动化，保留索引）

#### 2.6.1 问题：GUI 只做过「能启动」的冒烟，从没验证过交互

应用有三栏主窗口、按钮、表格、树控件，但此前唯一的 GUI 测试是
`exe --selftest`（启动 2 秒退出）。也就是说「勾选后能不能入队」「点开始后表格会不会更新」
「工作线程有没有碰 UI」这些问题**一次都没被验证过**。

#### 2.6.2 方案：`scripts/verify_gui.py`

在 offscreen 模式下**驱动真实控件**（`setCheckState` / 调真实槽函数 / 信号槽连接），
用 `processEvents` 驱动事件循环，把 M4 验收里「登录之后」的所有环节串起来跑：

```
[模拟平台] 真实 HTTP 接口（3 门课 / 7 条资源）+ 真实 HLS 媒体服务（含 AES-128 一路）
    → [真实 EcnuClient] fetch_catalog() → Catalog
    → [真实 MainWindow] 渲染课程树 → 勾选 2 条 → on_enqueue_checked()
    → [真实 PipelineWorker(QThread)] 下载 → 本地 ASR → 写出 txt/srt/md
    → [真实 Pipeline] 断点续跑：重跑不重下、不重跑 ASR
```

为此给模拟平台补了**静态媒体服务**（`serve(media_dir=...)`）：除接口外还能把本地生成的
HLS 播放列表/分片/AES key 直接发出去 —— 于是整条链路在完全离线的情况下端到端跑通。

#### 2.6.3 实测结果：29 项通过，0 项失败

```
✅ MainWindow 构造并显示成功  — 三栏 size=1560x940
✅ 课程树已渲染（3 个顶层课程节点）      ✅ 录播子节点共 7 条
✅ 勾选生效  — 2 条                    ✅ 「已勾选」统计已更新  — 已勾选：2 条 / 01:30:42
✅ 任务表格出现 2 行                   ✅ 任务初始阶段为 pending
✅ 后台线程已结束（UI 未被阻塞致死）    ✅ 收到 queue_finished 信号  — [(2, 0)]
✅ UI 收到阶段回调  — 21 条 task_stage 信号
✅ 阶段覆盖 downloading/transcribing/done
✅ 状态库中 2 条都是 done               ✅ 表格「阶段」列显示完成  — ['完成', '完成']
✅ 表格进度条到 100                     ✅ 共 6 个产物文件被记录
✅ 6 个产物真实存在于磁盘               ✅ 扩展名为 txt/srt/md 各 2 个
✅ txt 有真实转写文本  — 同学问好,今天我们讲现性表的顺序存储结构。…
✅ srt 含时间轴   ✅ md 含元信息与全文
✅ 树节点状态已刷新为完成               ✅ 日志面板有内容  — 2531 字符
✅ 重跑后仍是 done   ✅ 音频未重新下载   ✅ 产物仍为 3 个（并生成 .bak 备份）
✅ 窗口正常关闭
结果：29 项通过，0 项失败
```

这同时验证了几件此前没证据的事：**工作线程没有直接碰 UI**（21 条信号正常回传、界面正常更新）、
**产物「只增不改」的备份语义真的生效**（重跑后出现 `.bak-20260912-131901`）、
**树/表/日志三处状态一致**。

#### 2.6.4 第 7 轮修掉的 2 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 18 | offscreen 环境下**模态 `QMessageBox` 永久阻塞** | 任务全部成功的分支会弹「完成」框，自动化环境没有用户点击 → 验证脚本卡死（本次实测挂起 10 分钟）。真实使用不受影响 | `verify_gui.py` 把 `QMessageBox.information/warning/critical/question` 替换成记录器，既防卡死又能断言「该弹的窗弹了」 |
| 19 | 验证脚本里模拟平台的时长与本地样例素材不符 | 触发「时长偏差 > 2%」告警，属测试噪声 | 验证脚本把选中条目时长对齐素材真实时长 |

---

### 2.11 第 6 轮交付（清单客户端 HTTP 验证，保留索引）

#### 2.7.1 问题：M1 的 HTTP 逻辑一直没有被真实验证

真实课程平台校外不可达，于是 `EcnuClient`（分页 / 业务码 / 鉴权失效 / 候选路径探测 /
播放地址解析）**从来没有对着真实 HTTP 跑过一次** —— 全是单元级解析测试。
这是交付物里最大的未验证面：网络一通就要靠它抓清单。

#### 2.7.2 方案：模拟平台服务器 `scripts/mock_platform.py`

起一个本地 HTTP 服务，按平台常见的 `{code, msg, data}` 包装返回假数据
（3 门课 / 7 条录播，字段命名故意混用 camelCase、snake_case、别名），
然后用**同一个** `EcnuClient` 完整跑一遍。

服务按**端口**区分模式（URL 前缀会被 `urljoin` 吃掉，前缀式模式根本传不到服务端 —— 踩过一次）：

| 模式 | 行为 |
| --- | --- |
| `normal` | 正常返回，课程 3 门 / 资源 7 条，分页 `total` 生效 |
| `empty` | `total=0`（合法空清单） |
| `autherr` | 业务码 `A0230`（登录已过期） |
| `http401` | HTTP 401 |
| `redirect` | 302 跳登录页 |
| `slow` | 默认第一个候选路径返回业务错误，第二个才可用（验证候选探测） |
| `weird` | 响应结构异常（验证 `ApiChangedError`） |

#### 2.7.3 实测结果：26 项通过，0 项失败

```
[1] 全量清单：分页 / 字段宽容解析 / 保存
  ✅ 课程数 = 3（翻了 2 页）        ✅ 资源总数 = 7
  ✅ 时长解析正确（含 "01:02:05"/"45:30" 字符串形式）  — 20562s vs 期望 20562s
  ✅ 课程名解析（camelCase/snake_case/name 三种风格）
  ✅ 毫秒时间戳已转成本地时间      ✅ mime 推断（无 contentType 时按 URL 猜）
[2] catalog.json 落盘与读回
  ✅ 顶层统计字段齐全  — 课程数 3 / 资源总数 7 / 时长合计 05:42:42
[3] 播放地址解析（清单未带 URL 时走播放接口）
  ✅ 带时效签名参数被保留  — ...index.m3u8?sign=sig-R-10031&expires=1790000000
[4] 鉴权失效必须显式报错（不能静默返回空清单）
  ✅ 业务码 A0230 → AuthExpiredError
  ✅ HTTP 401   → AuthExpiredError
  ✅ 302 跳登录页 → AuthExpiredError
[5] 结构异常 → ApiChangedError      ✅
[6] total=0 正常返回空清单          ✅
[7] 候选路径探测自动切换并缓存命中   ✅  — {'course_list': '/api/course/list'}
[8] 请求记录已脱敏                  ✅  — 9 行，不含假 Cookie 值
结果：26 项通过，0 项失败
```

#### 2.7.4 同时并入了 pytest（15 个用例）

新增 `tests/test_client_http.py`：把模拟平台作为 fixture（模块级复用、按模式分端口），
覆盖全量清单、分页、时长字符串解析、毫秒时间戳、mime 推断、catalog 往返、
空清单、三种鉴权失效、结构异常、候选探测、播放地址（接口解析 / 已带 URL / 相对路径）、
请求记录脱敏。

测试 146 → **161**。

#### 2.7.5 第 6 轮修掉的 1 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 17 | `fetch_catalog(save=False)` 仍会把接口记录写进 `recon/network.jsonl` 并清空内存缓冲 | 调用方随后再 `dump_network_log()` 会拿到**空文件**，排障时以为「没有请求记录」（写验证脚本时命中） | 明确 `dump_network_log` 的语义是「写出并清空」；验证脚本改为读取 `recon/network.jsonl`。同时给 `EcnuClient` 补上接口记录的开关说明 |

---

### 2.12 第 5 轮交付（暂停闸门，保留索引）

#### 2.7.1 问题

一节课的录播可能 1~2 小时，下载 + 转写要跑十几分钟。原来的「暂停」只在**任务之间**生效 ——
用户点了暂停，却要等当前任务跑完（可能十几分钟）才有反应，等于没法用。

#### 2.7.2 方案：暂停闸门 `PauseGate`

新增 `src/ecnu_transcribe/pausegate.py`，在**天然断点**上响应暂停：

| 断点 | 位置 | 响应速度 |
| --- | --- | --- |
| 流水线各阶段之间 | `PipelineHooks.checkpoint()` | 立即 |
| 每个音频分段转写前 | `transcriber.transcribe_chunked()` | 立即 |
| 每次发起 ASR 请求前 | `transcriber._transcribe_one()` | 立即 |
| 重试退避期间 | `transcriber._transcribe_one()` | 立即 |
| 每个 LLM 处理块前 | `llm.TextPostProcessor` | 立即 |
| **ffmpeg 拉流过程中** | `downloader._download_once()` 的输出行回调 | 立即（**就地冻结**，恢复后继续下载而不重下） |

设计要点：

* **正在发出的那一次 ASR 请求不打断** —— 打断会白费已花的 ASR 额度；因此在分段边界停下。
* **取消优先于暂停**：暂停状态下点「停止」会立刻放行闸门，让取消检查接管并抛 `TaskCancelled`，
  不会卡死（有专门的回归测试与端到端验证）。
* 暂停期间每 10s 上报一次「已暂停 Ns」，恢复时上报「▶ 已恢复」，用户不会面对一片沉默。
* 未暂停时 `wait()` 是**无锁快速路径**（只做一次 `Event.is_set()`），不拖慢正常流程。

#### 2.7.3 端到端验证：`scripts/verify_pause.py`

在**真实运行的流水线**（本地加密 HLS / 本地语音合成素材 / 本地 ASR）中间暂停，实测 **9/9 通过**：

```
[2] 场景 A：运行中暂停 → 恢复 → 完成
    → 已请求暂停（t=0.7s，已收到 11 条进度事件）
  ✅ 暂停后进度不再前进（确实停在断点）  — 暂停后新事件 0 条，进度仍为 90.0%
  ✅ UI 收到暂停状态提示  — ⏸ 已暂停（分段边界，点「继续」恢复）
  ✅ 暂停期间任务未结束
    → 已恢复（暂停了 3.0s）
  ✅ 恢复后任务继续并完成  — stage=done
  ✅ 暂停/恢复未破坏产物（3 个文件）  — 暂停验证样例.txt, .srt, .md
  ✅ UI 收到恢复提示  — ▶ 已恢复
    实际暂停时长（闸门统计）：3.0s

[3] 场景 B：暂停期间点「停止」→ 立刻中断且落到 canceled
  ✅ 暂停中「停止」能在 30s 内退出  — 耗时 0.0s
  ✅ 任务状态落到 canceled
结果：9 项通过，0 项失败
```

#### 2.7.4 第 5 轮修掉的 3 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 14 | **本地文件当播放源时拉流失败** —— `build_input_args()` 无条件加 `-headers`/`-reconnect*` | 本地音频 / `file://` 源直接打不开（`Option headers not found`），写验证脚本时首次运行即命中 | 新增 `_is_local_source()`：本地路径与 `file://` 走**纯转码**路径（不带任何网络参数），文件不存在时给明确错误 |
| 15 | **用户取消被当成失败** —— 分片转写里取消抛的是 `TranscriptionError("转写被用户取消")` | 点「停止」后任务显示 **failed**，用户以为程序出错了（应该显示「已取消」） | 统一改为抛 `TaskCancelled`，状态机正确落到 **canceled** |
| 16 | **暂停期间仍会多打一次 ASR 请求** —— `_transcribe_one()` 重试循环入口没过闸门 | 白花一次 ASR 额度 | 每次真正发起请求前都过一遍闸门 |

#### 2.7.5 第 5 轮测试与文档

* 新增 `tests/test_pausegate.py`（18 个用例）：闸门语义、取消优先、超时放行、
  暂停时长统计、外部取消事件联动、流水线断点提示、转写器/下载器闸门接入。
* 测试 128 → **146**（全部离线，默认跳过 slow）。
* README「暂停/停止/断点续跑」小节重写；`docs/ARCHITECTURE.md` 模块表与限制章节更新。

---

### 2.13 第 4 轮交付（一键离线演示，保留索引）

`scripts\demo_offline.py` —— **不需要校园网、账号、任何 API Key**，一条命令看完整个产品：

```
合成模拟课堂语音（Windows 中文 SAPI）
   → 发布为本地 HLS 流（第 1 条 AES-128 加密、第 2 条明文）
   → AudioDownloader 真实 ffmpeg 拉流取音频
   → 本地 faster-whisper 语音识别
   → 输出 <课程名>/<标题>.{txt,srt,md}
   → 演示断点续跑（不重下 / 不重跑 ASR）
```

**实测输出**：

```
✅ 第1讲 绪论与算法复杂度  → DEMO-L01.wav  52.5s  [Windows 中文语音合成（SAPI）]
✅ 第2讲 线性表：顺序存储与链式存储  → DEMO-L02.wav  48.2s
✅ 《第1讲 绪论与算法复杂度》 AES-128 加密 → http://127.0.0.1:63012/hls/DEMO-L01/index.m3u8
✅ 《第2讲 线性表：顺序存储与链式存储》 明文   → .../DEMO-L02/index.m3u8
✅ 就绪：http://127.0.0.1:8334/v1（模型 faster-whisper-tiny）

    ✅ 《第1讲 绪论与算法复杂度》  状态=done  进度=100%
      • 数据结构与算法（离线演示）\第1讲 绪论与算法复杂度.txt  (407 bytes)
      • 数据结构与算法（离线演示）\第1讲 绪论与算法复杂度.srt  (541 bytes)
      • 数据结构与算法（离线演示）\第1讲 绪论与算法复杂度.md   (835 bytes)
    ✅ 《第2讲 线性表：顺序存储与链式存储》  状态=done  进度=100%
      • …txt (370 bytes)  …srt (538 bytes)  …md (849 bytes)

    ✅ 重跑后状态：done      ✅ 音频未重新下载：True
    ✅ ASR 调用次数：重跑前 0 → 重跑后 0（未重复调用 ✔）

完成：2/2 条任务成功，共 6 个产物文件
```

产物内容（真实识别结果，`tiny` 模型的错别字如实保留）：

```markdown
**[00:00:00.530]** 同学们好,今天我们讲数据结构的第一讲,训论与算法复杂读。
**[00:00:10.930]** 算法的时间复杂读用大欧记号表示,他描述的是书入规模区于无穷时运行时间的增长量级。
**[00:00:24.630]** 常見的時間複雜度從遞到高依次使,常數階,對數階,現性階,現性對數階,平方階和指數階。
**[00:00:40.630]** 舉個例子,二分查找的時間複雜度是對數階,而規並排序是現性對數階。
```

> 时间轴定位准确（4 句话落在 0.5s / 10.9s / 24.6s / 40.6s），
> 错别字（训论→绪论、书入→输入、大欧→大O）正是 DeepSeek 后处理的目标。

#### 2.8.1 第 4 轮修掉的 2 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 12 | **本机 ASR 端点被硬要求填 API Key** —— 项目自带的本地 ASR 服务不校验鉴权，但 `OpenAICompatibleTranscriber.__init__` 里 `if not api_key: raise` | 「零成本离线转写」这条路径**根本走不通**（离线演示首次运行直接 `failed`） | 本机端点（`localhost`/`127.0.0.1`/`::1`/`0.0.0.0`）允许空 Key 且不发 `Authorization` 头；远程端点仍强制要求，并在报错里教用户改写 Base URL。`probe_asr_endpoint()` 同步修正 |
| 13 | **离线演示的占位转写取不到文本** —— 音频文件名形如 `<安全标题>__<resource_id>.<ext>`，而占位转写器按文件 stem 查表 | 演示里三产物只有一行「占位转写」 | 改为按 `__<id>` 后缀解析 resource_id |

顺带加了防「`127.0.0.1.evil.com` / `localhost.evil.com` 被误判为本机」的安全测试。

#### 2.8.2 第 4 轮测试与文档

* 新增 `tests/test_asr_endpoint_config.py`（14 个用例）：本机端点空 Key、远程端点强制 Key、
  相似域名不误判、工厂函数在两种配置下的行为、自检在空 Key/连不上时的可读报错。
* 测试 114 → **128**（全部离线，默认跳过 slow）。
* README 顶部新增「想先看看效果？一条命令」小节 + 命令清单加第 ⑧ 项。

---

### 2.14 第 3 轮交付（本地 ASR 服务，保留索引）

`scripts\local_asr_server.py` —— 把 `faster-whisper` 包成 **OpenAI 兼容** HTTP 服务：

```
POST /v1/audio/transcriptions     （与云端同形，支持 verbose_json / srt / text）
GET  /v1/models
GET  /health
```

应用侧无需任何改动：设置 → 语音识别 → 类型「OpenAI 兼容端点」，
`Base URL=http://127.0.0.1:8000/v1`、`模型=faster-whisper-small`、Key 留空。
**音频全程不出本机。**

**真实语音端到端验证**（用 Windows 中文语音合成生成 19.4s 课堂风格语音，
再走应用自己的 `OpenAICompatibleTranscriber`）：

| 模型 | 用时 | 实时倍率 | 参考二元组覆盖率 |
| --- | --- | --- | --- |
| `tiny` | 2.1s | 9.3× | 15% |
| `small` | 8.9s | 2.2× | 30% |

```
$ python scripts\local_asr_server.py --model tiny --port 8417 --device cpu
$ python -c " 走 OpenAICompatibleTranscriber 转写 build/asr/speech_zh.wav "
OK: 7 段 / 66 字 / 音频 19.4s
  [ 0.00- 3.28] 今天我們來講二差數的便利
  [ 3.28- 6.12] 前續便利的順序是跟傑典
  [ 6.12- 8.52] 左指數 右指數
  [ 8.52-11.40] 中序便利的順序是左指數
时间轴单调: True
```

并且完整跑通三产物：

```
build\asr\output\演示课程\演示：二叉树遍历（本地 ASR 端到端）.txt  (193 bytes)
build\asr\output\演示课程\演示：二叉树遍历（本地 ASR 端到端）.srt  (429 bytes)
build\asr\output\演示课程\演示：二叉树遍历（本地 ASR 端到端）.md   (810 bytes)
```

`.md` 元信息正确（课程/讲师/时长 `00:19`/模型 `faster-whisper-tiny`/字数 66），
`.srt` 时间轴格式正确。

> `tiny`/`small` 的中文错别字（「二叉树」→「二叉數」、「根节点」→「跟傑典」）是模型能力问题，
> **正是 DeepSeek 后处理要修的**；课堂中文场景推荐 DashScope `paraformer-v2`。README 已写明。

#### 2.9.1 第 3 轮修掉的 3 个真实缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 9 | **本地 ASR 的 multipart 解析对整段 body 做裸 `split(boundary)`** | 音频是二进制、可能恰好含 boundary 字节序列 → 文件被切碎 → 服务端 500。**首次实测就命中** | 改为只在**行首**识别分隔符（前有 `\r\n`、后跟 `\r\n`/`--`），并新增 1MB 二进制往返回归测试 |
| 10 | **`device="auto"` 在 CPU-only ctranslate2 上仍选 CUDA** | 报 `Library cublas64_12.dll is not found`，且错误发生在**推理阶段**——服务能起、健康检查 OK、一转写就 500 | 改为**真实跑一次 GPU 前向自检**（而不是只看 `get_cuda_device_count()`），不通过就回退 CPU+int8 |
| 11 | **永久性 ASR 错误也会退避重试 4 次** | 上面那个 DLL 错误导致客户端白等 ~460s（实测 10 分钟超时） | 新增 `_PERMANENT_HINTS` 分类：配置/环境类错误（缺 DLL、模型名不存在、连接被拒、4xx 参数错）**立即失败**并给建议；429/5xx 仍正常退避重试 |

（另外：请求体里的错误消息现在会参与判定，避免把「服务端 500 但根因是环境问题」当成临时故障。）

#### 2.9.2 新增文档：`docs/ARCHITECTURE.md`（M6 面试级总结）

包含：文字版全局架构图、15 个模块的职责表、清单流与单任务流的完整数据流、
**12 条关键设计决策及取舍理由**（含"为什么不用固定迁移表"「为什么 LLM 只改文本不改时间轴」
「为什么自造加密 HLS 做集成测试」等）、7 条已知限制、10 项后续演进、6 层测试策略。

#### 2.9.3 测试与文档更新

* 新增 `tests/test_local_asr_server.py`（15 个用例）：multipart 边界攻击回归、
  大二进制往返、SRT 生成、设备选择、CUDA 自检不抛异常。
* 新增 `pytest.ini`：默认跳过 `slow` 标记（CUDA 自检会真的加载模型）；
  跑全部用 `pytest -m ""`。
* 新增 `requirements-optional.txt`（本地 ASR 可选依赖，含踩坑说明）。
* README 新增「方案 A：本地 ASR（零成本）」完整步骤 + 实测精度表 + 对应的常见错误条目。

---

### 2.15 第 2 轮交付（打包与接口自愈，保留索引）

#### 2.10.1 打包交付（M5）✅
```powershell
.venv\Scripts\python -m PyInstaller packaging\ecnu_transcribe.spec --noconfirm --clean
```

* 产物：`dist\大夏学堂转写助手\大夏学堂转写助手.exe`（5.5 MB）+ `_internal\`（合计 461 MB）
* **内嵌 ffmpeg 8.1.1 与 ffprobe**（`_internal\ffmpeg.exe` / `_internal\ffprobe.exe`），
  目标机器无需另装。
* `packaging\ecnu_transcribe.spec` 同时打包 `scripts/`，因此 exe 自带两个自检入口：
  * `大夏学堂转写助手.exe --doctor` → 冻结态自检
  * `大夏学堂转写助手.exe --login`  → 命令行方式完成一次人工登录
  * `大夏学堂转写助手.exe --selftest` → GUI 冒烟（2 秒后自动退出）

#### 2.10.2 打包实测证据

```
> dist\大夏学堂转写助手\大夏学堂转写助手.exe --doctor
[1] 路径解析
  ✅ 运行在打包产物中（frozen）  — ...\dist\大夏学堂转写助手
  ✅ app_root 可写（output 已创建）
  ✅ cache/logs/data 目录就绪
[2] 内嵌 ffmpeg / ffprobe
  ✅ 找到 ffmpeg  — ...\_internal\ffmpeg.exe
      ffmpeg version 8.1.1-essentials_build-www.gyan.dev
  ✅ 找到 ffprobe（更精确的时长探测）
[2b] 真实跑一次转码（生成 2s 音频 → 抽 mp3 → 探测时长）
  ✅ ffmpeg 生成测试音频  — 64078 bytes
  ✅ ffmpeg 抽取音频（-vn -ac 1 -ar 16000 -c:a libmp3lame）  — 17037 bytes
  ✅ 产出音频时长可探测  — 2.00s
[3] 凭据加密（Windows DPAPI）  ✅ DPAPI 可用
[4] sqlite 状态库
  ✅ 写入并读回任务        ✅ 产物列表正确落库  — ['a.txt', 'a.srt', 'a.md']
[5] Playwright Chromium（登录环节需要；打包版不内嵌浏览器）  ✅ Chromium 可启动
[6] 脱敏与配置  ✅ Cookie/token/学号 脱敏  ✅ 配置可读写
结果：16 项通过，0 项失败，0 项提醒
```

```
> dist\大夏学堂转写助手\大夏学堂转写助手.exe --selftest
exit code: 0
日志（冻结态）：
  frozen = True
  app_root = ...\dist\大夏学堂转写助手
  output_dir / media_cache_dir / state_db 均在 exe 同级目录
  user_data_dir = C:\Users\Administrator\AppData\Local\ecnu-transcribe
```

#### 2.10.3 第 2 轮修掉的两个打包缺陷

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| 7 | **Playwright 在冻结态把浏览器目录算到包内** —— 报 `Executable doesn't exist at _internal\playwright\driver\package\.local-browsers\chromium_headless_shell-1148` | 打包版点「登录」直接失败，等于交付物不可用 | 启动时自动把 `PLAYWRIGHT_BROWSERS_PATH` 指向 `%LOCALAPPDATA%\ms-playwright`（先探测该目录下确有 `chromium*` 才设置）；`--doctor` 增加该项检查与安装指引 |
| 8 | 抓包记录只对**值**做正则脱敏 | `Authorization: Bearer xxx` / `X-Api-Key` 这类「值本身不含敏感词」的头会原文落盘 | 新增 `redact_headers()`：按**键名**判定（`authorization/cookie/x-api-key/x-token/...`）整段替换 |

#### 2.10.4 第 2 轮新增能力（浏览器嗅探 / 登录诊断 / doctor）

* **浏览器接口嗅探兜底**（`login.sniff_api_paths` + `EcnuClient._sniff_endpoints`）：
  当所有候选接口路径都失败时，自动借已登录的持久化浏览器**真实打开一次页面**，
  用 CDP 观察平台自己调用了哪些 XHR/Fetch JSON 接口，按启发式分类为
  课程/资源/播放三类路径，落盘 `recon/sniffed_endpoints.json`，然后用真实路径重试。
  只嗅探 `*.ecnu.edu.cn`，每次运行最多一次，失败即回退候选路径。
  —— 这是「接口变更检测」从「只报错」升级为「自动纠正」。
* **登录卡住的诊断**：超时不再只报「超时」，而是按最后停留页面给出可照做的处理
  （停在 SSO → 完成认证；被 `vpn_key` 拦下 → 先连校园网/VPN）。
* **`scripts/doctor.py`**：环境/产物自检（路径、ffmpeg 真实转码、
  DPAPI、状态库读写、Chromium、脱敏、配置读写；第 22 轮又加了**产物编码字节头**，
  冻结态共 20 项）。
* 新增 7 个安全回归测试（DPAPI 往返、清空凭据、日志端到端脱敏、
  抓包按头名脱敏、`SessionState.summary()` 不泄漏值）。

#### 2.10.5 已实测的登录可达性

```powershell
# 用 LoginSession 真实启动 Chromium 并导航到入口（不登录，仅验证链路）
.venv\Scripts\python -c "..."   # 见 §4.4
# 输出：
#   PLAYWRIGHT_BROWSERS_PATH = C:\Users\Administrator\AppData\Local\ms-playwright
#   current url: https://sso.ecnu.edu.cn/login?service=https%3A%2F%2Fsso.ecnu.edu.cn%2Foauth2.0%2FcallbackAuthorize%3Fclient_id%3Dd46ba84...
#   title: 统一身份认证平台
```

**结论**：`入口 → webVPN 网关 → OAuth2 → 统一身份认证平台` 这条链路在浏览器里是通的，
页面确实渲染到了「统一身份认证平台」登录页。剩下的是「在页面里完成认证」这一步（需要你本人）。

---

### 2.16 第 1 轮交付（核心库 + GUI，保留索引）

```
src/ecnu_transcribe/    核心库
    paths.py      开发态/冻结态路径解析（output / cache / data / logs / %LOCALAPPDATA%）
    logbus.py     统一日志总线 + 脱敏（Cookie/token/api_key/手机号/身份证/学号）
    errors.py     异常体系（AuthExpiredError / ApiChangedError / DrmDetectedError / …）
    config.py     配置 + DPAPI 加密凭据（secrets.json 里只有密文）
    store.py      sqlite3 状态库（tasks/artifacts/events）+ 状态机 + 断点续跑
    catalog.py    清单模型（宽容字段解析 + 严格校验）
    media.py      唯一的 ffmpeg 出口（探测/取音频/静音检测/切分/DRM 检测/sha256）
    client.py     平台 HTTP 客户端（登录态复用、翻页、业务码判定、可达性诊断）
    login.py      Playwright 人工登录 + 抓包（脱敏）+ storage_state 落盘 + 浏览器代打桥
    downloader.py ffmpeg 拉流取音频（HLS/AES-128/重试/缓存/并发≤2/抖动）
    transcriber.py ASR 抽象 + DashScope（兼容 & 原生异步）+ OpenAI 兼容 + faster-whisper
    llm.py        DeepSeek 文本后处理（纠错/分段/摘要），失败不阻塞产物
    exporter.py   txt / srt / md 写出（只增不改，覆盖前自动 .bak-时间戳）
    pipeline.py   下载→转写→后处理→写出的编排 + 断点续跑
src/app/               PySide6 界面
    main.py        入口（字体/日志/状态库恢复/异常兜底/AI --selftest）
    workers.py     LoginWorker / CatalogWorker / PipelineWorker / ProbeWorker（QThread）
    ui/main_window.py     三栏主窗口 + 顶部操作条 + 登录引导对话框
    ui/settings_dialog.py 6 个设置标签页 + 连通性自检
scripts/               login.py / fetch_catalog.py / run_one.py / selftest_e2e.py
tests/                 95 个 pytest 用例（全部离线，fixture 驱动）
main.py                源码入口
requirements.txt       版本锁定
.gitignore             已含 recon/ 与 storage_state.json
docs/API.md            接口文档（当前为「实测网络路径 + 待抓包填充」状态）
```

### 2.17 关键设计决定（相对原始要求的偏离/补充，均有理由）

1. **只留文本，不留音视频**（按你本轮答复）：下载仍会取音频用于 ASR，但默认只缓存在
   `cache/media/`，产物只有 `.txt/.srt/.md`；设置页另有「保留音频副本 / 保留视频」开关（默认关）。
2. **ASR 选型 = 阿里云百炼 DashScope**（按你本轮答复）：默认
   `base_url=https://dashscope.aliyuncs.com/compatible-mode/v1`、`model=paraformer-v2`；
   同时保留「原生异步 API」开关（大文件更稳）与 OpenAI 兼容 / 本地 faster-whisper 兜底。
3. **登录 = 一次性人工登录 + 持久化复用**（按你本轮答复）：可见 Chromium +
   `%LOCALAPPDATA%\ecnu-transcribe\browser` 持久化目录 + `storage_state.json`；
   **不识别验证码、不绕过任何验证**。
4. **状态机改为「前进 + 出口态」模型**：任意阶段可到 `failed/canceled/pending`，
   其余要求前进，另加少量显式允许的后退（重跑/续跑）。比固定迁移表更耐改，且仍能挡住
   `done → downloading` 这类乱序回写。
5. **脱敏做了两层**：正则规则（Cookie/Authorization/带引号敏感键/无歧义
   `key=value`/URL query）+ **整段数字按长度判定**（手机号 11 位、身份证 18 位含 X、
   学号 10/11 位且以 `10` 开头）+ 运行期登记的字面量密钥。学号在加载配置时自动登记。
6. **`extract_list` 改为有界深度优先搜索**：平台包装层数不固定
   （`data.rows` / `result.pageData.list` / `data.data.records`），固定层级会漏。

---

## 8. M0 侦察：已实测到的网络事实（可复现）

### 4.1 证据

```powershell
# 1) DNS：本机 Clash(fake-IP) 把它解析到 198.18.x.x；公共 DoH 得到真实地址
python - <<'PY'
import httpx
print(httpx.get("https://223.5.5.5/resolve?name=courses.ecnu.edu.cn&type=A", verify=False, trust_env=False).text)
PY
# → {"Answer":[{"name":"courses.ecnu.edu.cn","type":1,"data":"202.120.88.100"}]}

# 2) 直连（绕开 Clash fake-IP）会跳到 webVPN 网关 —— 这是校外访问的正常第一跳
#    GET https://courses.ecnu.edu.cn/jy-application-resourcemanage-ui/
#    → 302 https://proxy.ecnu.edu.cn:443/vpn_key/update?
#         origin=https%3A%2F%2Fcourses.ecnu.edu.cn%2F...&reason=site+courses.ecnu.edu.cn+not+found
#    Set-Cookie: SERVERID2=Server1
#    （第 22 轮更正：早先把它读成「被封」，错。见 §4.2 与 docs/API.md §0.2.1）

# 3) 网关自身可达，且指向学校 OAuth2
#    GET https://proxy.ecnu.edu.cn/users/sign_in
#    → 302 https://api.ecnu.edu.cn/oauth2/authorize?scope=ECNU-Basic&redirect_uri=https://proxy.ecnu.edu.cn/ecnu_oauth2
#         &response_type=code&client_id=d46ba84ffc58611f&state=...
#    GET https://api.ecnu.edu.cn/oauth2/authorize?...  → 301 https://sso.ecnu.edu.cn/oauth2.0/authorize?...

# 4) 真实浏览器里的最后一跳：统一身份认证登录页**真的渲染出来了**（2026-09-12 实测）
#    GET https://sso.ecnu.edu.cn/login?service=...  → 200
#    随后 10 分钟内**零新请求** ⇒ 结论是「没人提交凭据」，不是网络问题

# 5) 学校 SSL-VPN 门户存活
#    GET https://vpn.ecnu.edu.cn/portal/  → 200（14KB 门户页）
```

### 4.2 结论

| 事实 | 判定 |
| --- | --- |
| 站点是 SPA（`#/` 路由） | **证实**（入口路径本身就是 `jy-application-resourcemanage-ui` 前端工程） |
| 登录走统一身份认证 | **证实**：`proxy.ecnu.edu.cn → api.ecnu.edu.cn/oauth2 → sso.ecnu.edu.cn` 三级 OAuth2 |
| 校外直连会跳到 webVPN 网关 | **证实**：`302 → proxy.ecnu.edu.cn/vpn_key/update` |
| ~~校外直连「被拦下 / 不可达」~~ | **❌ 第 22 轮更正**：这条 302 是**正常入口**；浏览器继续走完 OAuth 即到认证页（200）。只有页面正文写着 `site not found` 时才是真拒绝 |
| 认证页能正常打开、链路可达 | **证实**：第 ⑥ 跳 HTTP 200 + 登录页全部静态资源/配置接口 200 |
| 校外必须先连 SSL-VPN | **❌ 更正为「不必」**：认证页可达即可登录；SSL-VPN 只在网关真拒绝时才是必需 |
| 视频是 HLS/AES-128 或 mp4 | **待真实抓包**（本地已按 HLS+AES-128 做好并验证） |
| 播放地址带时效签名 | **待真实抓包** |
| 统一认证是否有验证码/二次验证 | **待真实登录观察**（不做绕过） |

### 4.3 已按降级预案写进代码的能力

* `AuthExpiredError`：登录态失效显式抛出，**绝不静默返回空清单**（测试覆盖）。
* `ApiChangedError`：接口结构变更显式失败并给出响应样本（测试覆盖）。
* `DrmDetectedError`：命中 Widevine/PlayReady/FairPlay/SAMPLE-AES 立即停止（端到端自检覆盖）。
* `SiteUnreachableError`：网关**明确拒绝**（页面报站点不存在/无权限）时给出人话提示
  （连 SSL-VPN 或换校园网）；正常跳转**不再**被误报为不可达（第 22 轮修正）。
* `LoginSession.probe_api_in_page` + `sniff_api_paths`：动态签名场景的「借已登录页面上下文代打」桥，
  以及候选路径全部失败时**自动嗅探真实接口**（第 2 轮新增，见 §2.4）。
* 限流预案：并发硬上限 2、请求间隔 300–800ms 抖动、可配限速、指数退避重试。

---

## 9. 可复现的证据命令与输出

### 5.1 依赖与自检

```powershell
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt
.venv\Scripts\python -m playwright install chromium
.venv\Scripts\python -m pip install -r requirements-optional.txt   # 可选：本地 ASR
```

```powershell
# ⭐ 一键离线演示：不需要网络/账号/任何 API Key，直接跑出 6 个产物
.venv\Scripts\python scripts\demo_offline.py
# → 完成：2/2 条任务成功，共 6 个产物文件
#   含：真实中文语音 → AES-128 加密 HLS 拉流 → 本地语音识别 → txt/srt/md + 断点续跑验证
```

```powershell
# 单元测试（离线，不需要网络）
.venv\Scripts\python -m pytest
# → 317 passed, 1 deselected
```

```powershell
# 产物编码（第 22 轮新增）：断言三产物字节头 = EF BB BF，且 .transcript.json 不带 BOM
.venv\Scripts\python -m pytest tests\test_exporter.py -q -k bom
# → 5 passed

# 冻结态里真跑一次导出并验字节头（打包产物专用）
"dist\大夏学堂转写助手\大夏学堂转写助手.exe" --doctor
# → [7] 产物编码（真实导出一次，验字节头）
#     ✅ 三产物均带 UTF-8 BOM  — .md=EF BB BF / .srt=EF BB BF / .txt=EF BB BF
#     ✅ transcript.json 不带 BOM
#     结果：20 项通过，0 项失败，0 项提醒

# 反向证据：同样的正文按 cp936 读会失败/变样（这就是加 BOM 的原因）
#   nobom.txt  → UnicodeDecodeError: 'gbk' codec can't decode byte 0xad
#   bom.txt    → 同学们好，今
```

```powershell
# 离线端到端：真实 ffmpeg 拉 AES-128 加密 HLS → 取音频 → 假 ASR → 三产物 → 断点续跑
.venv\Scripts\python scripts\selftest_e2e.py
# → 结果：22 项通过，0 项失败   （日志见 build/e2e_result.txt）

# 暂停功能端到端验证：真实流水线中途暂停 → 恢复 → 完成；暂停中停止 → 立刻取消
.venv\Scripts\python scripts\verify_pause.py
# → 结果：9 项通过，0 项失败

# 本地 ASR：真实中文语音 → 带时间轴文本 → txt/srt/md（不需要任何 API Key）
.venv\Scripts\python scripts\local_asr_server.py --model tiny --port 8417 --device cpu
.venv\Scripts\python -c " 走 OpenAICompatibleTranscriber 转写 build/asr/speech_zh.wav "
# → OK: 7 段 / 66 字 / 音频 19.4s，时间轴单调 True
```

关键输出节选：

```
[2] AudioDownloader 拉取加密 HLS（AES-128）
  ✅ 产出音频文件  — 第1讲 自检样例（加密HLS）__E2E-HLS-1.mp3
  ✅ 只保留音频（mp3）
  ✅ 时长与源一致（误差 < 2%）  — 源 24.00s / 产出 24.06s（偏差 0.27%）
  ✅ 记录了 sha256  — 0c27ffff6ec4dc7f…
[3] 缓存命中（断点续跑：不重下）
  ✅ 第二次命中缓存        ✅ sha256 与首次一致
[4] DRM 检测（不做绕过）
  ✅ DRM 被拦下并给出证据  — 检测到 DRM 保护（widevine） @ http://127.0.0.1:53750/index.m3u8…
[5] Pipeline：pending → done，产出 txt/srt/md
  ✅ 产出 3 个文件  — …txt, …srt, …md
  ✅ SRT 含时间轴  — 00:00:00,000 --> 00:00:12,032
  ✅ ASR 被调用了一次  — calls=1
[6] 断点续跑（第二次不重下、不重跑 ASR）
  ✅ 音频未被重新下载      ✅ ASR 未被重复调用（复用 transcript.json）
```

### 5.2 GUI 冒烟（无显示器环境）

```powershell
$env:QT_QPA_PLATFORM='offscreen'
.venv\Scripts\python main.py --selftest
# → 正常启动、打印路径、2 秒后自动退出（exit code 0）
```

### 5.3 打包产物验证

```powershell
.venv\Scripts\python -m PyInstaller packaging\ecnu_transcribe.spec --noconfirm
dist\大夏学堂转写助手\大夏学堂转写助手.exe --doctor     # → 20 项通过，0 项失败（含产物编码字节头）
.venv\Scripts\python scripts\verify_dist.py            # → 20 项通过，0 项失败（干净环境）
```

### 5.4 登录链路可达性

```powershell
# 用 LoginSession 真实启动 Chromium 并导航到入口（不登录，仅验证链路）
# 输出（节选）：
#   PLAYWRIGHT_BROWSERS_PATH = C:\Users\Administrator\AppData\Local\ms-playwright
#   profile dir: C:\Users\Administrator\AppData\Local\ecnu-transcribe\browser
#   [status] 启动 Chromium（用户目录：...）…
#   [status] 打开入口：https://courses.ecnu.edu.cn/jy-application-resourcemanage-ui/#/home
#   current url: https://sso.ecnu.edu.cn/login?service=https%3A%2F%2Fsso.ecnu.edu.cn%2Foauth2.0%2FcallbackAuthorize%3Fclient_id%3Dd46ba84...
#   title: 统一身份认证平台
#   browser launched OK; pages: 1
```

### 5.5 自检与真机实测暴露并修掉的 54 个真实缺陷（累计）

| # | 缺陷 | 影响 | 修复 |
| --- | --- | --- | --- |
| **53** | **`force=True` 时半份缓存被白扔** —— `fetch(force=True)`（GUI「强制重跑」）跳过缓存判定，把上次中断留下的半份音频直接覆盖重下 | 实测那半份是 1900s/3301s ⇒ 白扔 16 分钟下载时间 | 把「残件降级为断点基准」提到 `fetch()` 开头，**与 force 无关**；新增用例钉住 |
| **52** | **并发写日志导致轮转失败并丢日志** —— 两个进程写同一 `logs/app.log`，2MB 轮转时 `os.rename` 撞上对方句柄（`WinError 32`） | logging 打印一屏 `--- Logging error ---` 并**丢掉触发轮转的那条记录**；桌面应用被打开两次就会遇到 | `_MultiProcessSafeRotatingHandler`：轮转失败则本轮不轮转、继续追加，提示仅一次；2 个用例钉住「一条不丢 + 不冒堆栈」 |
| **50** | **被中断的半份音频被当成「完成」** —— `run_ffmpeg(check=False)` 从不看退出码；时长比清单少 42.4% 只写 `log.warning`；接着照常转写 | 用户拿到**残缺却毫无标记**的转写稿（实测 1900.5s/3301.0s = 57%），比直接失败糟得多 | `check=True`（先验产物再判死）+ **截断裁决**（<95% 即 `StreamTruncatedError`），残件转存 `.partial` 并重试；顺带补 `-ss` 断点续传 + concat 拼接 |
| **51** | **续跑盲目信任阶段标记** —— 任务记为 `audio_ready`，续跑时只看「文件存在且 > 1024 字节」就 `复用已有音频（不重下）` | 缺陷 50 留下的半份音频**又被送去转写**；`audio_ready → probing` 非法迁移还会**连带丢掉 play_url/duration 的刷新** | 复用前用 `ffprobe` 量实际时长与清单对比（差 >5% 即重下）；阶段靠后时保持当前阶段只更新字段 |
| **49** | **拉流停滞没有看门狗**（潜在：未在真实环境触发，已合成复现）—— 网络挂住时 ffmpeg 安静等待、主线程阻塞在读循环上，回调里的任何检查都不会执行 | 任务永远挂着一个不动的百分比，用户无法判断该等还是该停 | 独立线程看门狗 `run_ffmpeg(watchdog=…)`：窗口内等效倍速 < 0.25× 即中止并重试；暂停/收尾不误杀；由 `test_watchdog_actually_kills_hung_ffmpeg` 证明 |
| **48** | **登录窗口在屏幕外** —— Chromium 主窗口矩形 `(-25600,-25600) 159x27`（最小化哨兵坐标），而 JS 报 `x=60` | 用户说「我已经登录了」，程序说「登录态已过期」，**两句话都是真的**；用户改用 Edge 登录（cookie 库独占锁定，读不出来） | OS 层 `MoveWindow` 移回屏幕内（**不置顶**，用户明确要求过）+ `scripts/login_via_cdp.py`：普通 Chrome 窗口登录 + CDP 导出会话 |
| **47** | 播放地址是**带时限签名**的（`auth_key=…`），签名过期后仍拿死 URL 退避重试 4 次 | 白等几十秒后报「音频获取失败（重试 4 次）」，用户既不知道重试什么也不知道能做什么 | 新增 `PlayUrlExpiredError`：401/403/签名过期/404 一律快速失败，由流水线**重新解析新鲜地址** |
| **46** | 分段 ASR 结果只在内存里，成功后连切片都删 | 55 分钟课程中断一次就得**把付过费的分段全部重来** | 分段结果落盘 `cache/segments/<音频哈希>-<方案哈希>/result_NNN.json`，按索引/时间/模型/provider 校验后续跑复用 |
| **45** | 近乎无声的课堂音轨被自动增益后送 ASR | Whisper 会**编造文字**（实测出现「字幕by索兰娅」），交出一份看起来像转写、其实是幻觉的产物 | 用**均值**判静音（-45 dB）⇒ 不增益、跳过 ASR 并如实说明「是录像没录到声音，原因不在你的配置」；`asr_allow_silent` 作逃生开关 |
| **44** | 连通性报错太笼统（只有「无法连接」） | Clash 全局代理把校园网域名也代理走（香港出口）导致网关重置，用户完全不知道是自己代理的问题 | 三分类诊断（重置/DNS/超时）+ 可操作建议（含 `DOMAIN-SUFFIX,ecnu.edu.cn,DIRECT`） |
| **43** | 录播列表字段名猜错（`courseVodVideoDtoList` vs 真实 `courseVodViewList`） | 只有靠候选探测兜底才没出事；一旦兜底路径失效就是空清单 | 用**实测抓包**锁死真实字段名，并让模拟平台也用真名（否则测试永远发现不了） |
| **42** | 课堂录音电平极低（实测均值 -57 dB，正常语音 -20~-30 dB） | 本地 ASR 的 VAD 把整段判成「无语音」→ 空转写，报错还误导是用户配置问题 | 引入电平检测 + 固定增益（实测优于 `loudnorm`/`dynaudnorm`）+ 如实的空转写归因 |
| **41** | `LoginSession` 只会**保存**登录态、从不**载入** | 重启后 Chromium 丢掉会话 cookie，表现为「明明登录过却要重登」 | 启动时 `restore_storage_state()` 回灌 cookies + localStorage |

| 1 | `update_stage` 用**列名**校验额外字段，`outputs`/`meta` 被静默丢弃 | 任务完成后「产物列表」永远为空，GUI 点「打开输出目录」也拿不到 | 同时接受 dataclass 字段名与列名，未知字段改为显式告警 |
| 2 | `redact()` **不读取**已登记的密钥集合 | 运行期登记的 API Key 可能被写进日志 | 在 `redact()` 内合并 `_EXTRA_SECRETS` |
| 3 | 脱敏正则把 `storage_state = <路径>` 里的 `state` 当敏感键 | 正常日志被误遮 | 无引号形式仅匹配无歧义键名，且要求 `=` 两侧无空格 |
| 4 | 数字标识识别依赖 `(?!\d)` lookahead，本机解释器行为不一致 | 手机号/身份证/学号可能漏遮 | 改为按整段数字长度判定 + 身份证尾随 `X` 处理 |
| 5 | `plan_chunks` 收尾产生几秒的碎片段 | 多花一次 ASR 调用 | 尾巴过短时并入上一段 |
| 6 | DRM 预检在「命中音频缓存」时被跳过 | 缓存命中会绕过 DRM 判定 | 预检在缓存判定之前生效，并给 `fetch()` 增加 `force` 开关 |
| 7 | Playwright 在冻结态把浏览器目录算到包内 | **打包版点「登录」直接失败** | 启动时自动设 `PLAYWRIGHT_BROWSERS_PATH` 指向 `%LOCALAPPDATA%\ms-playwright` |
| 8 | 抓包只对**值**做正则脱敏 | `Authorization: Bearer xxx` 这类头会原文落盘 | 新增 `redact_headers()`，按**键名**判定整段替换 |
| 9 | 本地 ASR 的 multipart 解析对整段 body 做裸 `split(boundary)` | 音频二进制里可能恰好含 boundary 序列 → 文件被切碎 → 500（**首次实测即命中**） | 只在**行首**识别分隔符；新增 1MB 二进制往返回归测试 |
| 10 | `device="auto"` 在 CPU-only ctranslate2 上仍选 CUDA | `cublas64_12.dll not found` 且错误发生在**推理阶段**：服务能起、健康检查 OK、一转写就 500 | 改为**真实跑一次 GPU 前向自检**，不通过回退 CPU+int8 |
| 11 | 永久性 ASR 错误也会退避重试 4 次 | 实测导致客户端白等 ~460s（触发 10 分钟超时） | 新增永久错误分类（缺 DLL / 模型名不存在 / 连接被拒 / 4xx 参数错）**立即失败**并给建议；429/5xx 仍正常退避 |
| 12 | 本机 ASR 端点被硬要求填 API Key | 「零成本离线转写」路径**根本走不通**（离线演示首次运行直接 failed） | 本机端点（`localhost`/`127.0.0.1`/`::1`/`0.0.0.0`）允许空 Key 且不发 `Authorization`；远程端点仍强制要求；并加防「`127.0.0.1.evil.com` 误判为本机」测试 |
| 13 | 离线演示占位转写按文件 stem 查表，而音频名是 `<标题>__<id>.<ext>` | 演示里只有一行「占位转写」 | 改为按 `__<id>` 后缀解析 resource_id |
| 14 | 本地文件当播放源时拉流失败（无条件加 `-headers`/`-reconnect`） | 本地音频/`file://` 源直接打不开（`Option headers not found`） | 新增 `_is_local_source()`，本地源走纯转码路径 |
| 15 | 用户取消被当成失败（转写里抛 `TranscriptionError`） | 点「停止」后任务显示 **failed**，用户以为出错了 | 统一抛 `TaskCancelled`，状态正确落到 **canceled** |
| 16 | 暂停期间仍会多打一次 ASR 请求（重试循环入口没过闸门） | 白花一次 ASR 额度 | 每次发起请求前都过一遍闸门 |
| 17 | `fetch_catalog()` 内部已写并清空接口记录，调用方再 dump 得到空文件 | 排障时以为「没有请求记录」 | 明确「写出并清空」语义；验证脚本改读 `recon/network.jsonl` |
| 18 | offscreen 环境下**模态 `QMessageBox` 永久阻塞**（任务成功的分支会弹「完成」框） | 自动化验证脚本卡死（本次实测挂起 10 分钟）；真实使用不受影响 | `verify_gui.py` 把 `QMessageBox.*` 替换成记录器，既防卡死又能断言弹窗内容 |
| 19 | 验证脚本里模拟平台返回的时长（2712s 等真实课程时长）与本地 20s 样例素材不符 | 触发「时长偏差 > 2%」告警，属测试噪声、干扰判读 | 验证脚本把选中条目的时长对齐素材真实时长 |
| **20** | **`merge_segments` 的偏移被叠加两次** —— `_transcribe_one(offset=chunk.start)` 已返回全局时间戳，调用方又把 `chunk.start` 当作 offset 传给 merge | **长课程字幕时间轴整体错位**：5 段 20s 音频的末段被算到 32~36s，字幕完全不可用。只有会被切分的长音频触发，人工极难发现 | `results.append((0.0, segs))`；并在 `merge_segments` docstring 写清 offset 语义 |
| 21 | 分段切片缓存 key 只含音频哈希，改了切分方案后会复用旧切片 | 内容按旧方案切、时间戳按新方案平移 → 错位字幕，日志毫无异常 | key 改为 `<音频哈希>-<切分方案哈希>`，并给 `_segment_usable()` 加时长校验 |
| 22 | 片段/合并结果可能超出音频总长（ffmpeg 切片略超 + 端点按实际解码长度报时间戳） | 产生永远播不到的字幕 | 新增 `_clamp_to_range()`，分段内夹取 + 合并后整体夹取 |
| **23** | **重新分段时「解析失败」被当成「不需要换段」** —— `_parse_break_list()` 失败返回空集合，调用方理解为「模型说没有断点」 | **整篇被合并成一段**（10 段 → 1 段），时间轴信息被悄悄抹掉。因为文字还在，肉眼几乎看不出，但 `.srt` 退化成一条覆盖整节课的字幕 | 区分「解析失败」（返回 `None`，保持原分段）与「明确不换段」（空集合）；顺带修掉 `{"break_after": []}` 因 `or` 短路被误判 |
| 24 | 修复类请求把「输出格式示例」放在**用户消息**里 | 模型偶尔回显示例，示例里的 `i=0` 与真实数据撞车 —— 看起来调用成功，实际只改了第 0 条甚至没改 | 用户消息**只放数据**，格式说明全部移到 system prompt |
| 25 | 启动定时回调（`QTimer.singleShot(200ms)`）可能在窗口关闭后才触发 | 快速关窗时踩到已关闭的数据库，打出 `sqlite3.ProgrammingError` 堆栈 | 回调开头做可见性守卫 + `recover_orphans()` 异常兜底 |
| **35** | **`load_session_state()` 只防 JSON 语法错误、不防结构错误** —— 顶层是 `null`/`[]`、或 `cookies`/`localStorage` 不是字典列表时直接 `AttributeError` | 这个文件是 Playwright 写在用户目录里的明文 JSON，被手改/截断/版本差异都可能；而它在 **GUI 启动路径**上被调用 → **应用一打开就崩**，用户无法自救 | 新增 `_as_dict_list()` 形状防线；顶层非对象按「未登录」处理并告警；`stat()` 加 OSError 兜底。任何异常都退化为空登录态 |
| **37** | **登录超时诊断对 URL 做子串匹配** —— `sso.ecnu.edu.cn/login?service=...` 的 query 里 URL-encode 了 `redirect_uri=https%3A%2F%2Fproxy.ecnu.edu.cn%2Fecnu_oauth2`，而代码先判 `"proxy.ecnu.edu.cn" in url` | **把「页面正等你输密码」误报成「被 webVPN 网关拦下」**，把用户指去折腾网络。实测两次真机登录（10 分钟 + 30 分钟窗口）都给出这句误导结论，用户永远不知道该干什么 | 改为 `urlsplit` 后只看**主机 + 路径**；真拦截判据 = 停留页主机就是网关 **且** 正文写着站点不存在/无权限；新增 `_body_snippet()` 安全读正文 |
| **38** | **`_detect_logged_in()` 用 `vpn_key` 子串判「未登录」** —— 而校外经 webVPN 进平台时 URL 上完全可能带 `vpn_key` | 比 37 更糟：**登录成功会被永久判成失败**，用户反复重试到放弃；这类「成功被判失败」最难排查 | 同一套主机/路径判定（认证域=未登录；平台域+页面特征或 localStorage token=已登录），用**实测真 URL** 反锁 |
| **39** | **真实抓包文件 `recon/network.jsonl` 被模拟流量污染** —— `fetch_catalog()` 结尾无条件 dump，而 8 个测试用例都会调它 | 每跑一次测试就往真实抓包里灌 20+ 条 `127.0.0.1` 记录；实测累积 **644 条**，真实请求仅 **5 条**；还会与**正在进行的真实登录并发写同一文件** | ① `tests/conftest.py` autouse 夹具把 `ECNU_TRANSCRIBE_HOME`/`LOCALAPPDATA` 指向临时目录（测试再也写不到工作区）；② `dump_network_log()` 护栏：默认路径+全环回则不写（显式 `path=` 照写；含真实请求整体照写） |
| **40** | **`PRAGMA journal_mode=WAL` 瞬时 I/O 错误 → 应用「一打开就崩」** —— 上一个持有该库的进程刚被强杀、Windows 尚未释放 `-wal`/`-shm` 映射时触发（全量测试 2 次里偶发 1 次） | `StateStore()` 在 **GUI 启动路径**上：真抛出去用户只能看到「双击没反应」，无法自救。而 WAL 只是并发/性能优化 | 新增 `_enable_wal()`：退避重试 4 次 → 仍失败则**降级为默认回滚日志模式继续运行**并留 WARNING；新增 `StateStore.journal_mode` 便于自检 |
| **34** | **设了「限速」会让所有下载失败** —— `build_input_args()` 把 `-maxrate` 放进输入侧，而它是**输出侧编码**选项；ffmpeg 直接报 `is not a decoding option` / `Error opening input files` | **只要用户填了非 0 限速，所有任务必然失败**（重试 4 次后报「未产出有效音频文件」）。此前所有测试都用默认值 0，所以没暴露 | 移除该参数；限速改为在 downloader 里按 `-progress` 的 `total_size` 自行节流（实测 4.2MB HLS：3.3s → 14.9s，约 4.5×）。tooltip 如实写明是「近似限速」 |
| 33 | 「预设」下拉只按 provider 反查，而预设 ①③④⑥ 的 provider 都是 `openai_compatible` | 配的是本地服务却显示「④ OpenAI 官方」被选中，用户以为配错了 / 被误导去改配置 | 改为按 **(provider, base_url, model) 三元组**反查；不匹配时落到新增的「（自定义 / 当前配置）」项 |
| **29** | **`DashScopeTranscriber` 缺少 `_notify()`** —— 原生异步模式一路上都在调 `self._notify`，但类里没定义，也不是继承来的（与 OpenAI 兼容实现是组合关系） | GUI 里标注「大文件/长音频更稳」的开关**一勾就必然失败**（AttributeError），长课程推荐模式完全不可用 | 补上 `_notify()` 与 `_wait_gate()` |
| 30 | 原生模式 base URL 硬编码 | 无法指向私有化/代理网关，也让链路无法测试 | 改为可配置，支持 `ECNU_DASHSCOPE_NATIVE_BASE` 覆盖 |
| 31 | 句级结果为空时返回**空转写** | 用户看到「ASR 返回了空结果」这种**误导性报错**（其实识别成功） | 句级无产出时回退到 `transcripts[].text` |
| 32 | 下载转写结果的 HTTP 错误漏出原始 httpx 异常 | 结果链接过期时用户看到裸栈 | 包装成 `TranscriptionError` 并给可操作建议 |
| 27 | 登录交互信号连接晚于线程启动（竞态） | 用户在启动瞬间点「我已登录完成 / 取消登录」会被丢掉，表现为「点了没反应」 | 先接信号再 `start()`；回归测试在「线程启动那一刻」检查回调是否已连接 |
| 28 | 用户主动取消登录却弹「登录未完成」错误框 | 主动放弃被报错，吓人且误导 | `LoginWorker` 新增 `canceled` 信号，取消路径单独安静收尾（INFO 日志 + 状态栏提示） |
| **26** | **关窗时未收尾全部后台线程** —— `closeEvent` 手写清单只列了 pipeline/catalog/login，后加的 `probe_worker`（首启自动诊断）没列进去 | **每次正常退出都以 `0xC0000409` 崩溃**：关窗时诊断线程还在跑，Qt 析构仍在运行的 QThread。日志一切正常，所以早先的冒烟没发现（只看日志没看退出码） | `_all_workers()` 动态枚举（显式属性 + `findChildren(QThread)` 兜底）；只对任务型线程弹确认；超时 `terminate()` 兜底 |
| **36** | **产物是「无 BOM 的 UTF-8」，而本机 ANSI 代码页是 cp936(GBK)** —— `_atomic_write()` 用 `encoding="utf-8"` 写 `.txt/.srt/.md`，字节流里没有任何编码标记 | **中文产物在 Windows 上大面积显示乱码**：记事本、PowerShell `Get-Content`、以及很多字幕播放器都会按 cp936 猜 —— 这是**系统默认行为**，不是个别软件问题，用户会以为转写坏了。根因还包括：既有测试一律用 `encoding="utf-8"` 读回（不断言字节头），所以**读侧从未校验过编码标记** | `UTF8_BOM` + `_atomic_write(..., bom=)`；`write_txt/srt/md/export_all` 全部透传；新增 `Config.emit_utf8_bom`（默认开）+ 设置页开关。**`.transcript.json` 有意保持无 BOM**（机器可读产物，BOM 会坑朴素解析器），换行仍严格 LF |

（另外修正：`pick()` 把 `0` 当空值、`extract_list` 固定层级、SRT 超长句被切碎、
`_is_duplicate` 时间门过窄导致重叠区重复未去重、multipart 文件名尾引号未剥离。）

**本轮的一处「我错了」也要记下来**（不是产品缺陷，是诊断过程）：第 35 轮我把一次**正常以
2× 实时速度下载**的拉流判成「ffmpeg 卡死」（依据是一个属于别的尝试的残留 `.part`、
一个取错实例的 `IO Read Bytes/sec` 计数器），并按这个错误判断强杀了进程 ——
这才制造出缺陷 50 的事故样本。判断「有没有在下」要看日志里的实际产出
（`用时=` 与落盘时长/字节），不要看推测性计数器。

---

## 10. 下一步（按优先级）

0. **现在就能做（不需要网络/账号/Key）**：跑 `scripts\demo_offline.py`，
   一条命令看完「合成语音 → 加密 HLS → 拉流 → 识别 → 三产物 → 断点续跑」全流程（已实测 2/2 成功）。
1. ~~**【需要你】打通网络**~~ ✅ 第 22 轮已更正：那条 `302 → vpn_key/update` 是**正常第一跳**。
2. ~~**拉真实清单（M1）**~~ ✅ 第 25 轮已完成：应用自己的客户端拉到 **30 门课 / 894 节**，
   与网页端一致，播放地址也能解析出签名 mp4。
3. **【需要你】重新登录一次**（唯一剩下的动作；实测登录态**不跨天**）：
   双击 `dist\大夏学堂转写助手\大夏学堂转写助手.exe` → 点「登录」，
   在弹出窗口里输入学号 + 密码（或跑 `.venv\Scripts\python scripts\login.py --capture`）。
   > 实测依据：09-12 21:03 登录的会话，到 09-14 09:47 已失效（`/oauth2/token` 302 回网关）。
   > 应用启动时会提示「很可能已过期」，不会让你撞上莫名失败。
   > 若登录窗口在你这台机器上不可见（JS 坐标与 OS 坐标不一致，缺陷 48），
   > 改用 `.venv\Scripts\python scripts\login_via_cdp.py`：普通 Chrome 窗口 + CDP 读回会话。
4. **登录后我立刻跑这两条命令**（都不需要你盯着）：
   * `.venv\Scripts\python scripts\verify_gui_real.py`（`QT_QPA_PLATFORM=offscreen`）
     → 真实 GUI 拉真实清单，M4 真机验收（**已实测 14/14**）；
   * `.venv\Scripts\python scripts\run_acceptance.py --picks 381401,395443 --max-segment 300`
     → 2 条**电平正常**的真实录播端到端（音频 → 本地 ASR → txt/srt/md）。
     （已选定：《高等数学（二）》vodId 764453 峰值 -6.2 dB、《世界政治经济地理》vodId 800713 峰值 -2.0 dB）
     > 单条耗时主要花在拉流：800MB 的 mp4 **实测 0.9×~2.0× 实时**（30~60 分钟），
     > 只抽音频后转写约 11 分钟（本地 small）。中断了不要紧：断点续传只补抓剩余部分。
5. ~~ASR 二选一~~ ✅ 零成本本机 ASR 已跑通（`scripts\local_asr_server.py`）；
   你的 DashScope Key 填进设置页即可换成云端（配置项都已就绪）。
   > **质量提示（本轮实测）**：本地 `faster-whisper-small` 在课堂杂音段会出现繁体字与噪声串
   > （正文内容正确，但开头几分钟明显退化）。要更好的中文课堂效果，建议填 DashScope Key
   > 用 `paraformer-v2`，或把本地模型换成 `medium` / `large-v3`（CPU 上会明显更慢）。
6. ~~PyInstaller 打包~~ ✅ 已随每次改动重新打包（本轮最后为 v0.11.0，`--doctor` 20/20、`verify_dist` 20/20）。
7. ~~面试级总结~~ ✅ 已完成（`docs/ARCHITECTURE.md`）。
8. ~~暂停作用于任务内部~~ ✅ 已完成（`scripts\verify_pause.py` 9/9）。
9. **后续可做（非 DoD 必需）**：
   * 给本地 ASR 加 `initial_prompt`（「以下是普通话，请用简体中文」）压制繁体输出；
   * 断点续传目前只在**同一签名地址仍有效**时可用；地址过期时先重新解析地址再续传（现在会退回整段重下）；
   * 多机位资源目前取时长最长的一条，可让用户在界面上手选。

---

## 11. 阻塞与风险

| 项 | 状态 | 说明 / 需要的动作 |
| --- | --- | --- |
| ~~课程平台校外不可达~~ | 🟢 **第 22 轮更正：不是阻塞** | 那条 `302 → proxy.ecnu.edu.cn/vpn_key/update` 是**正常第一跳**；实测浏览器继续走完 OAuth 即到统一身份认证登录页（HTTP 200）。校外**不必**先连 SSL-VPN |
| 统一身份认证需人工完成 | 🔴 **唯一的阻塞点** | 两次真机登录窗口（10 分钟 + 30 分钟）抓包显示**零次凭据提交** —— 页面就绪、等人输入。需要你：双击 exe → 点「登录」→ 在窗口里输学号 + 密码（验证码/二次验证需你手动过，工具不做识别） |
| ASR API Key | 🟡 你自己填 | 设置页 → 语音识别；会用 DPAPI 加密存到 `%LOCALAPPDATA%`。未配置时会明确报错，**不会伪造转写内容**。也可先用零成本的本地 ASR（`scripts\local_asr_server.py`） |
| 真实接口字段未知 | 🟡 已在缓解 | `docs/API.md` 备好待填；解析层宽容 + 显式变更检测 + 浏览器嗅探自动纠正 |
| 播放地址是否带动态签名 | ⚪ 待观察 | 已备好「浏览器上下文代打」降级通路 |
| Chromium 需单独安装 | 🟢 已缓解 | 开发机已装；打包版在 `--doctor` 里显式检查并给出安装指引 |

**合规声明**：全程不破解、不绕过 DRM 与身份验证，不做验证码识别；不上传音频/文本到
除用户自己配置的 ASR / LLM 端点之外的任何第三方；不删除用户原始数据（产物只增不改）。

---

## 12. 安全自检（DoD 第 5 条）

```powershell
.venv\Scripts\python -m pytest tests/test_security.py -q
# → 全部通过，含：
#   test_secrets_are_not_plaintext_on_disk   （磁盘上只有 dpapi: 密文）
#   test_gitignore_covers_sensitive_artifacts（recon/ 与 storage_state.json 已忽略）
#   test_redact_* 系列                        （Cookie/token/手机号/身份证/学号全脱敏）
```

* 明文密码：**从不接收、从不保存、从不打印**（登录在浏览器窗口内由用户输入）。
* `storage_state.json` / `browser/` / `secrets.json` 全部位于
  `%LOCALAPPDATA%\ecnu-transcribe\`，**不在工作区内**，且已被 `.gitignore` 覆盖。
* `recon/network.jsonl` 写入前逐条脱敏。
