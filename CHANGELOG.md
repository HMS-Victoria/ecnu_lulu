# CHANGELOG — 大夏学堂录播转写助手

本项目记录：**版本变化点** + **平台接口变化点**（后者是同类工具最容易被悄无声息搞坏的地方）。

格式参考 Keep a Changelog；版本号遵循语义化版本。

---

## [0.12.0] — 温暖书房界面（2026-09-21）

- 将课程、任务分成两页，采用米白与墨绿主题；日志默认折叠，异常提供直接处理入口。
- 搜索与跨课程勾选保留稳定身份；刷新不自动排队，运行期间新增待办留到下一批。
- 任务支持状态筛选、详情与实际输出入口；明确区分继续处理和重新识别。
- 设置整理为四类，常用参数与高级参数分层；取消、连接检查不落盘，保留未知配置和密钥。
- 当前批次使用配置与密钥快照；修正恢复任务、显式重新识别和登录过期的界面衔接。
- 打包补齐本地识别的 NumPy 动态依赖；明确标注的学号脱敏不再依赖先前配置登记，独立启动自检通过。
- 426 项常规测试通过，1 项慢模型测试按原规则跳过；缩放与 Windows 桌面检查范围见 `docs/ui-refactor/HANDOFF_2026-09-20.md`。2026-09-21 应用户要求单独打包 v0.12.0，打包验证另见 `docs/releases/PACKAGING_0.12.0_2026-09-21.md`。

## 先前未发布工作 — 开源整理（作品展示）

把仓库整理成可以对外展示的状态。**没有改动任何运行时行为**，405 个用例保持全绿。

### 新增

* `scripts/make_shots.py`：离屏**真实渲染**界面截图，喂入演示清单 + 各阶段任务 + 真实格式日志，
  产出 `assets/shots/`。不依赖显示器，可在 CI/无桌面环境运行。
  内含两个踩坑修复：offscreen 平台不带字体（必须显式指定含 CJK 的字体，否则整屏豆腐块）；
  表格 `Stretch` 列在 resize 后不会重算宽度（标题会被压到不可读）。
* `.gitattributes`：**全仓库文本一律 LF**（`* text=auto eol=lf`），二进制显式标注。
  本项目产物写出逻辑要求 LF，若克隆者 `core.autocrlf=true`，字节级编码测试会出现
  只在别人机器上复现的失败 —— 显式声明后与本地 git 配置无关。
* `LICENSE`（MIT），并注明许可范围仅覆盖源码，**不含**平台课程音视频与转写文本。

### 变更

* `README.md` 重写为作品展示版：界面截图、30 秒概览、工程难点对照表、
  关键设计决策、525 项自动化检查的可复现清单、阅读顺序引导。
* `DSH目标模式提示词-大夏学堂录播转写助手.md` → `docs/ORIGINAL_SPEC.md`，
  并加说明头（它是项目启动时的原始需求提示词，原样保留）。
* `.gitignore` 扩充：`dist-*/`（试打包目录）、`output-*/`（换 ASR 端点跑对照的产物）、
  `data/state.db*`（任务状态库含课程名与任务记录）。

### 安全

* **移除仓库内的真实学号**：`config.py` 默认值、`logbus.py` 注释、`docs/API.md` 样例、
  `tests/fixtures` 与 `scripts/*.py` 共 15+ 处，统一替换为同形状的占位值 `20261234567`
  （11 位、`10` 开头，因此学号脱敏规则与相关测试仍然测在点子上）。
* 用户可见提示文案改为「你的学号」，不再硬编码任何个人标识。

---

## [0.11.2] — 2026-09-14

**下载提速 2.6 倍 + 分片上限按实测夹取**：用户连续反馈"下载好慢""日志里失败了"，
两条都查到了实测根因，并按数据修掉。

### 新增：并行取音频（缺陷 63）

用户反馈"1 秒只能下 2 秒视频"。实测确认这**不是**应用限速（`speed_limit_kib=0`、无代理），
而是 **CDN 的单连接限速**：

| 方式 | 吞吐 | 一节课（838 MB） |
| --- | --- | --- |
| 单连接 | 672 KB/s（curl 实测，12 MB 校验字节数） | 约 21 分钟 |
| 4 连接并发（持续 160 MB，四份各 40 MB 均完整） | **2.1 MB/s** | 约 6.6 分钟 |

实现：按**时间窗**并行取音频 —— 每路 ffmpeg 带 `-ss/-t`，在支持 Range 的 CDN 上
只拉自己那 1/N 的字节，最后用 concat 解复用器拼回完整音频并校验时长：

```
并行结果：4 路，用时 71.1s，音频 599.9s ⇒ 等效 8.4× 实时
对照单流（同期正在跑的下载）：3.2× 实时      ⇒ 提速 2.6×
时长正确（≈600s）：✅
```

* 新增配置 `download_connections`（默认 4）、`download_window_min_sec`（默认 120s）；
* **任何一步不成都自动退回原单流下载**：不支持 Range、时长未知、本地文件、
  某路失败、拼接失败、时长校验不过 —— 绝不冒险；
* 取消/暂停在多路下同样有效（看门狗回调里判断）；中间产物（`.seg*` / `.parallel*`）用完即清；
* 用自建 **支持 Range 的**测试服务器钉住机制（Python 自带的 `SimpleHTTPRequestHandler`
  不支持 Range，用它测会让"并行"静默退化成单流 —— 测试看似通过其实没测到）。

### 修复：千问 ASR 单请求时长上限（缺陷 62）

用户在 GUI 里跑《数据结构与算法》时**6 段全部失败**：`400 The audio is too long`。
实测边界（用户真实音频，逐档打）：

| 输入 | 结果 |
| --- | --- |
| 300s @64kbps（2.29 MB） | ✅ |
| **301s** @64kbps（2.30 MB） | ⛔ `The audio is too long` |
| 300s @192kbps（5.72 MB） | ✅ |

⇒ 上限是**时长 300 秒**，与体积无关（300s@192k 体积是失败档的 2.5 倍仍通过）。
而配置默认「单段 ASR 最长秒数」= **600s** ⇒ 必然 100% 失败。

* 对这类模型（千问/Fun ASR，走 `/chat/completions`）自动把单段夹到
  `CHAT_AUDIO_MAX_SEGMENT_SEC = 270`（留 ~10% 余量，静音切点可能略微超出 300）；
  用户**调更小**仍然生效（想要更细字幕可设 30~60）；
* **兜底**：万一仍被拒为过长，就地把片段对半切开重试（最多 3 层，每半段带正确偏移）；
* 报错文案不再把"音频过长"混进"缺 CUDA/模型名不存在"那套误导提示，而是直说该调哪个参数。

---

## [0.11.1] — 2026-09-14

**用真实 Key 把云端 ASR 真正打通**：用户填好百炼 Key 后实测发现，原实现的两条云端路
**都不能用**，而且默认模型名在当前账号上**根本不存在**。这一版按实测结果重写了调用方式。

### 修复（全部有实测依据）

55. **DashScope 云端 ASR 三条路全断，且默认模型不存在**。逐条实测（2026-09-14）：

    | 路径 | 实测结果 |
    | --- | --- |
    | `POST /audio/transcriptions`（应用原路） | **对所有 ASR 模型 404** ⇒ 用户填了 Key 也跑不出东西 |
    | 原生异步 `file_urls` | 上传凭证能拿到，但 `paraformer-v2` 提交后 `SERVER_ERROR`；`sensevoice-v1` 报 `InvalidFile.DownloadFailed`；千问/Fun 系列直接 `url error`（它们不走这条） |
    | `POST /chat/completions` + `input_audio` | ✅ **可用**：`data` 必须是 **data URL**（裸 base64 被当成 URL 拒绝）；content 里**只能有音频项**（加 text 项报 ``The dedicated task `asr` … does not support this input``）；返回纯文本、**无时间戳** |
    | 默认模型 `paraformer-v2` | 该账号 250 个模型里**查无此名** ⇒ 默认配置必然失败 |

    * 新增 `chat_audio` 路由：千问/Fun ASR 系列**自动**走 `/chat/completions`（`_use_chat_audio`），
      可用 `cfg.asr_chat_audio` 强制开/关；`realtime` 变体（websocket）明确排除。
    * 默认模型改为 **`qwen3-asr-flash`**（设置页预设 ② / 配置默认 / README 同步）。
    * **时间轴诚实处理**：千问 ASR 不返回时间戳 ⇒ 每个切片按其自身 `[start, end]` 出一个 segment
      （静音切分的边界），**不按标点编造时间**。想字幕更细就把「单段 ASR 最长秒数」调小
      （实测 20s ⇒ 55 分钟课 419 条字幕，均 8s，覆盖完整）。
    * `_upload_to_oss` 两处字段名错误（同一缺陷）：凭证键是 **`oss_access_key_id`**（不是
      `access_key_id`），表单字段要用**连字符**形式 `x-oss-object-acl` / `x-oss-forbid-overwrite`
      （接口 JSON 里是下划线）。写错时 OSS 回的是"accessKeyId is empty" /
      "Policy Condition failed"，看着像权限问题，其实是自己读空了。

    **质量对照（同一条 55 分钟录播，两种 ASR）**：

    | | 本机 `faster-whisper-small` | 千问 `qwen3-asr-flash` |
    | --- | --- | --- |
    | 字数 | 12,496 | **16,360** |
    | 繁体专用字（們/這/說/時/對…） | **677** | **15** |
    | 标点（，。、；：？！） | **3** | **1,880** |
    | 耗时 | 约 11 分钟 | **4.8 分钟**（219 段） |

    20 秒同段对照更直观：本机把"国际斗争的军事的需要/科学技术"听成
    "红击各种的军事的需要/特殊技术"，云端全对。

56. **`accept_real.py --output-dir` 静默失效**。`upsert_task` 对已存在的任务是
    "幂等返回原记录、保留进度"，不会改 `output_dir` —— 于是想用另一个目录做对照时，
    它**复用了旧稿、还写回了旧目录**（本轮实测：`output-qwen` 空目录 + 旧产物被重新导出）。
    修复：目录不一致时显式更新并打印。
57. **脚本日志写 stderr 会把成功脚本的退出码染成 1**（PowerShell 把任何 stderr 输出当
    `NativeCommandError`）。日志改为**只在真终端**输出到 stderr（GUI exe 无控制台、
    管道场景不再污染退出码），文件日志照旧全量。
58. `probe_asr_endpoint` 的"模型不在 /models 列表"提示对 ASR 模型是**误导**：
    实测 `qwen3-asr-flash` 不在列表里却完全可用。现在对 ASR 类模型改为说明
    "该端点的 /models 不含 ASR 模型，能否使用要看实际调用"。

59. **被中断留下的 `.part` 会被整段重下（两段修复）**。ffmpeg 的输出写在 `<目标>.part`，
    进程被杀/关窗时它留在磁盘上，而旧逻辑只认正式缓存路径：
    * **部分完成** ⇒ 现在转成断点基准并只补抓剩余部分；
    * **其实已下完**（实测用户的 `.part` 是 25.19 MB / ffprobe 3301.3s vs 清单 3301.0s，
      只是收尾改名那一刻被关）⇒ 现在**直接扶正**为正式音频，一次下载都不再发生。

61. **清单时长没传下去 ⇒ 进度永远显示 3%，且把完整音频白重下一遍**（用户实测发现）。
    GUI 那条课的清单 `Resource.duration_sec = 0`（任务行里其实是 3301），而流水线调用
    `downloader.fetch()` 时**没传** `expect_duration` ⇒ 下载器拿到 0：

    * 进度百分比是「已下载秒数 ÷ 总秒数」算的，总时长为 0 时**一个进度事件都不发**，
      界面死停在 3%（用户反馈"跑了 5 分钟还没下载多少"）；
    * 「残件是否已下完」也无从判断 ⇒ 把一份**完整**的 25.19 MB 音频改名成 `.partial`，
      随后 `_try_resume` 又因"不知道总时长"拒绝续传 ⇒ 从头重下 55 分钟课。

    修复：① 流水线把已计算的时长传下去（`resource.duration_sec or task.duration_sec`）；
    ② 新增 `_adopt_complete_leftover()`：`.part` / `.partial` 里**已下完**的那份直接扶正；
    ③ 时长未知（0）时**什么都不动**（扶正会把半成品当完整，改名成断点又续不了）；
    ④ 扶正后清掉另一份更小的残留，避免磁盘上留两份。
    另：`login.py` 里**仍有一处** `SW_SHOWMAXIMIZED + HWND_TOPMOST`（上次只改了另一条路径）
    —— 正是用户明确要求去掉的"置顶、挡着看进度"，现已移除，只把窗口移到屏幕内并带到前台。

60. **界面在系统深色模式下"灰灰的看不清"**（用户实测反馈）。根因不是选了浅灰：
    本机 Windows 处于深色模式（`AppsUseLightTheme=0`），Qt 因此给出深色背景，
    而界面里有一批文字被硬编码成**深灰/纯黑**（`#333`/`#444`/`#555`/`#000000`），
    深字压深底 ⇒ 对比度极低。此外全局**没有自带主题**，外观随系统漂移。
    * 新增 `src/app/ui/theme.py`：Fusion + 显式 QPalette + QSS 的**浅色高对比主题**，
      不再跟随系统深色模式；正文 17.63:1、次要文字 9.5:1、状态色 ≥ 6.6:1。
    * 16 处硬编码颜色改为主题常量（含树/表里的纯黑文字、状态色表、日志区字体）；
      基础字号 9pt → **10pt**。
    * 启动时在日志里写一行主题信息，便于用户/排障时确认是否生效：
      `界面主题：浅色高对比（正文 #16191d / 底 #ffffff，对比度 17.6:1，10 pt）`。
    * 新增 `scripts/verify_contrast.py`：**算出**每对配色的 WCAG 对比度（16 对，全部 ≥4.5:1）
      并用离屏真实渲染出主窗口/设置对话框截图（`build/ui_shots/`），数字与肉眼互为佐证。

### 新增工具

* `scripts/verify_cloud_asr.py`：填完 Key 后的**一站式自检** ——
  免费鉴权检查（只打 `/models`，不发音频不计费）+ `--live` 同段对照试转
  （切片位置自动从已有转写稿里挑**最长**的一段，避免切到静音/噪声段；
  空文本判失败 —— 早先"切到静音返回 0 字"会被当成通过）。
* `tests/test_dashscope_chat_audio.py`（14 个用例）：路由规则、data URL 形状、
  只放音频项、切片时间轴、凭证字段名（含向后兼容旧键名）、凭证不完整时的可诊断报错。
* 假服务器 `tests/fake_dashscope_server.py`：新增 `/chat/completions` 路由，
  并按**真实**字段名返回凭证（旧风格可用 `legacy_policy_keys=True` 复现）——
  之前它写的是 `access_key_id`，所以"字段名写错"这个真 bug 一直测不出来。

---

## [0.11.0] — 2026-09-14

**「残缺的音频绝不当成完整」**：本轮的所有改动都服务于同一条底线 ——
宁可明确失败并告诉用户怎么办，也不交出一份**看不出残缺的产物**。

### 修复（三条都与「诚实性」直接相关）

50. **被中断的半份音频被当成「完成」**（真实触发）。验收时 ffmpeg 被强杀，留下
    `1900.5s / 3301.0s`（57%）的 MP3，日志里三处都「知道」不对，却全都只是记一笔：
    `run_ffmpeg(check=False)` **从不看退出码**；时长偏差 42.4% 只写 `log.warning`；
    紧接着 `音频完成：… 时长=1900.5s`。于是这半份音频被送进转写，产物残缺而无标记。
    * `check=True`：ffmpeg 非 0 退出直接判失败（但先验产物 —— 完整就放行并留告警，不误杀好文件）。
    * **截断裁决**：实际时长 < 清单 95% ⇒ 抛 `StreamTruncatedError`，残件转存 `<目标>.partial`，
      由上层重试；重试仍不完整就如实报错。
    * **断点续传**：`-ss <已有秒数>` 只补抓剩余部分，concat 解复用器 `-c copy` 拼回并校验时长。
      「抓了一半」不再是纯损失（实测 5s 断点 + 补抓 7s = 12.1s，通过校验后才替换正式产物）。
51. **续跑盲目信任阶段标记**（真实触发）。任务已记 `audio_ready`，续跑时只看「文件存在且
    > 1024 字节」就 `复用已有音频（不重下）` —— 缺陷 50 的半份音频**又被送去转写**。
    阶段标记只说明「当时以为完成了」，不是产物完整的证据。
    * 复用前用 `ffprobe` 量实际时长，与清单差 > 5% 即重新拉流；
    * 顺带修掉 `audio_ready → probing` **非法迁移**导致 `play_url`/`duration` 刷新被整批丢弃
      （界面上于是显示旧地址、旧时长）：阶段靠后时保持当前阶段、只更新字段。
49. **拉流停滞没有看门狗**（潜在缺陷，合成复现）。网络流挂住时 ffmpeg **不会自己退出**，
    而主线程正阻塞在 `proc.stdout` 读循环上 —— 写在回调里的任何检查都不会被触发，
    任务永远挂着一个不动的百分比。
    * 新增独立线程看门狗 `run_ffmpeg(..., watchdog=…)`：窗口内「推进的媒体秒数 ÷ 窗口秒数」
      低于 `download_min_speed_ratio`（默认 0.25×）即中止本次拉流并重试；
      暂停中、以及已抓完正在封头，都不计入停滞（避免误杀）。
    * 诚实标注：**本环境没有真实停滞样本**（原先以为遇到过一次，实为误判，见下方「更正」）。

52. **两个进程同时写日志时，轮转失败并丢日志**（真实触发）。GUI 与验收脚本并发时，
    2MB 轮转去 `os.rename` 对方正开着的 `logs/app.log` → Windows `PermissionError: [WinError 32]`
    → logging 打印一屏 `--- Logging error ---` 并**丢掉触发轮转的那条记录**。
    桌面应用被打开两次就会遇到。修复：`_MultiProcessSafeRotatingHandler` —— 轮转失败就
    **本轮不轮转、继续追加**，提示仅一次；新增 2 个用例（让底层 `os.rename` 失败，
    断言「一条不丢 + 不冒 logging 堆栈 + 提示只出现一次」）。
53. **`force=True` 时半份缓存被白扔**。`fetch(force=True)`（GUI「强制重跑」）会跳过缓存判定，
    于是把上次中断留下的半份音频直接覆盖重下 —— 实测那半份是 1900s/3301s，等于白扔 16 分钟。
    修复：把「不完整缓存降级为断点基准」提到 `fetch()` 开头，**与 force 无关**；
    新增用例 `test_force_download_resumes_from_incomplete_cache`。
54. **验收脚本的 `--picks` 会静默少跑一条**。`recentWatchRecord` 返回的 courId
    与 `myself/curriculum` 的 `id` **不是同一套 ID 空间**；当指定 courId 不在「最近观看」里时，
    课表兜底查不到，那条就被丢掉 —— 同一命令跑出「1 条」还是「2 条」取决于观看记录，
    验收不可复现。修复：查不到就直接用 courId 取播放地址，并用响应里的 `courName` 补课程名
    （否则标题缺课程名、还会把已有产物改名）。

### 更正（本轮的一处自我纠错）第 35 轮曾判断「ffmpeg 卡死、20 分钟零字节」，**该判断是错的**：
当时看的 `.part` 文件属于**上一次尝试**、`IO Read Bytes/sec` 取的是**同名进程的另一个实例**；
而日志显示 `音频完成 … 用时=955.5s` 抓回 1900.5s ⇒ **2.0× 实时，健康**
（对照基准：另一条 3301s 的课实测 0.9× 实时）。**是我按错误判断强杀了进程，才制造出缺陷 50 的样本。**
教训：判断「有没有在下」只能看日志里的实际产出（`用时=` 与落盘时长/字节）。

### 测试

* `tests/test_downloader.py` +6：截断产物被拒且保留断点、完整产物不留断点、
  断点续传只补抓剩余、看门狗策略（停滞给原因 / 健康与收尾放行）、
  看门狗真的能杀掉「安静等待」的 ffmpeg。
* `tests/test_pipeline_resume_guard.py`（新文件，3 个用例）：短缓存必须触发重下且
  **半份音频绝不进转写**、完整缓存仍然复用（没修成「每次重下」）、
  `audio_ready` 续跑时字段刷新不被非法迁移吞掉。
* `scripts/verify_gui_real.py`：修掉一处**脚本自身竞态** —— 工作线程结束后
  `finished_ok` 还是排队信号，直接读 `win.catalog` 会读到刷新前那份，
  把一次**成功的刷新**判成失败（实测 `fetched_at` 停在旧值）。
  现在会先排空事件队列，并新增断言「刷新结果已应用到窗口」。14/14 通过。

### 踩坑记录（写进代码注释）

* 中间产物叫 `x.mp3.part` / `x.mp3.rest`，ffmpeg **从扩展名推断不出封装格式**
  （`Unable to choose an output format`）—— concat 拼接必须显式 `-f`；新增 `media.muxer_for()` 统一。
* `_run_inner` 里原有的函数级 `from . import media` 会让 `media` 成为**整个函数的局部名**，
  在其之前调用 `media.duration_of()` 必然 `UnboundLocalError`（真机第一次重跑就是这么挂的）。

---

## [0.10.7] — 2026-09-12

**真实账号首次跑通 M1**：拉到 30 门课 / 894 节有录播的真实清单，并启动 2 条真实视频端到端验收。

### 新增：真实平台接口（实测契约，已落成客户端主路径）

SPA 的懒加载 chunk 里共有 **227 条**接口路径（首个 bundle 只有 23 条）——
把 chunk 全量抓下来做正则枚举，一次拿到真实契约：

| 环节 | 接口 | 坑 |
| --- | --- | --- |
| 换令牌 | `GET /jy-application-resourcemanage/oauth2/token` | 用 **`result.jwt_token`**（910 字符）；头名 **`jwt-token`**，不是 Bearer |
| 学期 | `GET /v1/list/termYear` | 返回 `id` = acteId |
| 我的课表 | `GET /v1/myself/curriculum?acteId=&page.pageIndex=&page.pageSize=` | 分页必须 `page.pageIndex`（写 `pageNum` 回「分页不能为空」）；行在 `records` |
| 班级节次 | `POST /v1/statistics/teaching-class/user/course-list` | 字段名 `teachingClassId`（写 `teclId` 回「教学班id不为空」） |
| 播放地址 | `GET /v1/course_vod_urls_new?courseId=<courId>` | 带 `auth_key` 签名的**直连 MP4**（`dudaomedia.ecnu.edu.cn:40443`），可能多机位 |

`EcnuClient` 现在默认走这条路（`cfg.platform_api = True`，可关）；
拿不到 jwt-token 时（例如对着模拟平台跑测试）自动回退到原来的「候选路径探测」。

* 播放地址改为**按需解析**（`resolve_play_url`）：清单阶段不发几百次请求，选中哪条才解析哪条；
  多机位时取**时长最长**的一条。
* 清单口径修正：`/v1/teachingclass/list` 返回的是**行政班级**（年级班），不是本人在上的课 ——
  真正的课表来自 `myself/curriculum`。

### 修复

41. **`LoginSession` 只会保存登录态，从不载入** —— Chromium 的持久化用户目录**不保留会话级
    Cookie**，而 `_webvpn_key` 正是会话级 Cookie。后果：用户明明刚登录成功、httpx 侧用得好好的，
    再点一次「登录」却被弹回统一身份认证页要求重新认证，用户完全无法理解。
    修复：新增 `restore_storage_state()`（把 `storage_state.json` 里的 Cookie 与 localStorage
    注入浏览器上下文），`open()` 时自动调用；并新增 `scripts/capture_pages.py`
    复用登录态重走页面抓接口（含 `--hash-nav`：SPA 内部跳转，避免整页重载触发重新认证）。

42. **课堂录音电平极低时，ASR 返回空结果，而报错把用户引向错误方向**（严重，真机实测暴露）。
    实测数据（本次验收第一条录像，线性代数 2025-12-11）：

    ```
    源 mp4 音轨：aac 单声道 16 kHz，mean_volume = -57.5 dB，max_volume = -34.2 dB
    （正常课堂语音约 -20 ~ -30 dB）
    → faster-whisper 的 Silero VAD 整段判为「无语音」，0.3 秒返回空文本
    → 用户看到的却是「ASR 返回了空结果。可能原因：音频无声/过短、语言设置不对…」
    ```

    这不是个例：抽查 5 条真实录播，峰值从 **-2 dB 到 -80 dB** 不等
    （《世界政治经济地理》-2.9 dB，《通用学术英语听说》-66.8 dB，近乎无声）。

    修复分两层：

    * **自动增益**（`media.normalize_for_asr` + `downloader._apply_level_policy`）：
      送 ASR 前先量峰值，低于 **-30 dB** 才动手（-18 dB 的语音实测能正常识别，
      不做无谓重编码），用「高通 80 Hz + 固定增益 + 限幅 0.95」抬到约 -3 dBFS，
      增益上限 40 dB（避免把底噪放大成假语音）。新配置 `asr_auto_gain`（默认开）。
      为什么用固定增益而不是 `loudnorm`/`dynaudnorm`：**实测对比**（同一段真实课堂音频）
      `volume=+28dB` 能转出文字，而 loudnorm / dynaudnorm 反而让 VAD 判成无语音（0 字）。
    * **诚实报错**（`pipeline._empty_transcript_hint`）：空转写时先量电平 ——
      若峰值仍低于 -50 dB，直接说明「**原因不在你的配置**，是这条录像的音轨几乎没有声音，
      大概率是学校录制设备/麦克风的问题，建议在平台上试听确认」；
      电平正常时才列出「确实没有说话声 / 语言设置不符 / 端点不支持」这些可能。

43. **录播列表字段名写错**：真实字段是 **`courseVodViewList`**，代码里写的是
    `courseVodVideoDtoList`（早期猜测的名字）。它之所以还能工作，纯靠后面那段
    「任何带 url 的字典列表」兜底逻辑 —— 一旦平台多返回一个带 url 的列表就会挑错。
    已按真实字段名修正，并把 `tests/fake_platform_server.py` 一并改成真名，
    让契约测试锁住它。

44. **「连不上」的报错太笼统，用户无从下手**。实测（2026-09-12 深夜）：本机 Clash
    换了订阅后变成**全局代理**，出口 IP 从上海电信变成**香港** `103.151.172.13`，
    学校网关对这类来源**直接重置连接**（`WinError 10054 远程主机强迫关闭了一个现有的连接`），
    而界面上只有一句「无法连接」——用户不可能想到是代理分流的问题。

    修复：`EcnuClient._interference_hint()` 把「连不上」按异常特征细分成三类，
    每类都给**可照做的下一步**：

    * 连接被重置（10054 / ECONNRESET / TLS EOF）→ 「把 `*.ecnu.edu.cn` 设为直连，
      Clash 里加 `- DOMAIN-SUFFIX,ecnu.edu.cn,DIRECT`，或临时关闭代理」；
    * 域名解析失败 → 「fake-IP DNS 或未接校园网」；
    * 超时 → 「确认已接校园网/SSL-VPN；校园网内仍超时可能是分流把校内流量带出去了」。

    另外诊断证据里会带上「当前是否配置了代理」，方便用户自查。

45. **对近乎无声的音频做增益，会让 ASR「编造」出不存在的文字**（严重，实测暴露）。
    第 42 条给「电平偏低」加了自动增益。但把这条规则用在**真正没有声音**的录像上会出事：
    实测把 -67 dB 的底噪硬抬 40 dB 后，faster-whisper 在「无语音」输入上**编出了**
    `字幕by索兰娅` 这种模板文本 —— 用户会拿到一份看起来像转写、其实是幻觉的产物。
    这比诚实失败糟糕得多。

    修复分三处：

    * `media.normalize_for_asr()`：**近乎无声时不放大**，只标记 `silent=True`
      （放大底噪只会喂给模型噪声）。
    * `pipeline`：`silent` 的音频**根本不送 ASR**，直接以明确文案失败
      （快、省钱、更不会编造）；文案点明「平均电平 x dB，正常课堂语音约 -13 ~ -19 dB」
      「是学校录制设备/麦克风的问题，**不是你的配置问题**」「建议在平台上试听确认，
      换一条有声音的录播即可」；新配置 `asr_allow_silent`（默认 **关**）供你强行识别。
    * 判定依据从**峰值改成平均电平**（`SILENT_MEAN_DB = -45`）：实测那条没录到声音的
      线性代数录像，整段**峰值仍有 -23.8 dB**（某处有个响声），但 `mean` 只有 **-54.5 dB** ——
      按峰值判断会把它当成「电平正常」送去 ASR，然后拿到空结果甚至幻觉文本。
      实测对照：有语音的录像 `mean` 在 -13 ~ -19 dB，没语音的在 -54 ~ -80 dB，门槛取 -45 正好分开。

    反证脚本 `scripts/verify_silent_honesty.py`（8/8 通过）直接用**真实**那条录像跑完整流水线，
    断言：判为 silent、文件未被改动、ASR 未被调用、失败文案可照做、
    **没有产出任何 txt/srt/md**。

46. **分段 ASR 的结果只存在内存里 → 长课程中途被打断要「重新花钱识别一遍」**（严重）。
    一节课 55 分钟会被切成十几段，**云端 ASR 是按段计费的**。而 `transcribe_chunked()`
    把每段结果只放进内存列表，成功后连切片文件都删了 —— 中途一断
    （崩溃 / 关机 / 被环境回收 / 用户点停止），重跑就得把**已经付过费的分段全部重来**。
    「可断点续跑」在 ASR 这一段其实是不成立的。

    修复：每段转写成功后**先落盘再删切片**（顺序反了等于这一段白跑且钱已花），
    文件为 `<缓存>/segments/<音频哈希>-<方案哈希>/result_NNN.json`，
    下次运行按四项校验复用：`index` / `start`,`end` 与本次切分方案一致、
    `model` + `provider` 一致、JSON 可解析。`Transcript.meta["reused_segments"]`
    记录本次复用了多少段，日志里也会写「本次共复用 N/M 段已缓存结果（未重复调用 ASR）」。

    写这个功能时被自己的测试抓到两个问题，都记在这里：

    * **`data.get("start") or -1` 把 0.0 当假值** —— 第一段的 `start` 就是 0.0，
      于是**第一段永远命不中缓存**，每跑一次就多花一次钱。日志症状是
      「只有第 2..N 段被复用」。这是本项目第二次栽在「0 被当成空值」上（第一次是 `pick()`）。
    * 分段缓存目录是**全机共享**的（`cache/segments/<音频哈希>-<方案哈希>`，续跑正靠它生效），
      所以**测试必须把 `cache_dir()` 指到临时目录**，否则测试之间会互相复用结果。

48. **登录窗口可能在屏幕外/被最小化 → 用户看不见，只能去自己的浏览器登录，
    而那是另一个 profile，登录态传不过来**（严重，真机实测）。

    现象非常有迷惑性：用户说「我已经登录了啊」，程序说「登录态已过期」——**两边都莫名其妙**。

    实测取证：

    ```
    Chromium 主窗口用户区矩形 = (-25600, -25600) 159x27   ← Windows 对最小化窗口报的哨兵坐标
    同一时刻 JS 里 window.screenX/screenY = 60, 60        ← JS 在骗人，user32 才是真相
    ```

    也就是说：`--window-position=60,60` 与 `window.moveTo()` 在这个环境里都**不生效**，
    必须对窗口句柄调 user32 的 `MoveWindow`（实测：`(-25600,-25600) 159px` → `(60,60) 1400x880`，可见）。

    修复：

    * `LoginSession._move_window_os_level()`：用 ctypes 枚举窗口（只认
      「类名 `Chrome_WidgetWin_1` + 标题含 `Chromium`」，**不碰用户自己的 Edge/Firefox**），
      发现被最小化/在屏幕外就 `ShowWindow(RESTORE) + MoveWindow + SetForegroundWindow`；
      `ensure_window_visible()` 先读 JS 位置（用于日志），再走 OS 层修正。
    * 启动参数加 `--window-position=60,60 --window-size=1440,900 --hide-crash-restore-bubble`
      （后者避免上次异常退出留下的「要恢复页面吗？」气泡挡住页面）。
    * **不再置顶**：第一版为了「确保看得见」加了 `HWND_TOPMOST` + 最大化，结果挡住了用户看别的内容 ——
      用户的反馈是「不要置顶，挡到我看任务进度了」。可见性问题的正解是**放在屏幕内的普通窗口**，
      不是抢焦点。

    另外新增一条**完全不碰用户窗口**的登录通路 `scripts/login_via_cdp.py`：
    用系统自带的 Chrome 起一个**普通窗口**（独立 profile + `--remote-debugging-port`），
    用户在里面正常登录，脚本再通过 CDP 连上去把 Cookie / localStorage 导出成 `storage_state.json`。
    这条路径随后**真的跑通了**（26 个 cookie，`session_ok()` → `jwt-token 910 字符`），
    并据此完成了 M4 真机验收与 2 条真实录播的端到端验收。

### 顺带的两处「别让用户撞上莫名失败」

* **登录态过期提前说**：`MainWindow._startup_hints()` 检查 `storage_state.json` 的保存时间，
  超过 12 小时就提示「很可能已过期（实测 webVPN 会话隔夜失效）；刷新清单若报『登录态已失效』，
  点「登录」重新认证即可，**已下载的音频与已完成转写都不会重跑**」。
  实测依据：2026-09-12 21:03 登录的会话，到 09-14 09:47 已失效（`/oauth2/token` 302 回网关）。
* **`scripts/accept_real.py` 的失效路径**：原本崩在 `resp.json()`（`JSONDecodeError`，
  看起来像程序坏了），现在明确输出「⛔ 登录态已失效…请重新登录一次」并给出两条命令。

### 验收结果（真实账号，单命令一次跑完）

```
[1] 登录态探测：✅ 可用 —— jwt-token 910 字符
▶ 真实 GUI（离屏）拉取真实清单      → 14 项通过，0 项失败
   30 门课 / 894 条录播，fetched_at = 本次刷新（不是本地缓存）
▶ 2 条真实录播端到端                 → 完成：2/2 条成功
   《高等数学（二）》2026-03-02 vodId=764453 时长=3301s → txt/srt/md
   《世界政治经济地理》2026-03-16 vodId=800713 时长=3302s → txt/srt/md
全部通过 🎉
```

命令：`.venv\Scripts\python scripts\run_acceptance.py --login-wait 900 --picks "381401,395443" --max-segment 300`

### 测试

* `tests/test_logbus_rotation.py`（新文件，3 个用例）：轮转失败不丢日志 / 提示只出现一次 /
  **stderr 是管道时保持安静**（否则 PowerShell 会把 NativeCommandError 当成退出码 1）。
* `scripts/accept_real.py` 的挑选缺陷（第 54 条）：`--picks` 指定 courId 时若它不在
  「最近观看」里，课表兜底因 **ID 空间不同**查不到 → 该条被**静默丢弃**，
  于是「跑 2 条」变成「跑 1 条」且随观看记录波动。修复：直接用 courId 取播放地址，
  并从响应里的 `courName` 补出课程名（否则产出的标题会缺课程名、把已有产物改名）。

### 新增工具与测试

* `scripts/run_acceptance.py`：**一键真机验收编排** —— 探测登录态 → 无效就打开可见浏览器
  等你完成认证（默认最多等 4 小时）→ 自动跑「真实 GUI 拉真实清单」→ 自动跑
  「2 条真实录播端到端」→ 汇总每步结果。把「等人操作」这件事也交给程序，
  你什么时候登录，验收就什么时候开始，不需要有人盯着。
* `scripts/verify_gui_real.py`：用**真实登录态**驱动**真实 GUI**（离屏）跑
  「刷新清单 → 真实 CatalogWorker → 左栏渲染真实课程树 → 关窗收尾」，
  断言真实课程/录播出现在界面里（DoD 的 M4 那一条）。
* `tests/test_client_http.py` +2：三类连通性提示的区分、诊断证据带上代理配置。
* `tests/test_gui_recovery.py` +2：登录态过期会提示、刚保存的登录态不提示。
* 文档同步：`README.md` 新增「连接被重置（代理把校内域名也代理走）」与
  「课堂录音很轻」两条排错项、⑫ 一键真机验收命令；`docs/ARCHITECTURE.md` 的清单流
  与任务流改成**实测契约**（jwt-token、`page.pageIndex`、按需解析播放地址、电平策略）。

### 新增测试（5 个电平用例 + 12 个契约用例）

`tests/test_downloader.py`：极轻音频会被量出并自动增益（且不削顶）、
够用电平不被重编码、增益有上限、近乎无声时留下「是录像的问题」的告警、
`asr_auto_gain=False` 时完全不动音频；另有空转写文案的两种分支断言。

`tests/fake_platform_server.py` 精确复刻真实接口的**参数名 / 字段名 / 鉴权头名**，
`tests/test_platform_api.py` 用它钉死契约：接口前缀推导、token 解析与缓存、
`page.pageIndex` 分页、`records` 取行、`jwt-token` 头必须带上、多机位取最长、
无录像时报错、清单按科目归组且跳过无录像课、拿不到 token 时回退、
`platform_api=False` 时完全不请求真实接口。

---

## [0.10.6] — 2026-09-12

**用真实账号做首次真机登录，抓包推翻了一个持续 21 轮的误判**，并修掉由此暴露的 4 个缺陷。

### 关键更正：校外访问的 `302 → proxy.ecnu.edu.cn/vpn_key/update` **不是「被封」**

从第 1 轮起，文档与诊断信息一直把它读作「被 webVPN 网关拦下 → 需要校园网或 SSL-VPN」。
本轮用真实浏览器抓包（`scripts/login.py --capture`）拿到完整跳转链后确认：

```
① GET https://courses.ecnu.edu.cn/jy-application-resourcemanage-ui/   302
② GET https://proxy.ecnu.edu.cn/vpn_key/update?...reason=site+...+not+found  302
③ GET https://proxy.ecnu.edu.cn/users/sign_in                          302
④ GET https://api.ecnu.edu.cn/oauth2/authorize?...                     301
⑤ GET https://sso.ecnu.edu.cn/oauth2.0/authorize?...                   302
⑥ GET https://sso.ecnu.edu.cn/login?service=...                        200  ← 统一身份认证登录页
⑦     登录页的 JS / CSS / 配置接口                                        全部 200
```

**第 ⑥ 跳真的渲染出了登录页**，之后 600 秒内零新请求 —— 也就是说
链路完全正常、页面上就在等人输密码。结论：

* 那条 `302` 是校外访问的**正常第一跳**，校外**不必**先装 SSL-VPN；
* 旧结论「`webvpn.ecnu.edu.cn` 无公共 DNS ⇒ 校外无法访问」把两件事混为一谈了；
* 真正的阻塞点从来只是「**没有人**在浏览器里完成一次认证」。

### 修复

37. **登录超时诊断对 URL 做子串匹配 → 把「页面在等你输密码」误报成「被网关拦下」**（严重）。
    `sso.ecnu.edu.cn/login?service=...` 的 query 里 **URL-encode 了
    `proxy.ecnu.edu.cn`**（`redirect_uri=https%3A%2F%2Fproxy.ecnu.edu.cn%2Fecnu_oauth2`），
    而 `_diagnose_stuck()` 先做 `"proxy.ecnu.edu.cn" in url` —— 命中了。
    于是工具把用户指去折腾网络，而用户唯一该做的就是**把密码输进去**。

    修复：改为**按主机 + 路径 + 页面正文**判断（`urlsplit` 后只看 netloc/path，
    真正的拦截判据是「停留页的主机就是网关 **且** 正文写着站点不存在/无权限」）；
    新增 `_body_snippet()` 安全读正文（页面已关闭也不抛异常）。

38. **`_detect_logged_in()` 用 `vpn_key` 关键字判定「未登录」→ 校外登录成功会被永久判成失败**。
    校外经 webVPN 进入平台时，URL 上完全可能带 `vpn_key`，而原逻辑见到
    `vpn_key`/`login` 等子串就 `return False`。后果比 37 更糟：**登录明明成功，
    工具却一直说没登录**，用户会反复重试到放弃。

    修复：同一套主机/路径判定（认证域 = 未登录；平台域 + 页面出现课程/录播等特征
    或 localStorage 有 token = 已登录）。

39. **真实抓包文件 `recon/network.jsonl` 被模拟流量污染**。
    `fetch_catalog()` 结尾无条件 `dump_network_log()`，而测试里的 8 个用例都会调它 ——
    每跑一次测试就往**真实抓包**里灌 20+ 条 `127.0.0.1` 记录。实测该文件累积到
    **644 条，真实请求只有 5 条**；更糟的是它会与**正在进行的真实登录并发写同一文件**。

    修复（两层）：
    * `tests/conftest.py` 新增 session 级 autouse 夹具 `_isolate_app_home`：
      把 `ECNU_TRANSCRIBE_HOME` 与 `LOCALAPPDATA` 都指向临时目录，
      于是 `recon/`、`data/`、`cache/`、`output/`、`logs/` 与
      `%LOCALAPPDATA%\ecnu-transcribe\` 全部被隔离，**测试再也写不到工作区**；
    * `dump_network_log()` 加护栏：默认路径 + 本次记录**全为环回地址**时不写文件
      （记录**保留在内存**，调用方仍可用显式 `path=` 另存 ——
      `scripts/mock_platform.py` 正是这么做的；夹杂真实请求时整体照写，绝不丢真实数据）。

    顺带：`scripts/mock_platform.py` 的「抓包已生成」检查原本依赖这个污染行为，
    现在改为写自己的目录，并**新增一条检查**「模拟流量未污染真实抓包文件」（26 → 27 项）。

40. **`PRAGMA journal_mode=WAL` 的瞬时 I/O 错误会让应用「一打开就崩」**。
    全量测试里偶发出现（2 次里 1 次）：

    ```
    self._conn.execute("PRAGMA journal_mode=WAL")
    sqlite3.OperationalError: disk I/O error
    ```

    触发场景：上一个持有该库的进程**刚被强杀**，Windows 还没释放 `-wal` / `-shm`
    的文件映射。而 `StateStore()` 是 **GUI 启动路径**上构造的，异常抛出去
    用户看到的就是「双击 exe 没反应 / 一打开就崩」—— 而且用户完全无法自救。

    修复：新增 `_enable_wal()` —— 瞬时错误退避重试（4 次），
    仍失败则**降级为默认回滚日志模式继续运行**（WAL 只是并发/性能优化，
    本地单文件 + 单进程 GUI 场景下差异可忽略），并写一条 WARNING 留痕。
    新增 `StateStore.journal_mode` 便于自检呈现。

### 新增测试（9 个用例，全部先红后绿）

`tests/test_login_state.py` +8：用**实测真 URL** 反锁两个判定
（停在 SSO ≠ 被拦；平台 URL 带 `vpn_key` 仍算已登录）、真网关错误页才算拦截、
认证域/认证路径一律不算已登录、页面已关闭时诊断不崩。

`tests/test_client_http.py` +5：环回流量不写真实抓包、真实流量照写、
显式路径绕过护栏、混合流量整体照写、套件级不变量「`recon_dir()` 已被隔离」。

`tests/test_store.py` +4：正常启用 WAL、瞬时失败会重试、永久失败降级不抛异常、
降级有 WARNING 留痕（含一个踩坑记录：包装 sqlite 连接时**必须同时代理属性赋值**，
否则 `row_factory` 落不到真实连接上）。

### 文档更正

* `docs/API.md` §0.2/§0.2.1：补上**完整真实跳转链**与「怎么判断是 A 需要认证还是
  B 真被拒绝」的判据；标注旧结论已更正。
* `README.md` §3/§8.1、`docs/ARCHITECTURE.md` §5：网络说明改为「校外点登录完成
  一次认证即可，SSL-VPN 只在网关真拒绝时才需要」。

---

## [0.10.5] — 2026-09-12

**修复「产出的中文文本在 Windows 记事本 / 字幕播放器里显示乱码」**（默认行为变更）。

### 修复

36. **产物是「无 BOM 的 UTF-8」，而简体中文 Windows 默认代码页是 cp936(GBK)**
    —— 中文文本在记事本、以及**大量字幕播放器**（PotPlayer / MPC 等）里会被
    按系统代码页猜编码，显示成乱码。

    实测证据（改前 / 改后，同一台机器，`chcp` = 936）：

    ```
    无 BOM 头 6 字节 : E5 90 8C E5 AD A6      # E5 90 8C = 「同」的 UTF-8
    带 BOM 头 6 字节 : EF BB BF E5 90 8C

    Windows PowerShell 5.1 Get-Content（默认编码 = 有 BOM 认 BOM，无 BOM 按 cp936）
      nobom.txt   默认读取首行前 6 码点: U+935A U+5C7D U+E11F U+6D60 U+E100 U+30BD   ← 乱码
      bom.txt     默认读取首行前 6 码点: U+540C U+5B66 U+4EEC U+597D U+FF0C U+4ECA   ← 同学们好，今

    Python 侧同样：b1.decode('cp936') → UnicodeDecodeError: illegal multibyte sequence
    ```

    也就是说：**没有 BOM 时，Windows 上的读取方会按 cp936 猜**，结果是乱码
    （或直接解码报错，工具用 `errors='replace'` 吞掉后显示成一串问号/方块）。

    修复：`.txt` / `.srt` / `.md` 默认写入 **UTF-8 BOM**（`EF BB BF`），
    记事本与主流播放器据此判定 UTF-8；`exporter.UTF8_BOM` +
    `_atomic_write(..., bom=)` 统一实现，`Config.emit_utf8_bom` 可关。

    设计边界（有意为之）：

    - **`.transcript.json` 始终不带 BOM** —— 它是机器可读产物，BOM 会让
      `json.load()` 之外的朴素解析器踩坑；
    - 换行仍严格 `LF`（`newline=""`），BOM 不改变换行约定，SRT 播放器不受影响；
    - 设置页可关（**设置 → 输出 → 产物写入 UTF-8 BOM**），并在鼠标悬停时
      说明「什么情况下才该关」。

### 新增

- `tests/test_exporter.py` +5 个用例：默认带 BOM（校验 `EF BB BF` 原始字节）、
  可关闭、`write_txt(bom=)` 开关生效、**带 BOM 的 SRT 仍可被正常解析**
  （首行序号 + 时间轴正则 + 无 CRLF）、`.transcript.json` 不带 BOM。
  同时用一个 `_cp936_misreads()` 助手**如实刻画风险**：cp936 猜错既可能得到
  mojibake，也可能直接解码失败 —— 两种情况都算「猜错」。
- `tests/test_settings_dialog.py`：把 `emit_utf8_bom` 纳入「非默认配置 → 控件 →
  配置」往返测试（防「新字段静默不落盘」）。

---

## [0.10.4] — 2026-09-12

**修复「storage_state.json 结构不对就让应用启动即崩」**，并验证登录态到 HTTP 请求的完整契约。

### 修复

35. **`load_session_state()` 只防 JSON 语法错误、不防结构错误**（严重）。
    它只用 try/except 包住 `json.loads`，之后直接 `data.get(...)` 并假设
    `cookies` / `origins` / `localStorage` 都是「字典列表」。
    于是**合法 JSON 但结构不对**时直接崩：

    ```
    data = null                         → AttributeError: 'NoneType' object has no attribute 'get'
    data = []                           → AttributeError: 'list' object has no attribute 'get'
    data = {"cookies": "not a list"}    → AttributeError: 'str' object has no attribute 'get'
    localStorage = "bad"                → AttributeError: 'str' object has no attribute 'get'
    ```

    这个文件是**被外部工具（Playwright）写在用户目录里的明文 JSON**，
    被手改、被半途截断、或被旧版本写成别的形状都完全可能。
    而 `load_session_state()` 是 GUI **启动路径**上的调用（刷新登录态提示就调它），
    所以后果是「应用一打开就崩」，用户完全无法自救。

    修复：新增 `_as_dict_list()` 作为形状防线（只保留字典元素），
    顶层非对象时按「未登录」处理并记警告；`path.stat()` 也加了 OSError 兜底。
    **任何**异常或结构不符都退化为「空的登录态」，绝不向上抛。

### 新增

- `tests/test_login_state.py`（22 个用例）—— 这是全应用**最关键的契约**：
  Playwright 写出的登录态必须能被 httpx 侧正确消费；任何一处对不上，
  表现都是「登录明明成功了，但拉清单说未登录」，而用户看不出原因。

  **契约往返**（用本地假平台**收真实请求头**验证，不是只看对象字段）
  * storage_state 里的 Cookie 与 localStorage token **真的出现在 HTTP 请求里**；
  * 未登录时不伪造 Cookie 头；
  * localStorage 里是 `Bearer xxx` 形式时正确提到 `Authorization` 头；
  * Cookie 头以 `'; '` 分隔；必要的 Referer / UA 都在。

  **健壮性**
  * 10 种畸形文件（空、空白、非 JSON、语法错误、顶层数组/null、
    `cookies` 非数组、cookie 缺 name、`localStorage` 非数组）**都不抛异常**，
    且不会被当成「已登录」。

  **脱敏与诊断**
  * `summary()` 保留键名、**不泄漏任何值**；
  * 可达性诊断能区分「需要登录」（可达）与「被 webVPN 拦下」（不可达）。

  测试 290 → **312**。

---

## [0.10.3] — 2026-09-12

补齐状态库的**并发安全**与**崩溃恢复**测试（断点续跑的地基）。

### 说明

这一版**没有发现新缺陷** —— 13 个新用例全部一次通过。之前几轮连续抓到问题，
这轮的结果说明状态库这块的设计是站得住的；把这些不变量固化成测试，
是为了防止后续改动把它弄坏（断点续跑是目标的硬性要求）。

### 新增

- `tests/test_store_concurrency.py`（13 个用例）：

  **并发安全**（GUI 的工作线程会同时命中同一个 `StateStore` 单例）
  * 8 个线程并发 `upsert_task` 同一资源 → **只能产生一条任务**（唯一索引 + 幂等 upsert）；
  * 写线程与读线程并发跑 1 秒 → 无异常、**进度始终在 0~100**；
  * 6 个线程并发抢锁 → **只有一个赢家**（否则同一任务会被两个 worker 同时跑）；
  * 20 个任务并发推进阶段 → 统计与事件表**不丢事件**。

  **崩溃恢复**（断点续跑的地基）
  * 关闭再打开：任务、进度、产物列表、事件历史**全部保留**；
  * **真·强杀**（起子进程写一半后 `kill()`）：父进程仍能打开库、读到任务、
    且 `recover_orphans()` 能把它回退到可续跑起点（有音频 → `audio_ready` 不重下），
    之后还能继续推进到 `done`；
  * WAL 模式确实开启（崩溃恢复与并发读写都依赖它）；
  * 上次运行残留的锁不会永久阻塞任务（`clear_all_locks()` 后可重新获取）；
  * **`recover_orphans()` 幂等**：重复调用不会反复抹掉进度
    （它每次启动都跑，不幂等就会让用户看到进度倒退）；
  * 终态（`done`/`failed`/`canceled`）的任务**绝不**被启动恢复改动；
  * 有音频 → 回 `audio_ready`；音频不存在 → 回 `pending`。

  **损坏处理**
  * 库文件损坏时抛出可读的 `sqlite3.DatabaseError`（而不是静默给空清单）；
  * 文件被截断后重新打开：要么明确报错，要么仍能读出任务 ——
    **不能假装「没有任务」**（那会让用户以为任务丢了）。

  测试 277 → **290**。

---

## [0.10.2] — 2026-09-12

**修复「限速一开、下载必挂」**（下载器分支测试逼出来的）。

### 修复

34. **设置限速会让所有下载失败**（严重）。`media.build_input_args()` 把
    ``-maxrate`` 放进了 ``-i`` 之前的**输入侧**参数，但 ``-maxrate`` 是
    **输出侧编码**选项（质量控制的 VBV 参数）。ffmpeg 的实际反应是：

    ```
    Codec AVOption maxrate (maximum bitrate (in bits/s)...) is not a decoding option.
    Error opening input files: Invalid argument
    ```

    于是 pipeline 重试 4 次后以
    「音频获取失败（重试 4 次）：ffmpeg 未产出有效音频文件」告终 ——
    **只要用户在设置里填了「限速」非 0，所有任务都会失败**。
    之前没发现是因为所有测试用的都是 ``speed_limit_kib=0``（默认值）。

    修复：
    * 从输入侧参数里**移除** ``-maxrate``（并加注释说明为什么不能放这里）；
    * 限速改为在 downloader 里**自己节流**：读 ffmpeg ``-progress`` 的
      ``total_size``，按目标速率与经过时间睡眠，副作用是 ffmpeg 的 stdout 管道
      被写满后自身也会慢下来。
    * **实测有效**：4.2 MB 的 HLS，不限速 3.3s → 限速 128 KiB/s 后 14.9s（约 4.5×）。
      受 ffmpeg 内部缓冲与进度上报粒度（0.5s/次）影响，**不是字节级精确**，
      因此把设置项的 tooltip 改成如实描述「近似限速」。

### 新增

- `tests/test_downloader.py`（11 个用例）—— 下载器此前几乎没有测试，
  而它是「能不能拿到音频」的唯一路径：
  * **回归**：``build_input_args()`` 在任何限速值下都不能出现 ``-maxrate``；
  * **回归**：设了限速也必须能下载成功（旧代码 4 次重试后失败）；
  * 限速要真的变慢（≥1.8×，不做字节级断言）；
  * 带**时效签名**的播放地址能正常拉取（query 不被丢掉）；
  * 播放列表 404 时给出可读错误；
  * 本地文件走纯转码路径（回归：曾因网络参数导致 `Option headers not found`）；
  * 缓存路径稳定且文件名合法（断点续跑依赖它）；
  * 缓存命中跳过下载且 sha256 一致。

  测试 266 → **277**。

---

## [0.10.1] — 2026-09-12

补齐设置对话框（写入全部设置 + 两个 API Key 的唯一入口）的往返保真度测试，
并修掉「预设下拉显示与实际配置不符」的误导性 UI。

### 修复

33. **「预设」下拉可能显示与实际配置不符的预设** —— `_load()` 只按
    **provider** 反查预设，而预设 ①③④⑥ 的 provider 都是 ``openai_compatible``：
    用户配的是本地服务（``http://127.0.0.1:8000/v1``），下拉却显示
    「④ OpenAI 官方」，用户会以为自己配错了、或者被误导去改配置。

    修复：新增 `_match_preset()`，按 **(provider, base_url, model) 三元组**反查；
    匹配不上时落到新增的「（自定义 / 当前配置）」项。
    顺带：选中「（自定义）」不再覆写用户已填的 base_url / model；
    并且加载时会根据 provider 正确设置 base_url 输入框的可用状态。

### 新增

- `tests/test_settings_dialog.py`（11 个用例）—— 设置对话框此前**零测试**，
  而它负责写入全部设置与两个 API Key。核心是**往返保真度**：

  * `_load()`：用一份**每个字段都非默认**的配置打开对话框，
    逐项断言 40+ 个控件的显示值（含 `"96k"` → `96` 的单位换算、枚举/下拉的 data 值）；
  * `on_save()`：改 30+ 个控件后保存，读回配置逐项断言（含 `96` → `"96k"`、
    `jitter_max` 被夹到 `≥ jitter_min`、`output_dir` 原样落盘）；
  * **凭据**：Key 只能进 `ConfigManager.set_secret()`（DPAPI），
    断言 `config.json` 与 `secrets.json` 里都**没有明文 Key**；
  * `on_save()` 必须 `accept()` 对话框（否则调用方按 Cancel 处理，看起来「没保存」）；
  * 码率往返多轮不漂移（`32/64/128/192/320`）；
  * 预设：逐个断言 provider/base/model 都被填上、本地预设必须指向 `127.0.0.1`、
    **DashScope 预设的地址与模型必须与 README 一致**（防止文档与代码脱节）。

  测试 255 → **266**。

---

## [0.10.0] — 2026-09-12

**修复 DashScope「原生异步模式」完全不可用**（GUI 里标注为「大文件/长音频更稳」的那个开关），
并补上这条链路的完整测试。

### 修复

29. **`DashScopeTranscriber` 缺少 `_notify()` 方法 → 原生异步模式一进去就崩**
    （严重）。`_transcribe_native()` 一路上都在调 `self._notify(...)`，
    但 `DashScopeTranscriber` 自己没定义它，也没有继承
    `OpenAICompatibleTranscriber`（两者是**组合**不是继承）——
    于是调用原生模式立刻
    `AttributeError: 'DashScopeTranscriber' object has no attribute '_notify'`。

    也就是说：设置在 GUI 里写着「使用 DashScope 原生异步 API（大文件/长音频更稳，
    需要能访问 OSS）」，用户一勾选，**每次转写都必然失败**。
    修复：补上 `_notify()` 与 `_wait_gate()`（后者让原生模式的轮询循环也能响应暂停）。

30. **原生模式的 base URL 硬编码** —— 无法指向私有化部署/代理网关，
    也让这条链路彻底无法被测试。修复：改为可配置，支持
    ``ECNU_DASHSCOPE_NATIVE_BASE`` 环境变量覆盖（默认仍是官方地址）。

31. **句级结果为空时返回空转写** —— `_parse_native_result()` 只在
    ``sentences`` 为**真值**时才解析句级结果；若接口把句级结果省略、
    或句级条目的 text 全为空，就会返回**空转写**，用户看到的是
    「ASR 返回了空结果」这种**误导性报错**（其实识别是成功的）。
    修复：句级没产出任何内容时一定回退到 ``transcripts[].text``。

32. **下载转写结果的 HTTP 错误会漏出 httpx 异常** —— 结果链接过期（通常 24 小时）
    时抛的是原始 ``httpx.HTTPStatusError``，不是本项目的错误类型，
    用户看到的是裸栈。修复：包装成 `TranscriptionError` 并给出可操作建议
    （链接可能过期 / 网络无法访问该域名 / 本地已完成的处理不会重做）。

### 改进

- 原生模式轮询改为**自适应间隔**（2s → 最多 10s，而不是固定 5s），
  减少空转；并在等待期间响应暂停闸门。
- 轮询日志带上已轮询次数，便于判断是「接口慢」还是「卡住」。

### 新增

- `tests/fake_dashscope_server.py`：可编排的假 DashScope 原生服务
  （getPolicy 凭证 / OSS 上传 / 异步提交 / 轮询状态机 / 结果下发，
  可注入 401、空凭证、缺 task_id、提交 400、任务 FAILED、句级结果缺失等）。
- `tests/test_dashscope_native.py`（15 个用例）：完整成功链路（5 步逐项断言）、
  上传必须携带全部 OSS 凭证、提交必须带 `X-DashScope-Async: enable`、
  **毫秒时间戳换算**、句级缺失/全空时的整段文本兜底、
  5 类失败路径的可诊断报错、非原生模式不碰原生接口。
  测试 240 → **255**。

---

## [0.9.2] — 2026-09-12

登录流程的两处交互可靠性修复（都会直接砸在**首次使用**体验上）。

### 修复

27. **登录交互信号连接晚于线程启动（竞态）** —— `on_login()` 里先
    `login_worker.start()`、再 `dlg.confirmed.connect(...)` / `dlg.cancelled.connect(...)`。
    线程一起来就在轮询登录态，而两个按钮回调还没接上：用户在那一瞬间点
    「我已登录完成」或「取消登录」会被**丢掉**，表现为「点了没反应」。

    修复：先接信号再 `start()`（并加了注释说明为什么顺序重要）。
    同时补了回归测试 `test_login_signals_connected_before_thread_starts` ——
    它在「线程启动那一刻」检查回调是否已连接，旧顺序必然失败。

28. **用户主动取消登录却弹出「登录未完成」错误框** ——
    `LoginWorker` 把「用户点了取消」和「超时/认证失败」都走 `finished_ok(False, ...)`，
    主窗口于是弹 `QMessageBox.warning("登录未完成", ...)`：用户主动放弃却被报错，
    既吓人又误导。

    修复：`LoginWorker` 新增 `canceled` 信号，取消路径单独上报；
    主窗口新增 `_on_login_canceled()` 安静收尾（关对话框、INFO 日志、「已取消登录」状态），
    不再弹错误框。

### 新增

- `tests/test_gui_recovery.py` 增加 3 个登录流程用例：
  信号连接顺序（含「线程启动那一刻」的检查）、取消登录不弹错误框、
  worker 必须具备 `canceled/confirm_logged_in/request_stop` 接口。
  测试 237 → **240**。

---

## [0.9.1] — 2026-09-12

**修复「一关就异常退出」的崩溃**，并补上打包产物的干净环境验证。

### 修复

26. **关窗时未收尾全部后台线程 → 进程崩溃**（严重，影响每一次正常退出）。
    `MainWindow.closeEvent` 手写清单只列了 `pipeline/catalog/login` 三个线程，
    而后来加进来的 `probe_worker`（首启 800ms 后自动跑的「首启一键检查」）
    **没被列进去**：关窗时它还在跑（正在做联网探测、超时可达 20s），
    Qt 直接析构仍在运行的 `QThread` → 进程以 `0xC0000409` 退出。

    为什么之前没发现：早先的 `--selftest` 冒烟只看日志没看**退出码**，
    日志里一切正常（窗口关了、应用退出），但进程码是非零的。

    修复：
    * 新增 `_all_workers()` **动态枚举**（显式属性 + `findChildren(QThread)` 兜底），
      以后再加线程不会再漏；
    * 只对**任务型**线程弹「仍在运行」确认框，只读的诊断线程静默停掉、不打扰用户；
    * 等待超时后 `terminate()` 兜底，避免「点了关闭却没反应」。
    实测：`--selftest` 退出码由 `-1073740791` 变为 **0**（连跑 3 次稳定）。

### 新增

- **`scripts/verify_dist.py`** —— 打包产物的**干净环境**验证（DoD 第 1 条的正面证据）。
  把 `dist\大夏学堂转写助手` 整个复制到**纯 ASCII 路径**，用**全新的用户数据目录**
  （不继承本机 config/secrets/登录态），并把 `PATH` 收窄到**不含 Python**，
  然后真实启动 exe。实测 **20 项通过，0 项失败**：

  ```
  ✅ 复制到纯 ASCII 路径（C:\ecnu-clean-test）
  ✅ 用户数据目录已隔离      ✅ PATH 中已无 Python
  ✅ --doctor 退出码为 0     ✅ 自检无失败项（17/17）
  ✅ 用的是自带 ffmpeg（不是系统 PATH 里的）
  ✅ --selftest 退出码为 0   ✅ frozen 模式识别正确
  ✅ output/ cache/ data/ logs/ 都在 exe 同级（可整体拷走）
  ✅ 日志已写入且被脱敏
  ✅ 配置写到 %LOCALAPPDATA%\ecnu-transcribe，且不含明文密钥
  ```

- 新增 3 个关窗行为测试：`_all_workers()` 枚举完整性、
  诊断线程运行中关窗不弹框且能停住、任务线程运行中关窗会确认并等待。

### 验证（M5 验收的正面证据）

- 双击启动（不带参数）：进程存活、窗口标题正确
  （`大夏学堂录播转写助手 v0.9.1`）、`CloseMainWindow()` 优雅关闭 → **退出码 0**。
- exe 二进制扫描：**不含任何指向开发工作区的绝对路径**
  （`开发机绝对路径`、`.venv` 等均未出现）。

---

## [0.9.0] — 2026-09-12

补齐**异常路径与恢复操作**的测试（此前完全没覆盖），并修掉一个「快速关窗会报错」的问题。

### 修复

25. **启动定时回调可能在窗口关闭后触发** —— `_startup_hints()` 由
    `QTimer.singleShot(200, ...)` 触发；若用户在 200ms 内关掉窗口
    （或程序化 close 后立即关闭状态库），回调仍会执行并踩到已关闭的数据库：
    `sqlite3.ProgrammingError: Cannot operate on a closed database`。
    修复：回调开头做可见性守卫，并对 `recover_orphans()` 加异常兜底 ——
    启动提示不该反过来把应用搞崩。

### 新增

- `tests/test_gui_recovery.py`（16 个用例）：用**真实的 `MainWindow` +
  真实的 `PipelineWorker(QThread)`**，只把 `Pipeline.run` 换成可编排的假实现，
  覆盖此前完全没测的两块：

  **PipelineWorker 的异常分支**
  * 成功路径：UI 表格变「完成」、进度条到 100、`queue_finished` 计数正确；
  * **登录态中途失效**：发 `request_relogin` 信号、弹「登录态失效」提示、
    队列**立即停止**（剩余任务不重复尝试）、状态栏登录态提示同步更新；
  * **DRM**：任务标 failed、错误详情列有内容、界面仍可用；
  * 普通失败：`error` 落库、重试次数累加；
  * 用户取消：落 `canceled` 而不是 `failed`（状态机语义）。

  **界面的恢复操作**
  * 移除任务：软删除（产物与历史保留，`include_deleted=True` 还能查到）；
  * 重跑：`pending` + **保留音频缓存**（续跑不重下）；
  * 强制重跑：清空音频引用；
  * 清空列表：**不删除用户产物文件**；
  * 清除登录态：删除 `storage_state.json` 并把状态栏改回「未登录」；
  * 登录态提示：有/无状态时文案正确；
  * 树与表格状态一致（失败时两处都显示失败）；
  * 搜索过滤：命中/隐藏/清空恢复；
  * 「只选未转写」：已完成的资源不被勾选；
  * 导出清单：写出文件；
  * 打开输出目录：使用配置的目录。

  测试 218 → **234**。

---

## [0.8.0] — 2026-09-12

补齐 `llm.py`（DeepSeek 文本后处理）的测试，并修掉两个**会悄悄毁掉转写结果**的缺陷。

### 修复

23. **重新分段时「解析失败」被当成「不需要换段」→ 整篇被合并成一段** —— 严重缺陷。
    `_parse_break_list()` 解析失败时返回空集合，调用方把它理解成「模型说没有断点」，
    于是把整块片段 **coalesce 成一段**：10 个片段变成 1 个，时间轴信息被悄悄抹掉。
    因为**文字内容还在**（只是并成一段），肉眼几乎看不出问题，但 `.srt` 直接退化成
    一条覆盖整节课的字幕。

    修复：区分两种语义 —— 解析失败返回 `None`（保持原分段），
    明确返回空列表才是「不换段」。顺带修掉 `{"break_after": []}` 因 `or` 短路
    被误判为解析失败的问题。

24. **修复类请求把「输出格式示例」塞在用户消息里** —— 模型偶尔会把示例原样回显，
    示例里的 `i=0` 与真实数据的 `i=0` 撞车，看起来「调用成功了」，
    实际只改了第 0 条甚至什么都没改。修复：**用户消息里只放数据**，
    格式说明全部移到 system prompt。

### 新增

- `tests/fake_llm_server.py`：可编排的 OpenAI 兼容假 LLM 端点
  （按调用序号返回任意内容、可注入 401/429/500/503、可读取请求体）。
- `tests/test_llm_postprocess.py`（34 个用例）—— 核心是**安全不变量**而不是功能体验：
  * 修复前后**片段数量与时间戳必须逐个相等**（只允许改 text）；
  * 10 种畸形响应（空内容 / 非 JSON / 缺 text / 只回解释文字 / 回 HTML …）
    必须**退化为原文**，绝不污染产物；
  * HTTP 401/429/500/503 一律降级为「保留原文 + 告警」，不让任务失败；
  * 空转写不调用模型（不浪费额度）；
  * 重新分段不丢内容、时间轴单调、解析失败保持原分段；
  * 1-based 序号、`\`\`\`json` 围栏、前后夹说明文字等真实模型行为都能正确处理；
  * 长转写按上限分块调用，且每一块都被应用；
  * API Key 不出现在告警/日志里。

  测试 184 → **218**。

---

## [0.7.0] — 2026-09-12

**修复长课程字幕时间轴被叠加两次偏移的严重缺陷**（切分管线里的双偏移）。

### 修复

20. **`merge_segments` 的偏移被叠加两次（时间轴整体错位）** —— 严重缺陷。
    `transcribe_chunked()` 调用 `_transcribe_one(..., offset=chunk.start)` 时，
    返回的片段**已经是全局时间戳**；随后又把 `(chunk.start, segs)` 交给
    `merge_segments()`，而它的语义是「局部时间 + offset」→ 偏移被加了两次：
    第 N 段变成 `2*start + local`。

    实测症状（5 段 / 20s 音频）：

    ```
    merge input : [(0.0,(0.0,5.0)), (4.0,(4.0,9.0)), (8.0,(8.0,13.0)), (12.0,(12.0,17.0)), (16.0,(16.0,20.0))]
    merge output: [(0.0,5.0),      (8.0,13.0),      (16.0,21.0),      (24.0,29.0),       (32.0,36.0)]   ← 全错
    ```

    末段被算到 32~36s，而音频只有 20s —— 字幕时间轴完全不可用。
    **影响范围**：只有会被切分的长音频（超过单段上限的课程录播）才触发；
    短视频走整段转写路径不受影响。这正是真实课程最容易踩、而人工又最难发现的一类问题。

    修复：`results.append((0.0, segs))`，并在 `merge_segments` 的 docstring 里
    把 offset 语义写清楚（调用方若已拿到全局时间戳就必须传 0.0）。
    修复后：

    ```
    merge output: [(0.0,5.0), (4.0,9.0), (8.0,13.0), (12.0,17.0), (16.0,20.0)]   ✅ 与切分方案一致
    ```

21. **分段切片缓存被跨方案复用** —— `cache/segments/<音频哈希>/part_NNN.mp3`
    的 key 只含音频哈希。改了 `asr_max_segment_sec` / `overlap` / 切分策略后，
    上一次运行遗留的切片会被**直接复用**：内容是旧方案切的、时间戳按新方案平移，
    得到一堆错位字幕，而日志上完全看不出异常（文件名一样、文件也在）。
    修复：缓存目录 key 改为 `<音频哈希>-<切分方案哈希>`，
    并给 `_segment_usable()` 增加**时长校验**（与期望时长偏差 ≤ 25% 才复用），
    双保险防止「存在即信任」。

22. **片段/合并结果可能超出音频总长** —— ffmpeg 用 `-ss/-t` 切片时会略微超出请求长度，
    真实 ASR 端点也按实际解码长度返回时间戳，于是出现「结束时间 > 音频总长」的字幕
    （永远播不到）。修复：新增 `_clamp_to_range()`，把片段夹进所在分段区间，
    合并后再整体夹进 `[0, 音频总长]`。

### 新增

- `tests/fake_asr_server.py`：可编排的 OpenAI 兼容假 ASR 服务
  （按调用序号返回预设文本、按切片真实时长生成时间戳、支持 `text`/`srt`/`verbose_json`），
  用于在完全离线的情况下验证切分管线。
- `tests/test_chunked_pipeline.py`（10 个用例）：真实音频的静音检测与切分规划、
  **真实多段切分 → 逐段 ASR → 平移合并**的不变量断言（段段有调用、内容不丢不重、
  时间轴单调且不越界、偏移只加一次）、缓存 key 随切分方案变化、
  端点不支持 verbose_json 时回退 srt、全段失败时显式抛错。
  测试 174 → **184**。

---

## [0.6.0] — 2026-09-12

**首启一键检查**：打开应用就知道「缺什么、下一步做什么」。

### 新增

- **首启一键检查（`F2` / 工具栏「首启检查」）** —— 一次查完所有前置条件，
  并且**每个失败项都给出可照做的动作**，最后汇总一句「下一步做什么」：

  ```
  【首启一键诊断】
    ✅ 输出目录可写：<应用目录>/output
    ✅ ffmpeg 可用：C:\Users\...\ffmpeg.EXE
    ✅ 登录用浏览器（Chromium）：正常
    ⛔ 课程平台网络可达：已被 webVPN 网关拦截：该入口需要校园网或学校 SSL-VPN。
        → 当前既不在校园网、也没走 VPN。请接入校园网，或连接学校 SSL-VPN
          （https://vpn.ecnu.edu.cn/portal/），然后点「重新检查」。
    ⛔ 已保存登录态：尚未登录
        → 点「登录」，在弹出的浏览器里完成一次统一身份认证（学号 + 密码，可能还有验证码）
    ⛔ 语音识别（ASR）已配置：未填 API Key（端点 https://dashscope.aliyuncs.com/compatible-mode/v1）
        → 二选一：① 跑本地服务 scripts/local_asr_server.py，Base URL 指向
          http://127.0.0.1:8000/v1（不需要 Key）；② 到「设置 → 语音识别」填 DashScope API Key

  下一步：当前既不在校园网、也没走 VPN。请接入校园网，或连接学校 SSL-VPN…
  ```

  首启（未登录时）会自动静默跑一次并写进日志，用户一打开就能看到结论。

- `ecnu_transcribe.login.run_readiness_check()` / `ReadinessReport` / `ReadinessItem`：
  检查输出目录可写、ffmpeg、Chromium、站点可达性、登录态、ASR 端点；
  区分**阻塞项**与**非阻塞提醒**（例如未配置 DeepSeek 只是 ⚠️，不影响主流程）。
- `ProbeWorker` 支持 `full_readiness` 模式（原「连通性自检」保留）。
- `tests/test_readiness.py`（13 个用例）：报告结构、图标、阻塞判定、
  「下一步」优先级、失败项必须带动作、离线不抛异常、本机 ASR 端点不误判为未配置。
  测试 161 → **174**。

### 说明

- 这一版没有修复缺陷，纯粹是可用性补强：把「该连 VPN / 该登录 / 该配 ASR」
  从「用户自己猜」变成「应用直接告诉他」。

---

## [0.5.0] — 2026-09-12

**M4 验收流程自动化跑通**：驱动真实 GUI 完成「刷新清单 → 勾选 → 入队 → 开始 → 完成 → 6 个产物」。

### 新增

- **`scripts/verify_gui.py`** —— 在 offscreen 模式下**驱动真实 GUI 控件**跑完 M4 验收里
  「登录之后」的全部环节（登录本身需要本人过统一身份认证，无法自动化）：

  ```
  [模拟平台] 真实 HTTP 接口（3 门课 / 7 条资源）+ 真实 HLS 媒体服务（含 AES-128 一路）
      → [真实 EcnuClient] fetch_catalog()  →  Catalog
      → [真实 MainWindow] 渲染课程树 → setCheckState 勾选 2 条 → on_enqueue_checked()
      → [真实 PipelineWorker(QThread)] 下载 → 本地 ASR → 写出 txt/srt/md
      → [真实 Pipeline] 断点续跑：重跑不重下、不重跑 ASR
  ```

  实测 **29 项通过，0 项失败**：

  ```
  ✅ MainWindow 构造并显示成功  — 三栏 size=1560x940
  ✅ 课程树已渲染（3 个顶层课程节点）      ✅ 录播子节点共 7 条
  ✅ 勾选生效  — 2 条                    ✅ 「已勾选」统计已更新  — 已勾选：2 条 / 01:30:42
  ✅ 任务表格出现 2 行                   ✅ 任务初始阶段为 pending
  ✅ 后台线程已结束（UI 未被阻塞致死）    ✅ 收到 queue_finished 信号  — [(2, 0)]
  ✅ UI 收到阶段回调  — 21 条 task_stage 信号
  ✅ 阶段覆盖 downloading/transcribing/done
  ✅ 状态库中 2 条都是 done               ✅ 表格「阶段」列显示完成  — ['完成', '完成']
  ✅ 表格进度条到 100                     ✅ 共 6 个产物文件被记录
  ✅ 6 个产物真实存在于磁盘               ✅ 扩展名为 txt/srt/md 各 2 个
  ✅ txt 有真实转写文本                   ✅ srt 含时间轴   ✅ md 含元信息与全文
  ✅ 树节点状态已刷新为完成               ✅ 日志面板有内容  — 2531 字符
  ✅ 重跑后仍是 done   ✅ 音频未重新下载   ✅ 产物仍为 3 个（并生成 .bak 备份）
  ✅ 窗口正常关闭
  结果：29 项通过，0 项失败
  ```

- 模拟平台新增**静态媒体服务**（`serve(media_dir=...)`）：除接口外还能把本地生成的
  HLS 播放列表/分片/AES key 直接发出去，于是「清单 → 播放地址 → 下载 → 转写 → 产物」
  整条链路可以在完全离线的情况下端到端跑通。

### 修复

18. **offscreen 环境下模态 `QMessageBox` 会永久阻塞** — 任务全部成功的分支会弹
    「完成」对话框，在无显示器/自动化环境里没有用户点击，脚本直接卡死
    （本次实测命中：验证脚本挂起 10 分钟）。`verify_gui.py` 现在把
    `QMessageBox.information/warning/critical/question` 替换成记录器，
    既避免卡死又能断言「该弹的窗弹了」。
    另注：这不影响真实使用（有显示器时用户正常点确定）。
19. **验证脚本里清单时长与本地样例素材不一致** — 模拟平台按真实课程的时长（2712s 等）
    返回，而本地合成素材只有 20s，触发「时长偏差 > 2%」告警，属测试环境噪声。
    验证脚本改为把选中条目的时长对齐素材真实时长。

---

## [0.4.0] — 2026-09-12

清单客户端的 HTTP 逻辑用**模拟平台服务器**完成端到端验证（M1 逻辑验收）。

### 新增

- **模拟平台服务器 `scripts/mock_platform.py`**：本地 HTTP 服务，按平台常见的
  `{code, msg, data}` 包装返回假数据（3 门课 / 7 条录播，字段命名故意混用
  camelCase、snake_case、别名），然后用**同一个** `EcnuClient` 完整跑一遍。
  按**端口**区分 7 种模式：`normal` / `empty` / `autherr` / `http401` / `redirect` /
  `slow`（验证候选探测）/ `weird`（验证接口变更检测）。
  实测 **26 项通过，0 项失败**：

  ```
  ✅ 课程数 = 3（翻了 2 页）        ✅ 资源总数 = 7
  ✅ 时长解析正确（含 "01:02:05"/"45:30" 字符串形式）  — 20562s vs 期望 20562s
  ✅ 业务码 A0230 → AuthExpiredError
  ✅ HTTP 401    → AuthExpiredError
  ✅ 302 跳登录页 → AuthExpiredError
  ✅ 结构异常 → ApiChangedError
  ✅ total=0 正常返回空清单
  ✅ 候选探测自动切换并拿到数据  — {'course_list': '/api/course/list'}
  ✅ 请求记录已脱敏  — 9 行，不含假 Cookie 值
  ```

- `tests/test_client_http.py`（15 个用例）：把模拟平台作为 fixture（模块级复用、
  按模式分端口），覆盖全量清单、分页、时长字符串解析、毫秒时间戳、mime 推断、
  catalog 往返、空清单、三种鉴权失效、结构异常、候选探测、
  播放地址（接口解析 / 已带 URL / 相对路径）、请求记录脱敏。
  测试 146 → **161**。

### 修复

17. **接口记录「写出并清空」语义不清** — `fetch_catalog()` 内部已调用
    `dump_network_log()` 写文件并清空内存缓冲，调用方随后再 dump 会拿到**空文件**，
    排障时容易误判成「没有请求记录」（写验证脚本时命中）。
    明确该语义并补文档；验证脚本改为直接读 `recon/network.jsonl`。

---

## [0.3.0] — 2026-09-12

暂停真正作用于**任务内部**；本地文件播放源支持；取消状态语义修正。

### 新增

- **暂停闸门 `PauseGate`（`src/ecnu_transcribe/pausegate.py`）**：让「暂停」在
  **最近的天然断点**生效，而不是等整个任务跑完。
  断点覆盖：流水线各阶段之间、每个音频分段转写前、每次 ASR 请求前、重试退避期间、
  每个 LLM 处理块前；**ffmpeg 拉流通过阻塞输出读取就地冻结**（恢复后继续下载而不重下）。
  暂停期间每 10s 上报一次「已暂停 Ns」，恢复时上报「▶ 已恢复」。
  连带修正：暂停中用户点「停止」会立刻放行（不会卡在闸门上）。
- **`scripts/verify_pause.py`**：暂停功能的端到端验证脚本。在**真实运行的流水线**中间暂停，
  验证「进度确实停住 / UI 收到暂停提示 / 恢复后继续并完成 / 产物完好」，
  以及「暂停中点停止 → 立刻中断且落到 canceled」。实测 **9 项通过 0 项失败**：

  ```
  ✅ 暂停后进度不再前进（确实停在断点）  — 暂停后新事件 0 条，进度仍为 90.0%
  ✅ UI 收到暂停状态提示  — ⏸ 已暂停（分段边界，点「继续」恢复）
  ✅ 恢复后任务继续并完成  — stage=done
  ✅ 暂停/恢复未破坏产物（3 个文件）
  ✅ 暂停中「停止」能在 30s 内退出  — 耗时 0.0s
  ✅ 任务状态落到 canceled
  ```

- `tests/test_pausegate.py`（18 个用例）：闸门语义、取消优先、超时、
  暂停时长统计、流水线断点、转写器/下载器闸门接入。

### 修复

14. **本地文件当播放源时拉流失败** — `build_input_args()` 会无条件加上
    `-headers` / `-reconnect*`，ffmpeg 对本地文件会直接报
    `Option headers not found` / `Error opening input file`。
    新增 `_is_local_source()`：本地路径与 `file://` 走**纯转码**路径（不带任何网络参数），
    并在文件不存在时给出明确错误。
15. **取消被当成失败** — 分片转写里用户取消抛的是 `TranscriptionError("转写被用户取消")`，
    流水线按普通错误处理 → 任务落到 **failed** 而不是 **canceled**（用户会以为出错了）。
    统一改为抛 `TaskCancelled`，状态机正确落到 canceled。
16. **暂停期间仍会多打一次 ASR 请求** — `_transcribe_one()` 的重试循环入口没有过闸门。
    改为每次真正发起请求前都过一遍闸门。

---

---

## [0.2.0] — 2026-09-11

本地 ASR（零成本转写）、一键离线演示、接口自愈、打包交付与文档补全。

### 新增

- **一键离线演示** `scripts/demo_offline.py`：不需要校园网、不需要账号、不需要任何 API Key。
  自动合成「模拟课堂」语音（优先用 Windows 中文语音合成，缺失时退化为音调占位并**如实标注**）
  → 发布为本地 **HLS 流（一路 AES-128 加密）** → 真实 ffmpeg 拉流取音频 →
  本地语音识别 → 产出 6 个文件（2 条 × txt/srt/md）→ 演示断点续跑。
  实测：`完成：2/2 条任务成功，共 6 个产物文件`，重跑「音频未重新下载 ✔ / ASR 未重复调用 ✔」。
- **本地 OpenAI 兼容 ASR 服务** `scripts/local_asr_server.py`：
  把 `faster-whisper` 包成 `/v1/audio/transcriptions`（支持 `verbose_json` / `srt` / `text`）、
  `/v1/models`、`/health`。应用侧零改动即可使用（类型选「OpenAI 兼容端点」，
  Base URL 指向 `http://127.0.0.1:8000/v1`，Key 留空）。**音频全程不出本机。**
  实测：19.4s 中文语音，`tiny` 2.1s（9.3× 实时）、`small` 8.9s（2.2× 实时）。
- **设备自动选择 + 真实前向自检**：`resolve_device()` 不信任
  `get_cuda_device_count()`（GPU 驱动在、但 ctranslate2 是 CPU-only 构建时会误判），
  改为真跑一次 GPU 前向，失败即回退 CPU + int8 并打印原因。
- **ASR 错误分类**：新增 `_PERMANENT_HINTS`，把「重试也没用」的错误
  （缺 DLL、模型名不存在、连接被拒、4xx 参数错）与「值得重试」的错误（429/5xx/超时）分开。
- **浏览器接口嗅探兜底** `login.sniff_api_paths` + `EcnuClient._sniff_endpoints`：
  候选路径全失败时借已登录浏览器观察真实 XHR 接口并重试（只嗅探 `*.ecnu.edu.cn`，
  每次运行最多一次）。接口变更从「只报错」升级为「自动纠正」。
- **头部按名脱敏** `login.redact_headers()`：`authorization` / `cookie` / `x-api-key` / `x-token`
  等按键名整段替换（值的形态不可预测，靠键名判定才可靠）。
- **登录卡住的定向诊断** `LoginSession._diagnose_stuck()`：按最后停留页面给出可照做的处理。
- **`scripts/doctor.py`**：17 项环境/产物自检（冻结态路径、**真实跑一次 ffmpeg 转码**、
  DPAPI、状态库读写含产物列落库、Playwright Chromium、脱敏、配置读写）。
- **exe 命令行入口**：`--doctor`（自检）/ `--login`（命令行人工登录）/ `--selftest`（GUI 冒烟）。
- **打包交付** `dist\大夏学堂转写助手\大夏学堂转写助手.exe`（`--onedir`，内嵌 ffmpeg 8.1.1 + ffprobe）。
- **`docs/ARCHITECTURE.md`**：面试级架构说明（架构图、模块职责、数据流、
  12 条设计决策与取舍、已知限制、后续演进、测试策略）。
- **`pytest.ini`**：默认跳过 `slow` 标记；`requirements-optional.txt`：本地 ASR 可选依赖。
- 测试从 95 → **114**：新增 multipart 边界攻击回归、1MB 二进制往返、SRT 生成、
  设备选择、DPAPI 往返、清空凭据、日志端到端脱敏、抓包按头名脱敏。

### 修复

9. **multipart 解析对整段 body 做裸 `split(boundary)`** — 音频是二进制，里面可能恰好包含
   boundary 字节序列，导致文件被切碎、转写接口 500（**首次真实语音实测即命中**）。
   改为只在**行首**识别分隔符（前有 `\r\n`、后跟 `\r\n`/`--`）。
   一并修掉 `filename="x.mp3"` 尾引号未剥离的问题。
10. **`WhisperModel(device="auto")` 在 CPU-only ctranslate2 上仍选 CUDA** —
    报 `Library cublas64_12.dll is not found or cannot be loaded`，且错误发生在**推理阶段**
    （服务能起、健康检查 OK、一转写就 500，极难排查）。改为加载模型时做**真实 GPU 前向自检**。
11. **永久性 ASR 错误也会退避重试 4 次** — 上面的 DLL 错误导致客户端白等约 460s。
    改为按错误特征分类，永久错误立即失败并给出修正建议。
12. **本机 ASR 端点被硬要求填 API Key** — 项目自带的本地 ASR 服务不校验鉴权，
    但实现里 `if not api_key: raise`，导致「零成本离线转写」这条路径根本走不通
    （离线演示直接 `failed`）。改为：**本机端点（`localhost`/`127.0.0.1`/`::1`/`0.0.0.0`）
    允许空 Key 且不发送 `Authorization` 头**，远程端点仍强制要求，并在报错里教用户改写 Base URL。
    `probe_asr_endpoint()` 的自检同步修正；并加了防「`127.0.0.1.evil.com` 被误判为本机」的测试。
13. **`demo_offline.py` 占位转写取不到文本** — 音频文件名形如
    `<安全标题>__<resource_id>.<ext>`，而占位转写器按文件 stem 查表，导致永远命中不到。
    改为按 `__<id>` 后缀解析 resource_id。

---

## [0.1.0] — 2026-09-11

首个可用版本：完成核心库、GUI、命令行工具、打包交付、离线自检与安全加固；
真实账号验收待网络打通（需要校园网 / 学校 SSL-VPN + 一次人工登录）。

### 新增

**核心库 `src/ecnu_transcribe/`**

- `paths` — 开发态 / PyInstaller 冻结态路径解析。运行产物（`output/`、`cache/`、`data/`、`logs/`）
  跟应用根目录走；**敏感文件**（浏览器 profile、`storage_state.json`、`secrets.json`）固定放
  `%LOCALAPPDATA%\ecnu-transcribe\`，不进工作区。
- `logbus` — 统一日志总线（标准 logging + 轮转文件 + GUI 订阅）与**脱敏层**。
  脱敏规则：Cookie / `Authorization` 整行；带引号的敏感键；无歧义键名的 `key=value`；
  URL query 敏感键；按整段数字长度识别的手机号（11 位 `1[3-9]…`）、身份证（18 位含尾随 `X`）、
  学号（`10` 开头 10/11 位）；运行期登记的密钥与本人标识字面量。
- `errors` — 异常体系。`AuthExpiredError` / `ApiChangedError` / `DrmDetectedError` /
  `SiteUnreachableError` / `FfmpegMissingError` / `AsrNotConfiguredError` / `TaskCancelled` 等。
- `config` — `AppConfig`（60+ 可配项）+ `ConfigManager`。非敏感配置写 `config.json`；
  API Key 用 **Windows DPAPI**（`CryptProtectData`）加密写 `secrets.json`；
  DPAPI 不可用时**只留内存、不落盘**并显式告警。支持 `ECNU_ASR_API_KEY` / `ECNU_LLM_API_KEY` 环境变量。
- `store` — sqlite3 状态库（WAL）。三张表：`tasks`（含
  `id/course/resource_id/title/stage/progress/retry/error/output_dir/updated_at` 等 24 列）、
  `artifacts`（产物 sha256 审计）、`events`（状态迁移审计）。
  状态机：前进 + 任意阶段可到出口态（`failed/canceled/pending`）+ 少量显式允许的后退；
  非法迁移被拒并告警。`recover_orphans()` 在启动时把中间态任务回退到可续跑起点
  （有音频 → `audio_ready`，否则 `pending`）。
- `catalog` — `Course` / `Resource` / `Catalog` 模型。**宽容解析**（候选键 + 浅层路径）、
  时长支持 `"01:02:05"`、时间支持毫秒时间戳、mime 缺失时按 URL 猜；
  关键字段全缺时抛 `ApiChangedError`，**不产出空清单**。Windows 合法文件名清洗（含保留名）。
- `media` — **唯一**的 ffmpeg 出口。定位顺序：配置 → `ECNU_FFMPEG` → 随包 → PATH → `imageio-ffmpeg`。
  能力：探测（ffprobe 优先，退化解析 `ffmpeg -i`）、HLS/Header/Cookie 输入参数、
  `-vn -ac 1 -ar 16000 -c:a libmp3lame -b:a 64k` 输出、`silencedetect` 静音检测、
  按时长切段、sha256、DRM 标记扫描、进度行解析。子进程一律 `CREATE_NO_WINDOW`（打包后不弹黑窗）。
- `client` — `EcnuClient`：登录态从 `storage_state.json` 复用（Cookie + localStorage token 自动探测）；
  候选路径探测 + 缓存；有界深度优先的列表定位；`{code,msg,data}` 多层解包；
  业务码/消息 → `AuthExpiredError`；302 跳转分类（登录页 vs webVPN 网关）；
  自动翻页；请求间 300–800ms 抖动；脱敏接口记录写 `recon/network.jsonl`；
  `diagnose_access()` 输出人话版可达性诊断。
- `login` — Playwright **可见**浏览器人工登录（持久化 profile、
  `--disable-blink-features=AutomationControlled`）；CDP 请求/响应抓包并逐条脱敏；
  `storage_state` 落盘后收紧为「仅本用户可读写」；登录成功多信号判定
  （URL 特征 + 页面关键词 + localStorage token 键）；`probe_api_in_page()`
  为「动态签名」场景预留「借已登录页面上下文代打」的桥。
- `downloader` — ffmpeg 拉流取音频。HLS + AES-128（key 请求带同一份 Cookie/Header）、
  相对路径交给 ffmpeg 按 base URL 还原、超时重试 + 指数退避、`cache/media` 命中即跳过并校验时长、
  sha256、并发硬上限 2、可配限速、DRM 命中即停。
- `transcriber` — `Transcriber.transcribe(audio_path) -> Transcript(segments)` 抽象。
  三种实现：`DashScopeTranscriber`（OpenAI 兼容 + 原生异步，含 getPolicy/OSS 上传/轮询/取结果）、
  `OpenAICompatibleTranscriber`（`verbose_json` 不支持时自动回退 `srt`/`json`）、
  `FasterWhisperTranscriber`（本地兜底，按需导入）。切分：静音优先 + 重叠 1.5s +
  时间轴平移后按「文本重复 / 时间重叠」两类规则去重。`probe_asr_endpoint()` 做不发音频的鉴权自检。
- `llm` — DeepSeek 文本后处理。修复类要求返回 `{"segments":[{"i":..,"text":..}]}`，
  **只替换文本、保留原时间轴**；分段类返回 `{"break_after":[...]}`，以原片段为单位重组，
  绝不让模型重写时间戳；摘要类超长时分块摘要再汇总。任何失败都退化为原文并记录告警。
- `exporter` — 产出 `.txt` / `.srt` / `.md`（+ 便于二次加工的 `.transcript.json`）。
  纯文本按 800 字自然分段；SRT 单条 ≤ 12s / 60 字，按句子边界打包、
  极端情况按字数均分，并消除时间轴重叠；MD 含标题 + 元信息 + 摘要 + 带时间轴的全文。
  **只增不改**：覆盖前先备份 `*.bak-时间戳`。
- `pipeline` — 编排 `pending → probing → downloading → audio_ready → transcribing →
  post_processing → writing → done`。断点续跑：音频命中即跳过；转写结果落
  `<标题>.transcript.json`，音频 sha256 一致时直接复用（**不重复花 ASR 费用**）；
  `force=True` 才全量重跑。DRM / 登录失效 / 接口变更分别给出明确失败原因。

**GUI `src/app/`**

- `main.py` — 入口：高 DPI、中文字体、日志、配置、状态库恢复、`sys.excepthook` 兜底弹窗、
  `--selftest` 冒烟开关。冷启动自动 `recover_orphans()`。
- `workers.py` — 四个 `QThread`：`LoginWorker`（人工登录，支持「我已登录完成」）、
  `CatalogWorker`（拉清单）、`PipelineWorker`（执行队列，支持暂停/停止/重入队）、
  `ProbeWorker`（ffmpeg/站点/ASR/DeepSeek 自检）。全部通过信号回传，**不在工作线程碰 UI**。
- `ui/main_window.py` — 单窗口三栏：左（课程→录播树：搜索、全选/反选/只选未转写、
  时长合计、已勾选合计、状态着色）、中（任务队列：标题/课程/阶段/进度条/重试/耗时/错误详情/
  打开输出目录）、右（实时日志：复制/清空/自动滚动/日志文件）。
  顶部：登录 / 刷新清单 / 加入队列 / 开始 / 暂停 / 停止 / 设置 / 打开输出目录 / 导出清单 / 清除登录态。
  登录引导对话框（非模态、置顶）明确写清「需要手动登录一次」与安全说明。
  关窗时若有任务在跑会确认并请求停止，断点保留。
- `ui/settings_dialog.py` — 6 个标签页：语音识别（含 5 种预设与醒目提示
  「DeepSeek 没有语音转文字接口」）、文本加工（DeepSeek）、输出与产物、
  下载与媒体（ffmpeg 路径、并发、格式/码率/采样率/声道、缓存、保留音频/视频、重试、限速）、
  网络与登录（代理、TLS、超时、抖动、门户/接口/学号、登录态位置、校园网提示）、
  高级（运行信息、清缓存、自检输出）。

**命令行 `scripts/`**

- `login.py` — 交互式登录 / `--check` 检查登录态 / `--capture` 抓包。
- `fetch_catalog.py` — 纯 httpx 拉全量清单（M0/M1 验收工具），`--diagnose` / `--raw` /
  `--dump-recon` / `--json`；退出码区分登录失效（2）/ 站点不可达（3）/ 接口变更（4）。
- `run_one.py` — 端到端跑一条（`--index` / `--resource-id` / `--audio` / `--force`）。
- `selftest_e2e.py` — 22 项离线端到端自检：本地生成 **AES-128 加密 HLS** 并用真实 ffmpeg 拉流，
  验证时长偏差、缓存命中、DRM 拦截、三产物写出、断点续跑不重下不重跑。

**测试与文档**

- `tests/` — 102 个 pytest 用例（全部离线、fixture 驱动）：清单解析（多种字段命名）、
  切分规划、时间轴合并去重、ASR 响应解析（verbose_json / srt）、SRT 切分、
  导出与备份语义、状态机与断点续跑、脱敏与凭据不落明文（含 DPAPI 往返、
  日志端到端脱敏、抓包按头名脱敏）、`.gitignore` 安全项。
- `README.md`（环境/安装/首启引导/API 申请/常见错误/合规声明）、
  `docs/API.md`（实测网络路径 + 接口候选 + 待抓包清单）、
  `PROGRESS.md`（里程碑/证据命令/阻塞）、本 `CHANGELOG.md`。
- `packaging/ecnu_transcribe.spec` — PyInstaller `--onedir`，内嵌 ffmpeg/ffprobe，
  排除无用 Qt 模块与科学计算栈；同时打包 `scripts/`，使 exe 自带 `--doctor` / `--login` / `--selftest`
  三个自检入口。
- `scripts/doctor.py` — 17 项环境/产物自检：冻结态路径、**真实跑一次 ffmpeg 转码**、
  DPAPI、状态库读写（含产物列落库）、Playwright Chromium、脱敏、配置读写。

### 新增（第 2 轮：打包交付与接口自愈）

- **打包产物** `dist\大夏学堂转写助手\大夏学堂转写助手.exe`（`--onedir`，内嵌
  ffmpeg 8.1.1 与 ffprobe）。冻结态自检 **17 项全部通过**，`--selftest` 启动退出码 0。
- **浏览器接口嗅探兜底**（`login.sniff_api_paths` + `EcnuClient._sniff_endpoints`）：
  候选接口路径全部失败时，自动借已登录持久化浏览器打开一次页面，
  用 CDP 观察平台自身调用的 XHR/Fetch JSON 接口，启发式分类为
  课程/资源/播放路径，落盘 `recon/sniffed_endpoints.json`，再用真实路径重试
  （只嗅探 `*.ecnu.edu.cn`，每次运行最多一次，失败回退候选路径）。
  接口变更从「只报错」升级为「自动纠正」。
- **登录卡住的定向诊断**（`LoginSession._diagnose_stuck`）：按最后停留页面给出可照做的处理
  （停在 SSO → 完成认证；被 `vpn_key` 拦下 → 先连校园网/VPN；窗口被关 → 重新点登录）。
- **头部按名脱敏**（`login.redact_headers`）：`authorization` / `cookie` / `x-api-key` /
  `x-token` 等按键名整段替换 —— 值的形态不可预测，靠键名判定才可靠。
- **exe 命令行入口**：`--doctor`（自检）/ `--login`（命令行人工登录）/ `--selftest`（GUI 冒烟）。
- 7 个新的安全回归测试：DPAPI 往返、清空凭据、日志端到端脱敏、
  抓包按头名脱敏、`SessionState.summary()` 不泄漏值。

### 修复（第 2 轮：打包缺陷）

7. **Playwright 在冻结态把浏览器目录算到包内** — 报
   `Executable doesn't exist at _internal\playwright\driver\package\.local-browsers\chromium_headless_shell-1148`，
   导致**打包版点「登录」直接失败**（等于交付物不可用）。改为启动时自动把
   `PLAYWRIGHT_BROWSERS_PATH` 指向 `%LOCALAPPDATA%\ms-playwright`（先探测该目录下确有
   `chromium*` 才设置）；`--doctor` 增加该项检查与安装指引。
8. **抓包记录只对「值」做正则脱敏** — `Authorization: Bearer xxx`、`X-Api-Key: yyy`
   这类「值本身不含敏感词」的头会原文落盘。改为 `redact_headers()` 按**键名**判定整段替换。

### 修复（第 1 轮：离线自检暴露的 6 个真实缺陷）

1. **`StateStore.update_stage` 静默丢弃 `outputs` / `meta`** — 校验用的字段集合是**数据库列名**
   （`outputs_json`），而调用方传的是 **dataclass 字段名**（`outputs`），于是产物列表永远落不了库，
   GUI 也拿不到输出路径。改为同时接受两种名字，未知字段改为显式告警。
2. **`logbus.redact()` 不读取已登记的密钥集合** — `register_secret()` 登记的 API Key
   只有经过 `_RedactingFormatter` 才会被替换，直接调用 `redact()` 的路径（如网络记录脱敏）
   会漏掉。改为在 `redact()` 内部合并 `_EXTRA_SECRETS`。
3. **脱敏正则误伤正常日志** — 无引号的 `key=value` 规则会把
   `storage_state = D:\app\storage_state.json` 里的 `state` 当作敏感键。
   改为：无引号形式只匹配**无歧义**键名，且要求 `=` 两侧无空格。
4. **数字标识识别依赖 `(?!\d)` lookahead，行为不可靠** — 在同一解释器上出现
   「`\d(?!\d)` 匹配 `'12'` 为真、但 `10\d{8}(?!\d)` 匹配 11 位学号为假」的不一致表现。
   改为不依赖 lookaround：扫描整段数字串，**按长度 + 前缀**判定，并单独处理身份证尾随 `X`。
5. **`plan_chunks` 收尾会产出几秒的碎片段** — 白多一次 ASR 调用。改为剩余尾巴过短时并入上一段。
6. **DRM 预检在命中音频缓存时被跳过** — 用同一个资源先跑成功、再指向 DRM 流时会直接返回缓存，
   绕过 DRM 判定。改为预检在缓存判定之前生效，并给 `fetch()` 增加 `force` 开关。

一并修正的次要问题：

- `pick()` 把 `0` 当空值（`total=0`、`duration=0` 会被跳过）→ 只有 `None`/空白/空容器才算没值。
- `extract_list()` 固定层级解析，遇 `result.pageData.list` 这类多层包装即失败 → 改为有界 DFS。
- SRT 超长句被切成十几个碎片条目 → 先按句子打包，仍不满足约束时按字数均分。
- `_is_duplicate()` 时间门过窄（`overlap+2s`），重叠区里的重复句可能漏去重 → 文本比对的时间门
  放宽到 30s，同时保留「相隔很远的相同短句视为真实重复」的语义。

### 已知限制

- **真实账号验收未完成**：本机所在网络到 `courses.ecnu.edu.cn` 被学校 webVPN 网关拦下
  （`302 → proxy.ecnu.edu.cn/vpn_key/update?reason=site+...+not+found`），
  需接入校园网或学校 SSL-VPN 才能做真实清单抓取与端到端验收。
  因此 `docs/API.md` 中的业务接口路径/字段仍标为「⬜ 待抓包」，
  客户端采用「候选路径探测 + 浏览器嗅探 + 宽容解析 + 显式变更检测」的策略等待校正。
  已实测：浏览器能走到 `sso.ecnu.edu.cn` 的「统一身份认证平台」登录页，链路是通的。
- 真实录播的端到端转写（M2/M3 的真实资源验收）待网络打通 + 配置 DashScope API Key 后进行。
- 本地 `faster-whisper` 兜底实现需要用户自行 `pip install faster-whisper`
  （未内置在默认依赖里，避免为可选功能拖入 1GB 级模型与依赖）。
- 打包版**不内嵌 Chromium**（约 150 MB）：开发机装一次即可全机共用；
  无 Python 的目标机可下载官方 Chromium 后设置 `PLAYWRIGHT_BROWSERS_PATH`。
  `--doctor` 会显式检查这一项并给出指引。

### 平台接口变化记录

| 日期 | 变化 | 影响 | 处理 |
| --- | --- | --- | --- |
| 2026-09-11 | 首次侦察：`courses.ecnu.edu.cn` 校外访问被 webVPN 网关拦截 | 校外必须走 SSL-VPN/校园网 | 新增 `SiteUnreachableError` 的 webVPN 分类与人话提示；`diagnose_access()` 显式呈现 |
| 2026-09-11 | 统一身份认证链路确认为 `proxy.ecnu.edu.cn → api.ecnu.edu.cn/oauth2 → sso.ecnu.edu.cn`（企业微信式 OAuth2，`client_id=d46ba84ffc58611f`） | 登录必须人工完成，不能纯自动化 | 采用「人工登录一次 + 持久化 profile + `storage_state` 复用」方案 |
