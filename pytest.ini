[pytest]
testpaths = tests
# 默认跳过 slow（需要真实加载模型/耗时的用例）；跑全部用： pytest -m ""
addopts = -m "not slow" -q
markers =
    slow: 需要加载模型 / 耗时较长的测试（默认跳过，用 `pytest -m ""` 跑全部）
filterwarnings =
    ignore::DeprecationWarning
